# Quick Task 260425-w1z: Fix missing job-targeting cv_version_source enum value

## Scope

1. Add the missing `job-targeting` value to the canonical `cv_version_source` schema/migration contract.
2. Add a regression test in `cv-versions` so targeted rewrite versions keep their own source label.
