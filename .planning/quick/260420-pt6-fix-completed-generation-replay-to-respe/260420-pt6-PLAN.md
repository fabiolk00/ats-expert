# Quick Task 260420-pt6 Plan

## Goal

Close the completed-generation replay bypass so locked free-trial viewers never receive real signed artifact URLs, while paid viewers still benefit from safe replay without spending another credit.

## Tasks

1. Gate completed replay before signing artifact URLs and centralize replay result assembly behind viewer-aware preview access.
2. Reuse persisted preview lock state from the session or target so upgrading later still requires regeneration for previously locked artifacts.
3. Add a guardrail that rejects real artifact URLs whenever preview access is locked, and update regression tests to cover locked replay, paid replay, and post-upgrade replay.
