# Quick Task 260415-w9f Plan

## Goal

Redistribute the resume setup editor so the center column feels fuller, the action buttons stay anchored at the bottom, and the collapsed sections use the available space more intentionally.

## Tasks

1. Rework the main editor column layout in `user-data-page.tsx` so the editor lives inside a larger content surface with a dedicated bottom action bar.
2. Update `visual-resume-editor.tsx` so collapsed sections switch to a distributed overview layout instead of stacking compact rows that leave a large empty area.
3. Refresh the focused setup-page test to assert the new bottom action bar placement and keep the save button styling contract covered.
