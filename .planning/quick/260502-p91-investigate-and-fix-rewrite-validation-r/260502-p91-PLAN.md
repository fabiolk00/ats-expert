# Quick Task 260502-p91: Investigate and Fix Rewrite Validation Rewrite Failed in Shadow Batch

**Date:** 2026-05-02
**Status:** Completed

## Goal

Diagnose why `--include-rewrite-validation` returned only generic `rewrite_failed` results in the Job Targeting shadow batch and make the batch output actionable without generating artifacts or consuming user credits.

## Tasks

1. Reproduce one synthetic case with direct `rewriteResumeFull` diagnostics.
2. Preserve rewrite section error codes and failed section metadata from `rewriteResumeFull`.
3. Add safe batch logs for rewrite validation start/completion/failure.
4. Classify rewrite validation failures into specific issue types.
5. Generate a transparent synthetic trace fallback when a synthetic smoke run cannot call the model, so trace/structured validation still execute without pretending the LLM rewrite succeeded.
6. Keep cutover conservative by flagging rewrite operational issues in the analyzer.
7. Add regression tests and rerun synthetic 5/25 rewrite-validation samples.

## Verification

- Focused Vitest suite for runner/analyzer/rewrite diagnostics.
- `npm run typecheck`
- `npm test`
- Synthetic 5 rewrite-validation batch.
- Synthetic 25 rewrite-validation batch and analyzer report.

