# Quick Task 260425-u8e: Harden /api/file session lookup, retry, observability, and transient download UX

## Goal

Reduce false 404s and masked database failures in `/api/file/[sessionId]` while keeping the existing product flow intact.

## Tasks

1. Add a typed session lookup result that distinguishes `not_found` from real lookup failures.
2. Use that lookup inside `/api/file` with a short retry window plus richer structured logs.
3. Treat transient lookup failures as guided syncing states in the comparison and preview UIs.
4. Cover the new behavior with focused route, domain, and UI tests.
