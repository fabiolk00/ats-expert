# Quick Task 260414-u6l: Fix pnpm frozen lockfile drift and classify pdf_import_jobs in database convention audit

## Goal

Unblock CI by syncing `pnpm-lock.yaml` with the current root `package.json` and classifying the new `pdf_import_jobs` table in the database convention audit.

## Tasks

1. Update `src/lib/db/schema-guardrails.ts` so `pdf_import_jobs` follows the same convention profile as the other async job tables.
2. Refresh `pnpm-lock.yaml` from the current manifest without changing dependency intent.
3. Re-run the relevant install/audit commands and capture the outcome.
