# Quick Task 260420-p47 Plan

## Goal

Close the real free-trial preview bypasses by making preview lock persistence reliable in the synchronous generation path and by preventing timeline/version/compare APIs from returning real derived resume snapshots.

## Tasks

1. Centralize the `PreviewAccess` contract and helper logic for generated outputs and persisted patches.
2. Ensure synchronous `generateBillableResume` applies the same preview lock to both the returned output and the persisted patch.
3. Add central snapshot sanitization for version timeline and compare readers.
4. Apply sanitization to `session/[id]`, `session/[id]/versions`, and `session/[id]/compare`.
5. Add regression tests for synchronous persistence, locked timeline entries, locked compare responses, and sanitized workspace reads.
