# Quick Task 260424-f0q: fix new mojibake findings from audit:copy-regression

**Gathered:** 2026-04-24
**Status:** Completed

## Scope

Fix the new mojibake and PT-BR copy regressions introduced beyond the committed baseline so `audit:copy-regression` passes again.

## Tasks

1. Normalize the three newly introduced mojibake strings in billable-generation and tool tests/runtime messages.
2. Fix the three new PT-BR copy review findings in `route.model-selection.test.ts`.
3. Re-run `audit:copy-regression` and `typecheck`.
