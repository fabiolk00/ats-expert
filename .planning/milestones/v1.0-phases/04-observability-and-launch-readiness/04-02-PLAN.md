---
phase: 04-observability-and-launch-readiness
plan: "02"
type: execute
wave: 2
depends_on:
  - "01"
files_modified:
  - src/hooks/use-session-documents.ts
  - src/components/dashboard/session-documents-panel.tsx
  - src/components/dashboard/session-documents-panel.test.tsx
  - src/app/api/profile/extract/route.ts
  - src/app/api/profile/status/[jobId]/route.ts
  - src/components/resume/resume-builder.tsx
  - src/app/api/profile/extract/route.test.ts
  - src/app/api/profile/status/[jobId]/route.test.ts
autonomous: true
requirements:
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "Generated-artifact retrieval failures no longer collapse silently into an empty documents state."
    - "LinkedIn import start and polling failures return safe, localised, actionable copy."
    - "New user-facing failure messages preserve the existing API shape or extend it compatibly."
  artifacts:
    - path: "src/hooks/use-session-documents.ts"
      provides: "Actionable document-fetch error handling"
      contains: "Nao foi possivel carregar os documentos"
    - path: "src/app/api/profile/status/[jobId]/route.ts"
      provides: "Safe client-facing import failure message"
      contains: "errorMessage"
    - path: "src/components/resume/resume-builder.tsx"
      provides: "Import toast handling that uses the safe server-provided message"
      contains: "Confira se o link"
  key_links:
    - from: "src/hooks/use-session-documents.ts"
      to: "src/components/dashboard/session-documents-panel.tsx"
      via: "hook error state finally becomes visible in the panel"
      pattern: "error"
    - from: "src/app/api/profile/status/[jobId]/route.ts"
      to: "src/components/resume/resume-builder.tsx"
      via: "polling response carries safe failure guidance to the toast layer"
      pattern: "failed"
    - from: "src/app/api/profile/extract/route.ts"
      to: "src/app/api/profile/extract/route.test.ts"
      via: "route tests protect localized safe startup errors"
      pattern: "linkedin"
---

<objective>
Tighten user-facing error translation on the launch funnel so failures are visible, actionable, and safe.

Purpose: After observability is standardized, the next step is making sure users see meaningful guidance instead of silent empty states or opaque generic errors.
Output: Actionable document retrieval errors, safer LinkedIn import messaging, and tests covering the new client-facing behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-launch-readiness/04-RESEARCH.md
@.planning/phases/04-observability-and-launch-readiness/04-VALIDATION.md
@AGENTS.md
@src/hooks/use-session-documents.ts
@src/components/dashboard/session-documents-panel.tsx
@src/components/dashboard/preview-panel.tsx
@src/lib/dashboard/workspace-client.ts
@src/app/api/file/[sessionId]/route.ts
@src/app/api/profile/extract/route.ts
@src/app/api/profile/status/[jobId]/route.ts
@src/lib/linkedin/import-jobs.ts
@src/components/resume/resume-builder.tsx
@src/app/api/profile/extract/route.test.ts
@src/app/api/profile/status/[jobId]/route.test.ts
</context>

<threat_model>
<trust_boundaries>
- Authenticated user -> generated-document retrieval and preview flow
- Authenticated user -> LinkedIn import start route and polling loop
- Internal import-job state -> user-safe error translation returned to the browser
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-04-04</threat_id>
  <category>Information Disclosure</category>
  <component>Import failure messages</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Translate backend and provider failures into safe, actionable user copy instead of returning raw DB or provider messages to the browser.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-04-05</threat_id>
  <category>Denial of Service</category>
  <component>Documents panel</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Do not swallow fetch failures into an empty state; surface a retryable UI error so users know the artifacts are unavailable temporarily rather than missing entirely.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-04-06</threat_id>
  <category>Spoofing</category>
  <component>Client error handling</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve authenticated route ownership checks and response statuses while only improving the error copy and client-side handling of trusted server responses.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Stop silent generated-file failures in the dashboard documents flow</name>
  <files>src/hooks/use-session-documents.ts, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/session-documents-panel.test.tsx</files>
  <read_first>src/hooks/use-session-documents.ts, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/preview-panel.tsx, src/lib/dashboard/workspace-client.ts, src/app/api/file/[sessionId]/route.ts</read_first>
  <action>Remove the swallowed `getDownloadUrls(...).catch(...)` path from `useSessionDocuments`, preserve the current empty state only when the endpoint genuinely reports no files, and surface an actionable retryable error string when the fetch fails. If needed, extend `SessionDocumentsPanel` with a small retry action so users can recover without leaving the workspace. Add focused component coverage proving broken file fetches produce an error state instead of disappearing the panel.</action>
  <verify>npm test -- src/components/dashboard/preview-panel.test.tsx src/components/dashboard/session-documents-panel.test.tsx src/components/dashboard/resume-workspace.test.tsx</verify>
  <acceptance_criteria>
    - file-endpoint failures no longer collapse into a false empty state
    - the documents panel exposes an actionable error or retry affordance
    - successful empty states still render as empty rather than error
    - tests cover the difference between no files available and file-fetch failure
  </acceptance_criteria>
  <done>Users can tell the difference between “no generated files yet” and “the file service failed right now.”</done>
</task>

<task type="auto">
  <name>Task 2: Return safe, actionable LinkedIn import errors from start and polling routes</name>
  <files>src/app/api/profile/extract/route.ts, src/app/api/profile/status/[jobId]/route.ts, src/components/resume/resume-builder.tsx, src/app/api/profile/extract/route.test.ts, src/app/api/profile/status/[jobId]/route.test.ts</files>
  <read_first>src/app/api/profile/extract/route.ts, src/app/api/profile/status/[jobId]/route.ts, src/lib/linkedin/import-jobs.ts, src/components/resume/resume-builder.tsx, src/app/api/profile/extract/route.test.ts, src/app/api/profile/status/[jobId]/route.test.ts</read_first>
  <action>Localize and tighten the profile import route errors so authentication, invalid body, invalid LinkedIn URL, job-start failure, polling failure, and failed terminal job states all return safe user-facing messages in Portuguese. Extend the polling route to return a compatible safe `errorMessage` for failed jobs, wire `resume-builder.tsx` to use it, and keep raw backend details only in structured logs. Update the existing route tests to lock the new copy and compatibility in place.</action>
  <verify>npm test -- src/app/api/profile/extract/route.test.ts src/app/api/profile/status/[jobId]/route.test.ts</verify>
  <acceptance_criteria>
    - profile import routes no longer return English-only generic fallback copy for user-visible failures
    - failed import jobs return a safe message that helps the user retry without exposing internals
    - the resume-builder polling flow uses the safe server-provided message when available
    - route tests cover startup, polling, and failed terminal states
  </acceptance_criteria>
  <done>Profile import failures are understandable and actionable for users instead of dead-end generic toasts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/components/dashboard/preview-panel.test.tsx src/components/dashboard/session-documents-panel.test.tsx src/components/dashboard/resume-workspace.test.tsx src/app/api/profile/extract/route.test.ts src/app/api/profile/status/[jobId]/route.test.ts`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Users now see actionable safe guidance for document retrieval and LinkedIn import failures
  </success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-launch-readiness/04-02-SUMMARY.md`
</output>
