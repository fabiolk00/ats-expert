---
phase: 04-observability-and-launch-readiness
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/session/route.ts
  - src/app/api/session/[id]/messages/route.ts
  - src/app/api/file/[sessionId]/route.ts
  - src/app/api/webhook/clerk/route.ts
  - src/app/(auth)/layout.tsx
  - src/app/(auth)/dashboard/page.tsx
  - src/app/(auth)/settings/page.tsx
  - src/lib/asaas/optional-billing-info.ts
  - src/app/api/session/route.test.ts
  - src/app/api/session/[id]/messages/route.test.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/app/api/webhook/clerk/route.test.ts
  - src/app/(auth)/layout.test.tsx
autonomous: true
requirements:
  - OBS-01
user_setup: []
must_haves:
  truths:
    - "Session list, session messages, file signing, and Clerk webhook failures emit structured JSON logs with request or entity context."
    - "Optional billing metadata reads on authenticated pages stop using raw console output and log a consistent non-blocking event with page context."
    - "Tests prove the structured log helpers are used on the newly hardened paths."
  artifacts:
    - path: "src/app/api/session/route.ts"
      provides: "Structured logging for list and forbidden session endpoints"
      contains: "api.session.list_failed"
    - path: "src/app/api/session/[id]/messages/route.ts"
      provides: "Structured logging for session message retrieval failures"
      contains: "api.session.messages_failed"
    - path: "src/app/api/file/[sessionId]/route.ts"
      provides: "Structured logging for generated-file signing failures"
      contains: "api.file.download_urls_failed"
    - path: "src/app/api/webhook/clerk/route.ts"
      provides: "Structured logging for Clerk webhook config, verification, duplicate, and handler failures"
      contains: "clerk.webhook"
    - path: "src/lib/asaas/optional-billing-info.ts"
      provides: "Shared optional billing-info loader with structured diagnostics"
      contains: "billing.info.load_failed"
  key_links:
    - from: "src/lib/asaas/optional-billing-info.ts"
      to: "src/app/(auth)/layout.tsx"
      via: "shared optional billing read for authenticated surfaces"
      pattern: "billing.info.load_failed"
    - from: "src/app/api/file/[sessionId]/route.ts"
      to: "src/components/dashboard/preview-panel.tsx"
      via: "same signed-url contract used by preview and documents panel"
      pattern: "Generated resume artifacts could not be retrieved."
    - from: "src/app/api/webhook/clerk/route.ts"
      to: "src/app/api/webhook/clerk/route.test.ts"
      via: "test coverage for structured logging on critical webhook failures"
      pattern: "clerk.webhook"
---

<objective>
Standardize structured observability on the remaining fragile launch routes and non-blocking billing metadata reads.

Purpose: Phase 4 starts by making the sensitive failure paths diagnosable before any more UX-facing hardening lands.
Output: Shared optional billing-info loader, structured route logs on the remaining fragile server edges, and tests proving the new event names and fallback behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-launch-readiness/04-RESEARCH.md
@.planning/phases/04-observability-and-launch-readiness/04-VALIDATION.md
@AGENTS.md
@src/lib/observability/structured-log.ts
@src/app/api/session/route.ts
@src/app/api/session/[id]/messages/route.ts
@src/app/api/file/[sessionId]/route.ts
@src/app/api/webhook/clerk/route.ts
@src/app/(auth)/layout.tsx
@src/app/(auth)/dashboard/page.tsx
@src/app/(auth)/settings/page.tsx
@src/lib/asaas/quota.ts
@src/app/api/webhook/asaas/route.ts
@src/app/api/checkout/route.ts
</context>

<threat_model>
<trust_boundaries>
- Authenticated user requests -> server routes returning session, message, and file data
- Clerk webhook ingress -> app auth/bootstrap side effects
- Authenticated server-rendered pages -> optional billing metadata lookup and display
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-04-01</threat_id>
  <category>Information Disclosure</category>
  <component>Structured logs</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve the current flat JSON logger, serialize only safe scalar request and entity context, and avoid stacks, raw payloads, tokens, and file URLs.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-04-02</threat_id>
  <category>Repudiation</category>
  <component>Fragile route failures</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Emit consistent event names plus request or entity identifiers so operators can trace what failed without grepping ad-hoc text logs.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-04-03</threat_id>
  <category>Denial of Service</category>
  <component>Optional billing metadata reads</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Treat billing reads as optional, keep the workspace rendering on failure, and log the degradation as a warning instead of crashing authenticated pages.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace ad-hoc fragile-route logging with structured observability</name>
  <files>src/app/api/session/route.ts, src/app/api/session/[id]/messages/route.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/webhook/clerk/route.ts, src/app/api/session/route.test.ts, src/app/api/session/[id]/messages/route.test.ts, src/app/api/file/[sessionId]/route.test.ts, src/app/api/webhook/clerk/route.test.ts</files>
  <read_first>src/lib/observability/structured-log.ts, src/app/api/webhook/asaas/route.ts, src/app/api/checkout/route.ts, src/app/api/session/route.ts, src/app/api/session/[id]/messages/route.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/webhook/clerk/route.ts, src/app/api/file/[sessionId]/route.test.ts, src/app/api/webhook/clerk/route.test.ts</read_first>
  <action>Update the session list route, session messages route, generated-file signing route, and Clerk webhook route to use `logInfo`, `logWarn`, `logError`, and `serializeError` instead of raw `console.*`. Include request-safe context such as request method, request path, app user id, session id, target id, svix id, event type, and duplicate markers where relevant. Add or extend route tests so failures prove the new structured logging paths execute without changing the public HTTP contract.</action>
  <verify>npm test -- src/app/api/session/route.test.ts src/app/api/session/[id]/messages/route.test.ts src/app/api/file/[sessionId]/route.test.ts src/app/api/webhook/clerk/route.test.ts</verify>
  <acceptance_criteria>
    - the touched routes no longer emit raw `console.error`, `console.warn`, or `console.log`
    - route log events include stable event names and useful request or entity fields
    - existing response statuses and user-facing error payloads remain backward compatible
    - targeted route tests cover at least one structured logging failure path per touched area
  </acceptance_criteria>
  <done>Fragile route failures are diagnosable with the shared structured logger instead of ad-hoc console strings.</done>
</task>

<task type="auto">
  <name>Task 2: Centralize optional billing-info diagnostics for authenticated pages</name>
  <files>src/lib/asaas/optional-billing-info.ts, src/app/(auth)/layout.tsx, src/app/(auth)/dashboard/page.tsx, src/app/(auth)/settings/page.tsx, src/app/(auth)/layout.test.tsx</files>
  <read_first>src/app/(auth)/layout.tsx, src/app/(auth)/dashboard/page.tsx, src/app/(auth)/settings/page.tsx, src/lib/asaas/quota.ts, src/app/(auth)/layout.test.tsx</read_first>
  <action>Create a small shared helper for optional billing metadata reads that wraps `getUserBillingInfo`, logs a structured warning event such as `billing.info.load_failed` with surface and app-user context, and returns a user-safe non-blocking notice string alongside `billingInfo`. Migrate the auth layout, dashboard page, and settings page to this helper so billing-read failures stop using raw console errors and the shell can render a consistent warning state while leaving the workspace usable.</action>
  <verify>npm test -- src/app/(auth)/layout.test.tsx</verify>
  <acceptance_criteria>
    - billing metadata failures on authenticated pages no longer use raw `console.error`
    - a shared helper owns the structured warning event and safe fallback message
    - the auth layout still renders its children when billing lookup fails
    - layout tests cover the degraded-but-rendered path
  </acceptance_criteria>
  <done>Authenticated pages keep working when billing metadata is unavailable and operators get structured diagnostics with surface context.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/app/api/session/route.test.ts src/app/api/session/[id]/messages/route.test.ts src/app/api/file/[sessionId]/route.test.ts src/app/api/webhook/clerk/route.test.ts src/app/(auth)/layout.test.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Fragile launch routes and optional billing reads now emit consistent structured logs with safe context
  </success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-launch-readiness/04-01-SUMMARY.md`
</output>
