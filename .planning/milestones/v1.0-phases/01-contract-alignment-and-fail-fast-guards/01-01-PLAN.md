---
phase: 01-contract-alignment-and-fail-fast-guards
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .env.example
  - .env.staging.example
  - README.md
  - docs/ENVIRONMENT_SETUP.md
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - OPS-01
  - OPS-03
user_setup:
  - service: deployment-platform
    why: "Phase 1 removes legacy env aliases from runtime and docs, so deployed environments must use the canonical names before rollout."
    env_vars:
      - name: OPENAI_API_KEY
        source: "Current hosting provider environment settings"
      - name: ASAAS_ACCESS_TOKEN
        source: "Current hosting provider environment settings; replace any legacy ASAAS_API_KEY entry"
      - name: ASAAS_WEBHOOK_TOKEN
        source: "Asaas dashboard webhook settings"
      - name: UPSTASH_REDIS_REST_URL
        source: "Current hosting provider environment settings; replace any legacy UPSTASH_REDIS_URL entry"
      - name: UPSTASH_REDIS_REST_TOKEN
        source: "Current hosting provider environment settings; replace any legacy UPSTASH_REDIS_TOKEN entry"
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk dashboard -> API Keys"
      - name: CLERK_SECRET_KEY
        source: "Clerk dashboard -> API Keys"
      - name: CLERK_WEBHOOK_SECRET
        source: "Clerk dashboard -> Webhooks -> Signing secret"
    dashboard_config:
      - task: "Rename any legacy runtime variables to the canonical names before deploying Phase 1."
        location: "Hosting provider project settings -> Environment Variables"
        details: "Preview, staging, and production must match `.env.example` exactly because this phase intentionally removes alias support."
must_haves:
  truths:
    - "Committed local and staging templates exist and list the canonical launch-contract variable names with placeholder values only."
    - "README, environment setup docs, and CI all point at the same canonical provider names used by the runtime modules."
  artifacts:
    - path: ".env.example"
      provides: "Canonical local-development template for the launch-critical contract"
      contains: "ASAAS_ACCESS_TOKEN"
    - path: ".env.staging.example"
      provides: "Canonical staging-validation template"
      contains: "STAGING_ASAAS_WEBHOOK_TOKEN"
    - path: ".github/workflows/ci.yml"
      provides: "CI env block aligned to runtime names"
      contains: "UPSTASH_REDIS_REST_URL"
    - path: "docs/ENVIRONMENT_SETUP.md"
      provides: "Developer guidance for the canonical env contract"
      min_lines: 40
    - path: "README.md"
      provides: "Quick-start instructions that reference the committed templates"
      contains: ".env.example"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: ".env.example"
      via: "shared canonical env names"
      pattern: "ASAAS_ACCESS_TOKEN|UPSTASH_REDIS_REST_URL|UPSTASH_REDIS_REST_TOKEN"
    - from: "README.md"
      to: ".env.example"
      via: "copy-from-template quick start"
      pattern: "\\.env\\.example"
---

<objective>
Create the committed env templates and repo-level contract references that make the launch-critical provider names unambiguous.

Purpose: Runtime code is already using the right provider names, but onboarding, CI, and operator-facing docs still drift from that contract.
Output: Real `.env.example` and `.env.staging.example` files plus aligned README, environment docs, and CI configuration.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-CONTEXT.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-RESEARCH.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-VALIDATION.md
@README.md
@docs/ENVIRONMENT_SETUP.md
@.github/workflows/ci.yml
@scripts/verify-staging.sh
@docs/staging/SETUP_GUIDE.md
@src/lib/openai/client.ts
@src/lib/asaas/client.ts
@src/lib/rate-limit.ts
@src/lib/db/supabase-admin.ts
@src/middleware.ts
@src/app/api/webhook/asaas/route.ts
@src/app/api/webhook/clerk/route.ts
</context>

<threat_model>
<trust_boundaries>
- Repo templates and checked-in docs -> developers, CI, and operators provisioning credentials
- Runtime env contract -> external providers (OpenAI, Asaas, Upstash, Clerk, Supabase)
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-01-01</threat_id>
  <category>Tampering</category>
  <component>README, env docs, CI, and templates</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Use the existing runtime names as the only canonical contract, remove legacy names from checked-in artifacts, and make the acceptance criteria grep-verifiable.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-02</threat_id>
  <category>Information Disclosure</category>
  <component>Committed env templates</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep placeholders only in `.env.example` and `.env.staging.example`, and avoid copying any real values from the local `.env` file while updating the templates.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create committed env templates from the live runtime contract</name>
  <files>.env.example, .env.staging.example</files>
  <read_first>README.md, docs/ENVIRONMENT_SETUP.md, docs/staging/SETUP_GUIDE.md, scripts/verify-staging.sh, src/lib/openai/client.ts, src/lib/asaas/client.ts, src/lib/rate-limit.ts, src/lib/db/supabase-admin.ts, src/middleware.ts, src/app/api/webhook/asaas/route.ts, src/app/api/webhook/clerk/route.ts</read_first>
  <action>Create `.env.example` and `.env.staging.example` in the repo root. `.env.example` must include the canonical launch-contract entries `OPENAI_API_KEY`, optional `OPENAI_BASE_URL`, `ASAAS_ACCESS_TOKEN`, `ASAAS_WEBHOOK_TOKEN`, optional `ASAAS_SANDBOX`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `NEXT_PUBLIC_SUPABASE_URL`, `SUPABASE_SERVICE_ROLE_KEY`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, and `CLERK_WEBHOOK_SECRET`, plus an explicitly optional `LINKDAPI_API_KEY` section. `.env.staging.example` must include `STAGING_DB_URL`, `STAGING_API_URL`, `STAGING_ASAAS_WEBHOOK_TOKEN`, and `STAGING_ASAAS_ACCESS_TOKEN`, with comments that mark which values are required for staging verification. Use placeholder values only; never copy real secrets from `.env`.</action>
  <verify>rg -n "OPENAI_API_KEY|ASAAS_ACCESS_TOKEN|ASAAS_WEBHOOK_TOKEN|UPSTASH_REDIS_REST_URL|UPSTASH_REDIS_REST_TOKEN|CLERK_WEBHOOK_SECRET|STAGING_ASAAS_WEBHOOK_TOKEN|STAGING_ASAAS_ACCESS_TOKEN" .env.example .env.staging.example</verify>
  <acceptance_criteria>
    - `.env.example` contains `ASAAS_ACCESS_TOKEN`, `ASAAS_WEBHOOK_TOKEN`, `UPSTASH_REDIS_REST_URL`, and `UPSTASH_REDIS_REST_TOKEN`
    - `.env.example` contains an explicit optional section for `OPENAI_BASE_URL`, `ASAAS_SANDBOX`, and `LINKDAPI_API_KEY`
    - `.env.staging.example` contains `STAGING_DB_URL`, `STAGING_API_URL`, `STAGING_ASAAS_WEBHOOK_TOKEN`, and `STAGING_ASAAS_ACCESS_TOKEN`
    - Neither template contains `ASAAS_API_KEY`, `UPSTASH_REDIS_URL`, or `UPSTASH_REDIS_TOKEN`
  </acceptance_criteria>
  <done>The repo has committed local and staging templates that mirror the live runtime contract and contain only placeholders.</done>
</task>

<task type="auto">
  <name>Task 2: Align README, environment guidance, and CI with the canonical names</name>
  <files>README.md, docs/ENVIRONMENT_SETUP.md, .github/workflows/ci.yml</files>
  <read_first>README.md, docs/ENVIRONMENT_SETUP.md, .github/workflows/ci.yml, .env.example, .env.staging.example</read_first>
  <action>Update `README.md` quick start to reference the committed `.env.example` template that now exists. Rewrite `docs/ENVIRONMENT_SETUP.md` so its examples use the same canonical provider names as the runtime and clearly distinguish required versus optional entries. In `.github/workflows/ci.yml`, replace `ASAAS_API_KEY`, `UPSTASH_REDIS_URL`, and `UPSTASH_REDIS_TOKEN` with `ASAAS_ACCESS_TOKEN`, `UPSTASH_REDIS_REST_URL`, and `UPSTASH_REDIS_REST_TOKEN`, and add the dummy non-secret values needed by strict runtime validation such as `OPENAI_API_KEY`, `ASAAS_WEBHOOK_TOKEN`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, and `CLERK_WEBHOOK_SECRET`. Keep LinkdAPI explicitly outside the required launch contract.</action>
  <verify>powershell -NoProfile -Command "if (rg -n 'ASAAS_API_KEY|UPSTASH_REDIS_URL|UPSTASH_REDIS_TOKEN' README.md docs/ENVIRONMENT_SETUP.md .github/workflows/ci.yml) { exit 1 } else { exit 0 }"</verify>
  <acceptance_criteria>
    - `README.md` references `.env.example` as an existing committed template
    - `docs/ENVIRONMENT_SETUP.md` documents `ASAAS_ACCESS_TOKEN`, `ASAAS_WEBHOOK_TOKEN`, `UPSTASH_REDIS_REST_URL`, and `UPSTASH_REDIS_REST_TOKEN`
    - `.github/workflows/ci.yml` contains `OPENAI_API_KEY`, `ASAAS_ACCESS_TOKEN`, `ASAAS_WEBHOOK_TOKEN`, `UPSTASH_REDIS_REST_URL`, `UPSTASH_REDIS_REST_TOKEN`, `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY`, `CLERK_SECRET_KEY`, and `CLERK_WEBHOOK_SECRET`
    - `README.md`, `docs/ENVIRONMENT_SETUP.md`, and `.github/workflows/ci.yml` no longer contain `ASAAS_API_KEY`, `UPSTASH_REDIS_URL`, or `UPSTASH_REDIS_TOKEN`
  </acceptance_criteria>
  <done>The repo boundary artifacts all advertise the same provider names the runtime actually reads.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `powershell -NoProfile -Command "if (rg -n 'ASAAS_API_KEY|UPSTASH_REDIS_URL|UPSTASH_REDIS_TOKEN' README.md docs/ENVIRONMENT_SETUP.md .github/workflows/ci.yml .env.example .env.staging.example) { exit 1 } else { exit 0 }"`
- [ ] `rg -n "OPENAI_API_KEY|ASAAS_ACCESS_TOKEN|ASAAS_WEBHOOK_TOKEN|UPSTASH_REDIS_REST_URL|UPSTASH_REDIS_REST_TOKEN" README.md docs/ENVIRONMENT_SETUP.md .github/workflows/ci.yml .env.example` returns expected matches
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Developers, CI, and operators can point at a single committed source of truth for the launch-critical env names
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-alignment-and-fail-fast-guards/01-01-SUMMARY.md`
</output>
