---
phase: 01-contract-alignment-and-fail-fast-guards
plan: "02"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/openai/client.ts
  - src/lib/asaas/client.ts
  - src/lib/rate-limit.ts
  - src/lib/db/supabase-admin.ts
  - src/app/api/webhook/asaas/route.ts
  - src/app/api/webhook/clerk/route.ts
  - src/lib/openai/client.test.ts
  - src/lib/asaas/client.test.ts
  - src/lib/rate-limit.test.ts
  - src/lib/db/supabase-admin.test.ts
  - src/app/api/webhook/asaas/route.test.ts
  - src/app/api/webhook/clerk/route.test.ts
  - src/lib/linkedin/linkdapi.test.ts
autonomous: true
requirements:
  - OPS-01
  - OPS-02
user_setup: []
must_haves:
  truths:
    - "Launch-critical provider modules throw actionable missing-config errors in non-test environments instead of silently degrading or depending on non-null assertions."
    - "Webhook authentication and replay-protection paths require their own dedicated secrets instead of borrowing unrelated credentials."
    - "Optional LinkdAPI behavior stays explicit and on-demand rather than becoming a startup blocker."
  artifacts:
    - path: "src/lib/openai/client.ts"
      provides: "OpenAI client with explicit non-test credential guard"
      contains: "OPENAI_API_KEY"
    - path: "src/lib/asaas/client.ts"
      provides: "Asaas client with explicit access-token guard"
      contains: "ASAAS_ACCESS_TOKEN"
    - path: "src/lib/rate-limit.ts"
      provides: "Lazy Upstash initialization with actionable errors"
      contains: "UPSTASH_REDIS_REST_URL"
    - path: "src/app/api/webhook/asaas/route.ts"
      provides: "Asaas webhook auth that requires a dedicated webhook token"
      contains: "ASAAS_WEBHOOK_TOKEN"
    - path: "src/app/api/webhook/clerk/route.ts"
      provides: "Clerk webhook path with explicit Redis and webhook-secret validation"
      contains: "CLERK_WEBHOOK_SECRET"
    - path: "src/lib/openai/client.test.ts"
      provides: "Regression coverage for OpenAI fail-fast behavior"
      min_lines: 20
  key_links:
    - from: "src/lib/openai/client.test.ts"
      to: "src/lib/openai/client.ts"
      via: "missing-env regression case"
      pattern: "OPENAI_API_KEY"
    - from: "src/app/api/webhook/asaas/route.test.ts"
      to: "src/app/api/webhook/asaas/route.ts"
      via: "dedicated webhook-token auth checks"
      pattern: "ASAAS_WEBHOOK_TOKEN"
    - from: "src/app/api/webhook/clerk/route.test.ts"
      to: "src/app/api/webhook/clerk/route.ts"
      via: "lazy Redis and webhook-secret validation"
      pattern: "CLERK_WEBHOOK_SECRET"
---

<objective>
Remove fail-open behavior from launch-critical provider modules and lock the stricter contract with focused regression tests.

Purpose: Phase 1 is not complete if missing credentials merely fail later or on the wrong code path. The runtime has to reject bad non-test setups immediately and predictably.
Output: Hardened provider modules plus focused Vitest coverage for the missing-config paths.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-CONTEXT.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-RESEARCH.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-VALIDATION.md
@src/lib/db/supabase-admin.ts
@src/lib/linkedin/linkdapi.ts
@src/lib/openai/client.ts
@src/lib/asaas/client.ts
@src/lib/rate-limit.ts
@src/app/api/webhook/asaas/route.ts
@src/app/api/webhook/clerk/route.ts
@src/lib/openai/client.test.ts
@src/app/api/webhook/asaas/route.test.ts
@src/lib/linkedin/linkdapi.test.ts
</context>

<threat_model>
<trust_boundaries>
- CurrIA server runtime -> OpenAI, Asaas, Upstash, Supabase, and Clerk provider APIs
- External webhook senders -> CurrIA webhook routes and replay-protection storage
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-01-03</threat_id>
  <category>Denial of Service</category>
  <component>OpenAI, Asaas, and Supabase config accessors</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Remove the OpenAI `test-key` fallback, replace non-null assertions with explicit guards, and keep strictness active for every non-test environment.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-04</threat_id>
  <category>Spoofing</category>
  <component>Asaas webhook authentication</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Require `ASAAS_WEBHOOK_TOKEN` explicitly instead of falling back to `ASAAS_ACCESS_TOKEN`, then cover the route behavior with regression tests.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-05</threat_id>
  <category>Denial of Service</category>
  <component>Redis-backed webhook and rate-limit paths</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Move Redis credential reads behind lazy getters with actionable errors so misconfigured non-test environments fail clearly while tests can still import the modules safely.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Replace permissive provider fallbacks with explicit non-test guards</name>
  <files>src/lib/openai/client.ts, src/lib/asaas/client.ts, src/lib/rate-limit.ts, src/lib/db/supabase-admin.ts, src/app/api/webhook/asaas/route.ts, src/app/api/webhook/clerk/route.ts</files>
  <read_first>src/lib/openai/client.ts, src/lib/asaas/client.ts, src/lib/rate-limit.ts, src/lib/db/supabase-admin.ts, src/app/api/webhook/asaas/route.ts, src/app/api/webhook/clerk/route.ts, src/lib/linkedin/linkdapi.ts</read_first>
  <action>Use `src/lib/db/supabase-admin.ts` as the baseline pattern and tighten the other provider modules to match it. Remove the OpenAI `test-key` fallback. Replace top-level non-null assertions in Redis-backed modules with lazy config access so the code throws actionable errors when invoked in local dev, preview, staging, or production, but still remains importable in tests. In `src/app/api/webhook/asaas/route.ts`, require `ASAAS_WEBHOOK_TOKEN` directly instead of falling back to `ASAAS_ACCESS_TOKEN`. Keep the error wording consistent and actionable: `Missing required environment variable <NAME> for <component>.` Do not turn `LINKDAPI_API_KEY` into a startup requirement.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - `src/lib/openai/client.ts` no longer contains the string `test-key`
    - `src/app/api/webhook/asaas/route.ts` no longer returns `process.env.ASAAS_ACCESS_TOKEN` from its expected-token helper
    - `src/lib/rate-limit.ts` and `src/app/api/webhook/clerk/route.ts` no longer rely on top-level `process.env.UPSTASH_REDIS_REST_URL!` or `process.env.UPSTASH_REDIS_REST_TOKEN!`
    - Hardened provider modules throw messages that include `Missing required environment variable` and the exact env name they need
  </acceptance_criteria>
  <done>Missing launch-critical config fails immediately and descriptively on the paths that actually need it.</done>
</task>

<task type="auto">
  <name>Task 2: Add focused Vitest coverage for the hardened contract</name>
  <files>src/lib/openai/client.test.ts, src/lib/asaas/client.test.ts, src/lib/rate-limit.test.ts, src/lib/db/supabase-admin.test.ts, src/app/api/webhook/asaas/route.test.ts, src/app/api/webhook/clerk/route.test.ts, src/lib/linkedin/linkdapi.test.ts</files>
  <read_first>src/lib/openai/client.test.ts, src/app/api/webhook/asaas/route.test.ts, src/lib/linkedin/linkdapi.test.ts, vitest.config.ts</read_first>
  <action>Add targeted tests that unset the relevant env vars, dynamically import or invoke the modules, and assert the exact actionable error messages for OpenAI, Asaas client, Supabase admin, Redis-backed limiter initialization, Clerk webhook verification, and Asaas webhook auth. Extend the existing webhook tests rather than replacing them. Keep one explicit regression in `src/lib/linkedin/linkdapi.test.ts` that proves LinkdAPI remains optional and only throws when its fetch path is used.</action>
  <verify>npm test -- src/lib/openai/client.test.ts src/lib/asaas/client.test.ts src/lib/rate-limit.test.ts src/lib/db/supabase-admin.test.ts src/app/api/webhook/asaas/route.test.ts src/app/api/webhook/clerk/route.test.ts src/lib/linkedin/linkdapi.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/openai/client.test.ts` contains `OPENAI_API_KEY`
    - `src/lib/asaas/client.test.ts` contains `ASAAS_ACCESS_TOKEN`
    - `src/lib/rate-limit.test.ts` contains `UPSTASH_REDIS_REST_URL`
    - `src/app/api/webhook/clerk/route.test.ts` contains `CLERK_WEBHOOK_SECRET`
    - `src/lib/linkedin/linkdapi.test.ts` contains `LINKDAPI_API_KEY`
  </acceptance_criteria>
  <done>The stricter runtime contract is protected by focused tests that fail fast if the project drifts back toward silent degradation.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/openai/client.test.ts src/lib/asaas/client.test.ts src/lib/rate-limit.test.ts src/lib/db/supabase-admin.test.ts src/app/api/webhook/asaas/route.test.ts src/app/api/webhook/clerk/route.test.ts src/lib/linkedin/linkdapi.test.ts`
- [ ] `powershell -NoProfile -Command "if (rg -n 'test-key' src/lib/openai/client.ts) { exit 1 } else { exit 0 }"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Every launch-critical provider path either has a dedicated required-env guard or already uses an equivalent explicit-throw pattern
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-alignment-and-fail-fast-guards/01-02-SUMMARY.md`
</output>
