---
phase: 03-billing-settlement-validation
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - scripts/replay-staging-asaas.ts
  - scripts/check-staging-billing-state.ts
  - scripts/verify-staging.sh
  - scripts/README.md
  - docs/staging/SETUP_GUIDE.md
  - docs/staging/VALIDATION_PLAN.md
  - docs/billing/OPS_RUNBOOK.md
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
user_setup: []
must_haves:
  truths:
    - "Operators can replay named settlement scenarios and snapshot staging billing state using committed repo-local tooling."
    - "Staging setup, preflight, and runbook docs agree on the current settlement contract, prerequisites, and proof flow."
    - "Phase 3 explicitly treats the checkout `externalReference` shape as a contract question to validate instead of assuming docs or code are already correct."
  artifacts:
    - path: "scripts/replay-staging-asaas.ts"
      provides: "Named webhook replay helper for staging settlement scenarios"
      contains: "one_time_settlement"
    - path: "scripts/check-staging-billing-state.ts"
      provides: "Staging billing state snapshot helper"
      contains: "credit_accounts"
    - path: "docs/staging/VALIDATION_PLAN.md"
      provides: "Phase 3 scenario matrix and operator validation steps"
      contains: "duplicate delivery"
    - path: "scripts/verify-staging.sh"
      provides: "Stage-zero preflight for billing validation"
      contains: "STAGING_ASAAS_WEBHOOK_TOKEN"
  key_links:
    - from: "scripts/replay-staging-asaas.ts"
      to: "src/app/api/webhook/asaas/route.ts"
      via: "same webhook endpoint used in staging"
      pattern: "/api/webhook/asaas"
    - from: "scripts/check-staging-billing-state.ts"
      to: "src/lib/asaas/quota.ts"
      via: "runtime and display balance evidence tables"
      pattern: "user_quotas"
    - from: "docs/staging/VALIDATION_PLAN.md"
      to: "docs/billing/IMPLEMENTATION.md"
      via: "shared contract and scenario language"
      pattern: "settlement"
---

<objective>
Turn the current docs-only staging billing flow into a runnable operator toolkit for Phase 3.

Purpose: Phase 3 cannot produce credible billing evidence until the repo has committed helpers for replaying named scenarios, snapshotting billing state, and guiding operators through the current settlement contract without hidden prerequisites.
Output: Staging replay tooling, billing snapshot tooling, and aligned setup and runbook documentation.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-billing-settlement-validation/03-RESEARCH.md
@.planning/phases/03-billing-settlement-validation/03-VALIDATION.md
@AGENTS.md
@.env.staging.example
@scripts/verify-staging.sh
@scripts/stress-test-setup.sh
@scripts/README.md
@docs/staging/SETUP_GUIDE.md
@docs/staging/VALIDATION_PLAN.md
@docs/billing/OPS_RUNBOOK.md
@docs/billing/IMPLEMENTATION.md
@src/app/api/webhook/asaas/route.ts
@src/app/api/checkout/route.ts
@src/lib/asaas/event-handlers.ts
@src/lib/asaas/credit-grants.ts
@src/lib/asaas/quota.ts
@src/lib/asaas/external-reference.ts
</context>

<threat_model>
<trust_boundaries>
- Operator workstation -> staging environment secrets and endpoints
- Replay helper -> live `/api/webhook/asaas` route in staging
- Snapshot helper -> staging database reads for billing evidence
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-03-01</threat_id>
  <category>Information Disclosure</category>
  <component>Staging replay tooling</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Load secrets from `.env.staging`, redact secret values from logs, and expose dry-run and list-only modes so operators can inspect behavior safely.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-02</threat_id>
  <category>Tampering</category>
  <component>Scenario payload definitions</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Commit named scenario fixtures in repo, keep identifiers explicit, and document exactly how each scenario maps to the current settlement contract.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-03</threat_id>
  <category>Denial of Service</category>
  <component>Half-configured operator environments</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep `bash scripts/verify-staging.sh` as the first step, document shell prerequisites, and fail early with actionable diagnostics before any replay attempt.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add repo-local staging replay and billing snapshot helpers</name>
  <files>scripts/replay-staging-asaas.ts, scripts/check-staging-billing-state.ts, scripts/README.md</files>
  <read_first>.env.staging.example, scripts/verify-staging.sh, scripts/stress-test-setup.sh, scripts/README.md, src/app/api/webhook/asaas/route.ts, src/lib/asaas/event-handlers.ts, src/lib/asaas/credit-grants.ts, src/lib/asaas/quota.ts, docs/billing/IMPLEMENTATION.md</read_first>
  <action>Create `scripts/replay-staging-asaas.ts` as a scenario-driven helper that loads `.env.staging`, targets `${STAGING_API_URL}/api/webhook/asaas`, authenticates with `STAGING_ASAAS_WEBHOOK_TOKEN`, and supports `--list-scenarios`, `--scenario <name>`, `--dry-run`, and `--output <file>` without printing raw secrets. Commit named scenarios covering `one_time_settlement`, `inactive_subscription_snapshot`, `initial_recurring_activation`, `renewal_replace_balance`, `cancellation_metadata_only`, `duplicate_delivery`, and `partial_success_reconcile`. Create `scripts/check-staging-billing-state.ts` to snapshot billing rows for a given `--user`, `--checkout`, or `--subscription`, returning JSON that includes at least `billing_checkouts`, `credit_accounts`, `user_quotas`, and `processed_events`. Update `scripts/README.md` so operators can run both helpers without consulting planning files.</action>
  <verify>tsx scripts/replay-staging-asaas.ts --list-scenarios</verify>
  <acceptance_criteria>
    - `scripts/replay-staging-asaas.ts` lists the named scenarios and exposes `--dry-run`
    - `scripts/check-staging-billing-state.ts` can print help text and mentions the required billing tables
    - helper logs do not print raw tokens or secrets
    - `scripts/README.md` documents the replay helper and snapshot helper
  </acceptance_criteria>
  <done>Phase 3 has committed operator tooling for replaying billing scenarios and capturing staging state.</done>
</task>

<task type="auto">
  <name>Task 2: Reconcile preflight and runbook docs around the current settlement contract</name>
  <files>scripts/verify-staging.sh, docs/staging/SETUP_GUIDE.md, docs/staging/VALIDATION_PLAN.md, docs/billing/OPS_RUNBOOK.md</files>
  <read_first>scripts/verify-staging.sh, docs/staging/SETUP_GUIDE.md, docs/staging/VALIDATION_PLAN.md, docs/billing/OPS_RUNBOOK.md, docs/billing/IMPLEMENTATION.md, src/lib/asaas/external-reference.ts, src/app/api/checkout/route.ts</read_first>
  <action>Update the staging setup guide, validation plan, and billing runbook so they all use the same Phase 3 proof path: install shell prerequisites, populate `.env.staging`, run `bash scripts/verify-staging.sh`, replay named scenarios with the committed helper, and snapshot resulting state. Keep `verify-staging.sh` as the required first step. Make shell dependencies explicit, note that Phase 3 must confirm the outgoing checkout `externalReference` shape because docs and runtime currently disagree, and ensure duplicate-delivery validation is clearly documented.</action>
  <verify>npm test -- src/lib/asaas/event-handlers.test.ts src/app/api/webhook/asaas/route.test.ts src/lib/asaas/credit-grants.test.ts src/lib/asaas/quota.test.ts src/app/api/checkout/route.test.ts</verify>
  <acceptance_criteria>
    - `scripts/verify-staging.sh` documents or checks the env variables needed by the replay flow
    - `docs/staging/SETUP_GUIDE.md` lists shell prerequisites and `.env.staging` preparation
    - `docs/staging/VALIDATION_PLAN.md` names the Phase 3 scenario matrix including duplicate delivery
    - `docs/billing/OPS_RUNBOOK.md` points operators to the committed replay and snapshot commands
    - at least one doc explicitly calls out the unresolved `externalReference` contract drift for validation
  </acceptance_criteria>
  <done>Operators have one consistent, documented preflight and replay path for Phase 3 instead of piecing it together from old scripts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/asaas/event-handlers.test.ts src/app/api/webhook/asaas/route.test.ts src/lib/asaas/credit-grants.test.ts src/lib/asaas/quota.test.ts src/app/api/checkout/route.test.ts`
- [ ] `tsx scripts/replay-staging-asaas.ts --list-scenarios`
- [ ] `tsx scripts/check-staging-billing-state.ts --help`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo contains committed tooling and aligned docs for staging billing replay and evidence capture
  </success_criteria>

<output>
After completion, create `.planning/phases/03-billing-settlement-validation/03-01-SUMMARY.md`
</output>
