---
phase: 03-billing-settlement-validation
plan: "03"
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - src/lib/asaas/event-handlers.ts
  - src/lib/asaas/credit-grants.ts
  - src/lib/asaas/quota.ts
  - src/app/api/webhook/asaas/route.ts
  - src/app/api/checkout/route.ts
  - src/lib/asaas/external-reference.ts
  - src/lib/asaas/event-handlers.test.ts
  - src/lib/asaas/credit-grants.test.ts
  - src/lib/asaas/quota.test.ts
  - src/app/api/webhook/asaas/route.test.ts
  - src/app/api/checkout/route.test.ts
  - src/lib/asaas/external-reference.test.ts
  - src/lib/asaas/checkout.test.ts
  - docs/billing/IMPLEMENTATION.md
  - docs/billing/OPS_RUNBOOK.md
  - docs/staging/VALIDATION_PLAN.md
  - .planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md
  - .planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
user_setup:
  - Keep the staging environment, replay helper, and snapshot helper available for targeted reruns
must_haves:
  truths:
    - "Every staging-found billing inconsistency is patched only at the owner files named by the evidence and covered by regression tests."
    - "Billing documentation is reconciled to the validated live contract instead of remaining split between docs and runtime behavior."
    - "Affected scenarios are rerun and marked resolved only after both local regressions and live staging evidence agree."
  artifacts:
    - path: ".planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md"
      provides: "Resolved and open status tracking for Phase 3 remediation"
      contains: "Resolved"
    - path: "src/lib/asaas/external-reference.test.ts"
      provides: "Regression coverage if checkout reference drift is confirmed to matter"
      contains: "curria:v1"
    - path: "docs/billing/IMPLEMENTATION.md"
      provides: "Validated billing contract documentation"
      contains: "settlement"
  key_links:
    - from: ".planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md"
      to: "src/lib/asaas/event-handlers.ts"
      via: "owner-file remediation mapping"
      pattern: "owner_files"
    - from: "src/app/api/checkout/route.ts"
      to: "src/lib/asaas/external-reference.ts"
      via: "checkout externalReference formatting"
      pattern: "externalReference"
    - from: ".planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md"
      to: "docs/billing/IMPLEMENTATION.md"
      via: "remediation outcome reflected in validated docs"
      pattern: "BILL-01"
---

<objective>
Fix only the billing inconsistencies that live staging surfaced, then lock the validated behavior into tests, docs, and rerun evidence.

Purpose: Phase 3 should not speculate about billing refactors. It should remediate the exact issues proven by staging, add the smallest durable regression coverage, and confirm the fixes against the same scenario matrix.
Output: Focused billing patches, updated tests and docs, and evidence showing gaps were resolved or explicitly left open.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-billing-settlement-validation/03-RESEARCH.md
@.planning/phases/03-billing-settlement-validation/03-VALIDATION.md
@.planning/phases/03-billing-settlement-validation/03-02-PLAN.md
@.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md
@.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md
@AGENTS.md
@docs/billing/IMPLEMENTATION.md
@docs/billing/OPS_RUNBOOK.md
@docs/staging/VALIDATION_PLAN.md
@src/lib/asaas/event-handlers.ts
@src/lib/asaas/credit-grants.ts
@src/lib/asaas/quota.ts
@src/app/api/webhook/asaas/route.ts
@src/app/api/checkout/route.ts
@src/lib/asaas/external-reference.ts
@src/lib/asaas/event-handlers.test.ts
@src/lib/asaas/credit-grants.test.ts
@src/lib/asaas/quota.test.ts
@src/app/api/webhook/asaas/route.test.ts
@src/app/api/checkout/route.test.ts
</context>

<threat_model>
<trust_boundaries>
- Staging evidence -> remediation scope selection
- Billing hotfixes -> credit safety and idempotency invariants
- Updated docs -> future operators relying on validated behavior
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-03-07</threat_id>
  <category>Regression</category>
  <component>Billing hotfixes</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Patch only owner files named by staging evidence, add targeted regression tests, and keep the broader billing suite green.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-08</threat_id>
  <category>Tampering</category>
  <component>Contract documentation</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Update docs only after remediation and rerun evidence confirm the live contract, especially for checkout reference formatting and settlement semantics.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-09</threat_id>
  <category>Integrity</category>
  <component>Remediation closure</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Mark a gap `Resolved` only when both local regressions and the affected staging rerun pass; otherwise leave it explicitly open.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Patch only the owner files named by the staging gaps register</name>
  <files>src/lib/asaas/event-handlers.ts, src/lib/asaas/credit-grants.ts, src/lib/asaas/quota.ts, src/app/api/webhook/asaas/route.ts, src/app/api/checkout/route.ts, src/lib/asaas/external-reference.ts</files>
  <read_first>.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md, .planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md, src/lib/asaas/event-handlers.ts, src/lib/asaas/credit-grants.ts, src/lib/asaas/quota.ts, src/app/api/webhook/asaas/route.ts, src/app/api/checkout/route.ts, src/lib/asaas/external-reference.ts</read_first>
  <action>Read `03-STAGING-GAPS.md` and patch only the billing owner files named there. If the gaps file says `No open gaps`, do not change billing logic. If the `externalReference` drift is confirmed to affect live behavior, reconcile the formatter, checkout path, and dependent assertions to the single validated shape instead of leaving docs and runtime split. Keep changes targeted to the staging-proven inconsistency set.</action>
  <verify>npm run typecheck</verify>
  <acceptance_criteria>
    - billing logic remains unchanged when the gaps file reports no open issues
    - any code remediation maps directly to a scenario and owner file named in the gaps register
    - checkout reference formatting is reconciled only if staging evidence proves it needs remediation
  </acceptance_criteria>
  <done>Only the staging-proven inconsistencies are patched, with no speculative billing refactor.</done>
</task>

<task type="auto">
  <name>Task 2: Add regression coverage and reconcile billing docs to the validated behavior</name>
  <files>src/lib/asaas/event-handlers.test.ts, src/lib/asaas/credit-grants.test.ts, src/lib/asaas/quota.test.ts, src/app/api/webhook/asaas/route.test.ts, src/app/api/checkout/route.test.ts, src/lib/asaas/external-reference.test.ts, src/lib/asaas/checkout.test.ts, docs/billing/IMPLEMENTATION.md, docs/billing/OPS_RUNBOOK.md, docs/staging/VALIDATION_PLAN.md</files>
  <read_first>.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md, docs/billing/IMPLEMENTATION.md, docs/billing/OPS_RUNBOOK.md, docs/staging/VALIDATION_PLAN.md, src/app/api/checkout/route.test.ts, src/lib/asaas/event-handlers.test.ts, src/lib/asaas/credit-grants.test.ts, src/lib/asaas/quota.test.ts</read_first>
  <action>Add or tighten the regression tests needed to lock in the remediation, using the smallest relevant test files from the billing suite. If the checkout reference contract changed, add or update `src/lib/asaas/external-reference.test.ts` and `src/lib/asaas/checkout.test.ts` so the validated shape is explicit. Reconcile `docs/billing/IMPLEMENTATION.md`, `docs/billing/OPS_RUNBOOK.md`, and `docs/staging/VALIDATION_PLAN.md` so operators and future contributors read the same contract the code now enforces.</action>
  <verify>npm test -- src/lib/asaas/event-handlers.test.ts src/app/api/webhook/asaas/route.test.ts src/lib/asaas/credit-grants.test.ts src/lib/asaas/quota.test.ts src/app/api/checkout/route.test.ts src/lib/asaas/external-reference.test.ts src/lib/asaas/checkout.test.ts</verify>
  <acceptance_criteria>
    - every code remediation has at least one targeted regression assertion
    - docs describe the validated live behavior rather than stale or speculative contract details
    - checkout reference tests exist if that contract was part of the remediation
  </acceptance_criteria>
  <done>The remediation is durable because tests and docs now reflect the validated contract.</done>
</task>

<task type="manual">
  <name>Task 3: Rerun affected staging scenarios and close gaps only after proof</name>
  <files>.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md, .planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md</files>
  <read_first>.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md, .planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md, scripts/replay-staging-asaas.ts, scripts/check-staging-billing-state.ts</read_first>
  <action>For each remediated scenario, rerun the relevant staging replay command with `tsx scripts/replay-staging-asaas.ts --scenario <name>` and append the remediation result, identifiers, and updated snapshot to `03-STAGING-EVIDENCE.md`. Mark a gap `Resolved` in `03-STAGING-GAPS.md` only when both the local test suite and the live rerun pass. If there were no open gaps, add an explicit note that no remediation reruns were needed.</action>
  <verify>powershell -NoProfile -Command "$targets = 'Resolved','Open','No open gaps','owner_files','BILL-01','BILL-02','BILL-03'; $files = '.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md','.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"</verify>
  <acceptance_criteria>
    - every resolved gap references a rerun or states explicitly why no rerun was required
    - evidence and gap files both distinguish open versus resolved status
    - unresolved items remain visible instead of being silently dropped
  </acceptance_criteria>
  <done>Gap closure is backed by rerun evidence rather than optimistic status updates.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npm test -- src/lib/asaas/event-handlers.test.ts src/app/api/webhook/asaas/route.test.ts src/lib/asaas/credit-grants.test.ts src/lib/asaas/quota.test.ts src/app/api/checkout/route.test.ts src/lib/asaas/external-reference.test.ts src/lib/asaas/checkout.test.ts`
- [ ] `powershell -NoProfile -Command "$targets = 'Resolved','Open','No open gaps','owner_files','BILL-01','BILL-02','BILL-03'; $files = '.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md','.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Staging-found billing issues are either resolved with proof or left visible as explicit open gaps
  </success_criteria>

<output>
After completion, create `.planning/phases/03-billing-settlement-validation/03-03-SUMMARY.md`
</output>
