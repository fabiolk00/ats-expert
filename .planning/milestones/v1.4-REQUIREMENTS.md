# Requirements Archive: v1.4 Agent Core Modularization, Security Hardening, and Release Stability

**Archived:** 2026-04-15
**Outcome:** Shipped and later verification-backfilled with accepted runtime debt

This milestone delivered the planned product and engineering work, and Phase 32 of `v1.5` later backfilled the missing archived `VERIFICATION.md` artifacts for phases `28`, `29`, `30`, `31`, and `31.1`. The requirement outcomes below now reflect archived verification status rather than the original missing-proof state.

## Final Requirement Outcomes

| Requirement | Phase | Outcome |
|-------------|-------|---------|
| AGENT-01 | 28 | Verified passed via `28-VERIFICATION.md` with archived seam and route regression evidence |
| AGENT-02 | 29 | Verified passed via `29-VERIFICATION.md` with extracted recovery, streaming, and persistence evidence |
| AGENT-03 | 29 | Verified passed via `29-VERIFICATION.md` with direct seam tests plus route and SSE regressions |
| SEC-01 | 30 | Verified passed via `30-VERIFICATION.md` with canonical-host checkout proof |
| SEC-02 | 30 | Verified passed via `30-VERIFICATION.md` with fail-closed authenticated mutation trust checks |
| SEC-03 | 30 | Verified passed via `30-VERIFICATION.md` with checkout hostile-origin rejection and webhook boundary proof |
| REL-01 | 31 | Verified passed via `31-VERIFICATION.md` with long-vacancy browser proof |
| REL-02 | 31 | Verified passed via `31-VERIFICATION.md` with encoding-regression cleanup evidence |
| REL-03 | 31 | Verified passed via `31-VERIFICATION.md` with the release-critical CI browser gate |
| PERF-01 | 31.1 | Verified passed via `31.1-VERIFICATION.md` with node-first Vitest and retry-delay test seams |
| PERF-02 | 31.1 | Verified passed via `31.1-VERIFICATION.md` with hot-suite overhead reduction proof |
| PERF-03 | 31.1 | Verified passed via `31.1-VERIFICATION.md`; residual broad-suite runtime debt remains explicit |

## Requirement Notes

### Agent Runtime Modularization

- **AGENT-01**: Shipped via explicit services for message preparation, vacancy detection, and pre-loop setup, with route and seam regression tests.
- **AGENT-02**: Shipped via extracted recovery, streaming, and persistence modules that narrowed `agent-loop.ts`.
- **AGENT-03**: Shipped via targeted tests around the new seams plus existing route and loop regressions.

### Authenticated and Billing Trust Boundaries

- **SEC-01**: Shipped via canonical app-url derivation for checkout callbacks.
- **SEC-02**: Shipped via fail-closed request-trust enforcement on sensitive authenticated mutations.
- **SEC-03**: Shipped via regression proof around checkout/browser trust and the token-authenticated Asaas webhook boundary.

### Generation Stability and Release Hygiene

- **REL-01**: Shipped via repaired long-vacancy browser coverage and workspace-return proof.
- **REL-02**: Shipped via cleanup of mojibake expectations in active regression nets.
- **REL-03**: Shipped via an explicit release-critical E2E gate in CI.

### Test Runtime and CI Feedback

- **PERF-01**: Shipped via node-first non-E2E runtime defaults and test-safe retry-delay control.
- **PERF-02**: Shipped via targeted overhead reduction in the slowest already-profiled UI suites.
- **PERF-03**: Shipped via the canonical `test:profile:non-e2e` command promoted into CI.

## Accepted Gaps at Archive Time

- The broad non-E2E runtime still exceeded a strict local `2m` ceiling even after the structural fixes in `31.1`.
- Generated closeout artifacts required manual correction because the archive helper did not fully account for inserted phase `31.1`.

---

For the audit details, see [.planning/milestones/v1.4-MILESTONE-AUDIT.md](/c:/CurrIA/.planning/milestones/v1.4-MILESTONE-AUDIT.md).
