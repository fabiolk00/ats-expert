---
phase: 20-dead-code-tooling-and-safety-baseline
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - .eslintrc.json
  - README.md
autonomous: true
requirements:
  - HYG-01
user_setup: []
must_haves:
  truths:
    - "The repo exposes package scripts for dead-code and dependency-hygiene tooling."
    - "Unused-import autofix is available through lint tooling, but scoped safely for the current brownfield state."
    - "The setup does not prematurely force global unused-local compiler enforcement."
  artifacts:
    - path: "package.json"
      provides: "Runnable scripts for cleanup tooling"
      contains: "unused"
    - path: ".eslintrc.json"
      provides: "Unused import cleanup baseline"
      contains: "unused-imports"
---

<objective>
Add the dead-code tooling baseline and package scripts so contributors can run one documented inventory workflow before cleanup phases begin.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-20-dead-code-tooling-and-safety-baseline/20-CONTEXT.md
@package.json
@.eslintrc.json
@tsconfig.json
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add hygiene tooling scripts and safe lint autofix baseline</name>
  <files>package.json, .eslintrc.json, README.md</files>
  <read_first>package.json, .eslintrc.json, README.md, tsconfig.json</read_first>
  <action>Add the required dev tooling and package scripts for unused imports, dead exports, orphan files, and dependency cleanup. Wire in `eslint-plugin-unused-imports` in a scoped way that supports autofix without turning on global repo breakage. Update top-level contributor docs with the new commands.</action>
  <verify>pnpm lint && pnpm unused --help && pnpm depcheck --help && pnpm orphans --help</verify>
  <done>The repo has runnable hygiene scripts and a safe unused-import cleanup baseline.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm unused --help`
- [ ] `pnpm depcheck --help`
- [ ] `pnpm orphans --help`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-20-dead-code-tooling-and-safety-baseline/20-01-SUMMARY.md`
</output>
