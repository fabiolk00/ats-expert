---
phase: 20-dead-code-tooling-and-safety-baseline
plan: "03"
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - .github/workflows/ci.yml
  - docs/developer-rules/README.md
autonomous: true
requirements:
  - HYG-01
  - HYG-02
user_setup: []
must_haves:
  truths:
    - "The hygiene toolchain has focused verification coverage or documented execution paths in CI or contributor workflow."
    - "Contributors can discover the new dead-code workflow from the main developer-doc entrypoints."
    - "Phase 20 ends with a proven baseline rather than just installed packages."
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "Optional CI visibility or placeholder safety for hygiene commands"
      contains: "lint"
    - path: "docs/developer-rules/README.md"
      provides: "Discoverability for the hygiene workflow"
      contains: "dead-code"
---

<objective>
Close the phase by making the new hygiene baseline discoverable and verifiable through contributor-facing or CI-adjacent entrypoints.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-20-dead-code-tooling-and-safety-baseline/20-CONTEXT.md
@package.json
@.github/workflows/ci.yml
@docs/developer-rules/README.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add verification visibility and discoverability for the hygiene baseline</name>
  <files>.github/workflows/ci.yml, docs/developer-rules/README.md</files>
  <read_first>.github/workflows/ci.yml, docs/developer-rules/README.md, package.json</read_first>
  <action>Expose the new hygiene baseline through the main developer-doc index and decide whether CI should run any safe visibility checks now or simply reserve them for later phases. Keep this phase conservative: prove the baseline is runnable and discoverable without turning cleanup into a noisy blocking gate too early.</action>
  <verify>pnpm lint && rg -n "dead-code|unused|depcheck|orphans" docs/developer-rules/README.md .github/workflows/ci.yml package.json</verify>
  <done>The baseline is discoverable, and the repo has a clear verification path for the new hygiene commands.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `rg -n "dead-code|unused|depcheck|orphans" docs/developer-rules/README.md .github/workflows/ci.yml package.json`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-20-dead-code-tooling-and-safety-baseline/20-03-SUMMARY.md`
</output>
