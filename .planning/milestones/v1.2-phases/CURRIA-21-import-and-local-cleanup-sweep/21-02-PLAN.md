---
phase: 21-import-and-local-cleanup-sweep
plan: "02"
type: execute
wave: 1
depends_on:
  - 21-01
files_modified:
  - src/app/api/e2e/**/*
  - src/app/api/cron/**/*
  - src/lib/auth/**/*
  - src/lib/openai/**/*
  - src/lib/db/supabase-admin.ts
  - src/lib/asaas/client.ts
autonomous: true
requirements:
  - CLEAN-02
user_setup: []
must_haves:
  truths:
    - "Low-risk unused locals and parameters are removed or intentionally marked with stable underscore conventions."
    - "Framework or interface-required parameters are preserved when needed."
    - "Cleanup avoids false positives from dynamic or framework-driven seams."
  artifacts:
    - path: "src/lib/auth"
      provides: "Representative local cleanup slice"
      contains: "_"
---

<objective>
Reduce low-risk unused locals and parameters in the same approved slices, using underscore ignores only where signatures or framework seams require them.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-21-import-and-local-cleanup-sweep/21-CONTEXT.md
@.eslintrc.json
@docs/operations/dead-code-cleanup-workflow.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Remove or mark low-risk unused locals and parameters</name>
  <files>src/app/api/e2e/**/*, src/app/api/cron/**/*, src/lib/auth/**/*, src/lib/openai/**/*, src/lib/db/supabase-admin.ts, src/lib/asaas/client.ts</files>
  <read_first>.eslintrc.json, docs/operations/dead-code-cleanup-workflow.md</read_first>
  <action>Clean obviously dead locals and parameters in the approved scopes. When a callback or framework contract still requires the argument position, rename it with a leading underscore rather than deleting the signature slot.</action>
  <verify>pnpm lint && pnpm typecheck</verify>
  <done>Low-risk locals and parameters are cleaned or explicitly ignored with stable conventions.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-21-import-and-local-cleanup-sweep/21-02-SUMMARY.md`
</output>
