---
phase: 21-import-and-local-cleanup-sweep
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/e2e/**/*
  - src/app/api/cron/**/*
  - src/lib/auth/**/*
  - src/lib/openai/**/*
  - src/lib/db/supabase-admin.ts
  - src/lib/asaas/client.ts
autonomous: true
requirements:
  - CLEAN-01
user_setup: []
must_haves:
  truths:
    - "Unused imports are removed through the committed lint autofix path in agreed low-risk scopes."
    - "Cleanup stays inside the already approved brownfield lint slices."
    - "No runtime behavior changes are introduced while removing imports."
  artifacts:
    - path: "src/lib/openai"
      provides: "Representative cleaned slice"
      contains: "import"
---

<objective>
Use the staged lint autofix baseline to remove unused imports in the approved cleanup scopes, keeping the diff narrow and behaviorally neutral.
</objective>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/phases/CURRIA-21-import-and-local-cleanup-sweep/21-CONTEXT.md
@package.json
@.eslintrc.json
@docs/operations/dead-code-cleanup-workflow.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Run and validate staged unused-import cleanup</name>
  <files>src/app/api/e2e/**/*, src/app/api/cron/**/*, src/lib/auth/**/*, src/lib/openai/**/*, src/lib/db/supabase-admin.ts, src/lib/asaas/client.ts</files>
  <read_first>.eslintrc.json, package.json, docs/operations/dead-code-cleanup-workflow.md</read_first>
  <action>Run the committed lint autofix path in the approved slices, review the resulting import removals, and keep only changes that are clearly low-risk and behavior-neutral.</action>
  <verify>pnpm lint:types:fix && pnpm lint && pnpm typecheck</verify>
  <done>Unused imports are removed in the agreed slices and the baseline remains green.</done>
</task>
</tasks>

<verification>
Before declaring plan complete:
- [ ] `pnpm lint:types:fix`
- [ ] `pnpm lint`
- [ ] `pnpm typecheck`
</verification>

<output>
After completion, create `.planning/phases/CURRIA-21-import-and-local-cleanup-sweep/21-01-SUMMARY.md`
</output>
