---
phase: 32-phase-verification-backfill-and-audit-contract
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/milestones/v1.4-MILESTONE-AUDIT.md
  - .planning/milestones/v1.4-ROADMAP.md
  - .planning/milestones/v1.4-REQUIREMENTS.md
  - .planning/milestones/v1.4-phases/
autonomous: true
requirements:
  - VER-01
user_setup: []
must_haves:
  truths:
    - "Phase 32 must establish one canonical location for archived phase verification rather than reactivating the `v1.4` milestone."
    - "Verification backfill must be conservative and traceable to committed artifacts such as archived summaries, tests, and audit evidence."
    - "The repo should have an explicit verification schema before any phase-level pass or fail claims are written."
  artifacts:
    - path: ".planning/milestones/v1.4-phases"
      provides: "Canonical archive location for reconstructed v1.4 phase evidence"
      contains: "CURRIA-28-agent-input-and-setup-service-extraction"
    - path: ".planning/milestones/v1.4-MILESTONE-AUDIT.md"
      provides: "Audit baseline that the backfill must repair"
      contains: "VERIFICATION.md artifacts"
  key_links:
    - from: ".planning/milestones/v1.4-ROADMAP.md"
      to: ".planning/milestones/v1.4-phases"
      via: "phase identity and expected plan layout"
      pattern: "Phase 28"
    - from: ".planning/milestones/v1.4-REQUIREMENTS.md"
      to: ".planning/milestones/v1.4-phases"
      via: "requirement-to-phase mapping for conservative verification writing"
      pattern: "AGENT-01"
---

<objective>
Define the canonical archive and verification contract for the shipped `v1.4` phases before any requirement evidence is backfilled.

Purpose: Phase 32 starts by preventing ad hoc proof files and by making the archive target, phase set, and verification shape explicit.
Output: A stable `v1.4` phase-archive location and a documented verification schema that later plans can apply consistently.
</objective>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.4-ROADMAP.md
@.planning/milestones/v1.4-REQUIREMENTS.md
@.planning/milestones/v1.4-MILESTONE-AUDIT.md
@AGENTS.md
</context>

<threat_model>
<trust_boundaries>
- Archived milestone artifacts -> newly written verification files
- Git-history evidence -> current archive claims
- Verification schema -> future milestone audit interpretation
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-32-01</threat_id>
  <category>Tampering</category>
  <component>Verification backfill contract</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Define one canonical archive path and one explicit verification schema before writing pass or fail claims so evidence cannot drift across ad hoc files.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-32-02</threat_id>
  <category>Repudiation</category>
  <component>Requirement evidence</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Require every future verification claim to cite archived summaries, tests, or audit sources tied to the mapped requirement IDs.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-32-03</threat_id>
  <category>Information Disclosure</category>
  <component>Backfilled proofs</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep verification content limited to shipped requirement evidence and residual gaps; do not copy sensitive runtime output, tokens, or raw environment details into archive documents.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Create the canonical v1.4 phase-archive target</name>
  <files>.planning/milestones/v1.4-phases/</files>
  <read_first>.planning/milestones/v1.4-ROADMAP.md, .planning/MILESTONES.md</read_first>
  <action>Create `.planning/milestones/v1.4-phases/` and seed one directory per shipped `v1.4` phase using the canonical phase names from the archived roadmap. The archive target must clearly separate the shipped phase history from the active `v1.5` planning directory and give later tasks a stable place to reconstruct evidence.</action>
  <verify>powershell -NoProfile -Command "$dirs = Get-ChildItem .planning/milestones/v1.4-phases -Directory; if ($dirs.Count -ne 5) { exit 1 }"</verify>
  <acceptance_criteria>
    - `.planning/milestones/v1.4-phases/` exists
    - the archive contains one directory for each shipped `v1.4` phase, including decimal phase `31.1`
    - the active milestone state remains pointed at `v1.5`, not reactivated to `v1.4`
  </acceptance_criteria>
  <done>The repo has one canonical archive location for reconstructed `v1.4` phase evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Document the verification schema and evidence rules for the backfill</name>
  <files>.planning/milestones/v1.4-MILESTONE-AUDIT.md, .planning/milestones/v1.4-phases/</files>
  <read_first>.planning/milestones/v1.4-MILESTONE-AUDIT.md, .planning/milestones/v1.4-REQUIREMENTS.md, .planning/REQUIREMENTS.md</read_first>
  <action>Document the required `VERIFICATION.md` structure that each backfilled `v1.4` phase must follow: requirement table, status, evidence, residual gaps, and explicit non-claims. The schema should be anchored in the current audit findings and should state that only committed summaries, tests, archived requirements, and audit evidence may be used to justify a requirement status.</action>
  <verify>node \"$HOME/.codex/get-shit-done/bin/gsd-tools.cjs\" state validate</verify>
  <acceptance_criteria>
    - the backfill rules describe required sections for each `VERIFICATION.md`
    - the rules require requirement-level evidence and residual-gap language
    - the rules make conservative non-claims explicit for any missing or ambiguous proof
  </acceptance_criteria>
  <done>The phase has one explicit verification-writing contract before any backfilled pass/fail claims are added.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" state validate`
- [ ] `v1.4-phases/` exists with five phase directories
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo has a stable archive target and explicit verification schema for the shipped `v1.4` phases
  </success_criteria>

<output>
After completion, create `.planning/phases/32-phase-verification-backfill-and-audit-contract/32-01-SUMMARY.md`
</output>
