---
phase: 39-move-ats-targeting-and-artifact-work-into-async-processors
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/jobs/runtime.ts
  - src/lib/jobs/repository.ts
  - src/lib/jobs/processors/ats-enhancement.ts
  - src/lib/jobs/processors/job-targeting.ts
  - src/lib/jobs/processors/artifact-generation.ts
  - src/lib/jobs/runtime.test.ts
  - src/lib/agent/async-dispatch.ts
  - src/lib/agent/ats-enhancement-pipeline.ts
  - src/lib/agent/job-targeting-pipeline.ts
  - src/lib/agent/tools/pipeline.test.ts
  - src/lib/resume-generation/generate-billable-resume.ts
  - src/lib/resume-generation/generate-billable-resume.test.ts
  - src/app/api/session/[id]/generate/route.ts
  - src/app/api/session/[id]/generate/route.test.ts
  - src/app/api/agent/route.sse.test.ts
  - .planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/**
autonomous: true
requirements:
  - JOB-02
  - ART-01
  - STATE-01
user_setup: []
must_haves:
  truths:
    - "Phase 39 adds one same-app durable processor runtime on top of the Phase 37 jobs repository; it must not introduce a second queue, worker service, or persistence model."
    - "ATS enhancement and job targeting processors must reuse `runAtsEnhancementPipeline(...)` and `runJobTargetingPipeline(...)` instead of re-implementing rewrite logic."
    - "Artifact generation must leave the request path, use effective source-of-truth snapshot selection, and complete durable jobs with `resume_generation` result refs rather than signed URLs."
    - "Async failures must preserve the previous valid `optimizedCvState` and related last-good optimized metadata; failing the run must not clobber stable preview state."
    - "`/api/agent` and `/api/session/[id]/generate` stay as thin handoff surfaces with existing auth, trust, and brownfield guardrails preserved."
    - "Phase 39 is execution plumbing only; rich status UI, polling, and setup-flow async UX changes remain deferred to Phase 40 unless explicitly required by a blocker."
  artifacts:
    - path: "src/lib/jobs/runtime.ts"
      provides: "Claim-safe same-app kickoff and processor dispatch for durable ATS, targeting, and artifact jobs"
      contains: "export async function startDurableJobProcessing"
    - path: "src/lib/jobs/processors/ats-enhancement.ts"
      provides: "ATS worker adapter that reuses the existing pipeline and durable snapshot result refs"
      contains: "export async function processAtsEnhancementJob"
    - path: "src/lib/jobs/processors/job-targeting.ts"
      provides: "Job-targeting worker adapter that reuses the existing pipeline and durable snapshot result refs"
      contains: "export async function processJobTargetingJob"
    - path: "src/lib/jobs/processors/artifact-generation.ts"
      provides: "Artifact worker adapter that reuses `generateBillableResume(...)` and completes with `resume_generation` refs"
      contains: "export async function processArtifactGenerationJob"
    - path: "src/app/api/session/[id]/generate/route.ts"
      provides: "Thin artifact route that validates the request and durably hands generation to the processor runtime"
      contains: "export async function POST"
    - path: "src/lib/jobs/runtime.test.ts"
      provides: "Focused proof for claim fencing, kickoff, processor dispatch, and terminal writes"
      contains: "describe('startDurableJobProcessing'"
  key_links:
    - from: "src/lib/jobs/runtime.ts"
      to: "src/lib/jobs/repository.ts"
      via: "runtime must claim, update, complete, or fail durable jobs using the shared repository only"
      pattern: "claimJob"
    - from: "src/lib/jobs/processors/artifact-generation.ts"
      to: "src/lib/resume-generation/generate-billable-resume.ts"
      via: "artifact workers must reuse the existing billable generation path instead of direct file generation"
      pattern: "generateBillableResume"
    - from: "src/lib/jobs/processors/ats-enhancement.ts"
      to: "src/lib/agent/ats-enhancement-pipeline.ts"
      via: "ATS workers must preserve current rewrite and validation semantics"
      pattern: "runAtsEnhancementPipeline"
    - from: "src/lib/jobs/processors/job-targeting.ts"
      to: "src/lib/agent/job-targeting-pipeline.ts"
      via: "targeting workers must preserve current rewrite and validation semantics"
      pattern: "runJobTargetingPipeline"
    - from: "src/app/api/session/[id]/generate/route.ts"
      to: "src/lib/jobs/source-of-truth.ts"
      via: "artifact dispatch must preserve effective source selection and durable lineage"
      pattern: "resolveEffectiveResumeSource"
---

<objective>
Execute ATS enhancement, job targeting, and artifact generation through durable async processors while preserving the current business semantics, validation rules, billing safety, and source-of-truth behavior.

Purpose: Phase 39 is where the frozen async contracts start doing real work. Heavy resume processing must leave the request path, but the brownfield product behavior and safety guarantees must remain intact.
Output: One same-app durable job runtime, three typed processors, state-safe pipeline failure behavior, a durably dispatched artifact generation route, and focused regression proof.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-RESEARCH.md
@.planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@src/types/jobs.ts
@src/lib/jobs/repository.ts
@src/lib/jobs/source-of-truth.ts
@src/lib/agent/async-dispatch.ts
@src/lib/agent/ats-enhancement-pipeline.ts
@src/lib/agent/job-targeting-pipeline.ts
@src/lib/resume-generation/generate-billable-resume.ts
@src/app/api/session/[id]/generate/route.ts
@src/lib/profile/pdf-import-jobs.ts
@src/lib/linkedin/import-jobs.ts
</context>

<threat_model>
<trust_boundaries>
- Public artifact-generation route -> authenticated app-user-scoped durable job dispatch
- Async dispatch helper -> same-app processor kickoff and claim ownership
- Claimed worker -> session, target, generation, and job persistence surfaces
- Processor outputs -> durable result refs and preserved optimized-state snapshots
</trust_boundaries>
<stride_register>
<threat>
  <threat_id>T-39-01</threat_id>
  <category>Elevation of Privilege</category>
  <component>Job, session, and target loading</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep every durable job, session, and resume-target read scoped by internal app user ID so no worker can cross tenant boundaries.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-39-02</threat_id>
  <category>Tampering</category>
  <component>Claim ownership and terminal writes</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Use repository claim fencing and owner-bound terminal updates so stale or duplicated workers cannot overwrite the current job owner.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-39-03</threat_id>
  <category>Repudiation</category>
  <component>Artifact lineage and source selection</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Persist durable input and result refs through source-of-truth helpers so the app can prove which snapshot or version produced a generated file.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-39-04</threat_id>
  <category>Denial of Service</category>
  <component>Duplicate dispatch and repeated generation attempts</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve idempotency-key reuse and generation-record reuse so repeated requests do not create duplicate heavy work or double-bill credits.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-39-05</threat_id>
  <category>Tampering</category>
  <component>Failure-state persistence</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Preserve the last good optimized snapshot on failed async runs so invalid or partial output cannot replace a previously valid state.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add claim-safe durable job runtime and wire async kickoff</name>
  <files>src/lib/jobs/runtime.ts, src/lib/jobs/repository.ts, src/lib/agent/async-dispatch.ts, src/lib/jobs/runtime.test.ts</files>
  <read_first>src/types/jobs.ts, src/lib/jobs/repository.ts, src/lib/agent/async-dispatch.ts, src/lib/profile/pdf-import-jobs.ts, src/lib/linkedin/import-jobs.ts, .planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-RESEARCH.md</read_first>
  <action>Create `src/lib/jobs/runtime.ts` as the one same-app kickoff layer for durable ATS, targeting, and artifact jobs. It must claim queued or stale-running jobs through the shared repository, schedule actual work with `queueMicrotask(...)`, dispatch to a typed processor by `JobType`, and persist completion or failure through the repository's fenced terminal writes. Update `src/lib/agent/async-dispatch.ts` so newly created or claimable reused jobs immediately nudge this runtime instead of stopping after `createJob(...)`. If execution clearly needs a running-stage update helper, add the smallest ownership-fenced helper in `src/lib/jobs/repository.ts`; do not add external queue infrastructure, new lifecycle vocabulary, or a second persistence path. Add `src/lib/jobs/runtime.test.ts` to prove kickoff, stale reclaim, one-owner claim behavior, and terminal-write fencing.</action>
  <verify>npm run typecheck && npx vitest run src/lib/jobs/runtime.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/jobs/runtime.ts` contains `export async function startDurableJobProcessing(`
    - `src/lib/jobs/runtime.ts` contains `queueMicrotask(`
    - `src/lib/jobs/runtime.ts` contains `claimJob(`
    - `src/lib/jobs/runtime.ts` contains `completeJob(`
    - `src/lib/jobs/runtime.ts` contains `failJob(`
    - `src/lib/agent/async-dispatch.ts` contains `startDurableJobProcessing(`
    - `src/lib/jobs/runtime.test.ts` contains `describe('startDurableJobProcessing'`
  </acceptance_criteria>
  <done>The durable-job foundation no longer ends at job creation; it can claim and run work safely inside the app process.</done>
</task>

<task type="auto">
  <name>Task 2: Add ATS and targeting processors and preserve last-good optimized state</name>
  <files>src/lib/jobs/processors/ats-enhancement.ts, src/lib/jobs/processors/job-targeting.ts, src/lib/agent/ats-enhancement-pipeline.ts, src/lib/agent/job-targeting-pipeline.ts, src/lib/agent/tools/pipeline.test.ts</files>
  <read_first>src/lib/agent/ats-enhancement-pipeline.ts, src/lib/agent/job-targeting-pipeline.ts, src/lib/jobs/source-of-truth.ts, src/lib/agent/tools/pipeline.test.ts, .planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-RESEARCH.md</read_first>
  <action>Create ATS and job-targeting processor adapters that load the session for the claimed job, call the existing pipeline module, and complete or fail the durable job with the correct snapshot result refs. Keep current business logic, validation semantics, logging, and version creation behavior intact. Change only the destructive failure behavior that conflicts with `STATE-01`: failed validation or `persist_version` paths must preserve the previous valid optimized snapshot and related last-good optimized metadata instead of clearing them. Update `src/lib/agent/tools/pipeline.test.ts` to prove both processors preserve last-good optimized state while still reporting failed runs correctly.</action>
  <verify>npm run typecheck && npx vitest run src/lib/agent/tools/pipeline.test.ts src/lib/jobs/runtime.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/jobs/processors/ats-enhancement.ts` contains `export async function processAtsEnhancementJob(`
    - `src/lib/jobs/processors/ats-enhancement.ts` contains `runAtsEnhancementPipeline(`
    - `src/lib/jobs/processors/job-targeting.ts` contains `export async function processJobTargetingJob(`
    - `src/lib/jobs/processors/job-targeting.ts` contains `runJobTargetingPipeline(`
    - `src/lib/jobs/processors/ats-enhancement.ts` contains `buildSnapshotResultRef(`
    - `src/lib/jobs/processors/job-targeting.ts` contains `buildSnapshotResultRef(`
    - `src/lib/agent/tools/pipeline.test.ts` contains `preserves the previous valid optimizedCvState`
  </acceptance_criteria>
  <done>ATS and target-job durable workers reuse the existing rewrite services and no longer destroy stable optimized state on failed async runs.</done>
</task>

<task type="auto">
  <name>Task 3: Move artifact generation behind durable handoff with source-of-truth lineage</name>
  <files>src/lib/jobs/processors/artifact-generation.ts, src/lib/resume-generation/generate-billable-resume.ts, src/app/api/session/[id]/generate/route.ts, src/lib/resume-generation/generate-billable-resume.test.ts, src/app/api/session/[id]/generate/route.test.ts</files>
  <read_first>src/app/api/session/[id]/generate/route.ts, src/lib/resume-generation/generate-billable-resume.ts, src/lib/jobs/source-of-truth.ts, src/lib/agent/tools/index.ts, src/lib/db/resume-targets.ts, .planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-RESEARCH.md</read_first>
  <action>Create an artifact-generation processor that resolves the correct source snapshot from the durable job input, reuses `generateBillableResume(...)`, and completes the durable job with `buildResumeGenerationResultRef(...)` on success or the correct durable error ref on failure. Refactor `/api/session/[id]/generate` so auth, trust, career-fit gating, and Zod validation stay intact but the route no longer calls inline `dispatchTool('generate_file', ...)`. The route should create or reuse a durable `artifact_generation` job, start runtime kickoff, preserve the existing acceptance semantics for in-progress work, and only expose immediate result metadata when it is already durably known instead of fabricating a `resumeGenerationId` for a fresh queued job. Update the generation-service and route tests to prove source selection, lineage, and route acceptance behavior.</action>
  <verify>npm run typecheck && npx vitest run src/lib/resume-generation/generate-billable-resume.test.ts src/lib/jobs/source-of-truth.test.ts "src/app/api/session/[id]/generate/route.test.ts"</verify>
  <acceptance_criteria>
    - `src/lib/jobs/processors/artifact-generation.ts` contains `export async function processArtifactGenerationJob(`
    - `src/lib/jobs/processors/artifact-generation.ts` contains `generateBillableResume(`
    - `src/lib/jobs/processors/artifact-generation.ts` contains `buildResumeGenerationResultRef(`
    - `src/app/api/session/[id]/generate/route.ts` does not contain `dispatchTool(`
    - `src/app/api/session/[id]/generate/route.ts` contains `createJob(`
    - `src/app/api/session/[id]/generate/route.ts` contains `startDurableJobProcessing(`
    - `src/app/api/session/[id]/generate/route.test.ts` contains `inProgress`
    - `src/lib/resume-generation/generate-billable-resume.test.ts` contains `sourceCvSnapshot`
  </acceptance_criteria>
  <done>Artifact generation leaves the request path, keeps lineage through durable refs, and preserves the route's safety gates.</done>
</task>

<task type="auto">
  <name>Task 4: Lock the async execution path with focused regressions</name>
  <files>src/lib/jobs/runtime.test.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/resume-generation/generate-billable-resume.test.ts, src/app/api/session/[id]/generate/route.test.ts, src/app/api/agent/route.sse.test.ts</files>
  <read_first>src/lib/jobs/runtime.test.ts, src/lib/agent/tools/pipeline.test.ts, src/lib/resume-generation/generate-billable-resume.test.ts, src/app/api/session/[id]/generate/route.test.ts, src/app/api/agent/route.sse.test.ts, .planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-VALIDATION.md</read_first>
  <action>Finish the focused regression suite so the new runtime, worker adapters, and route handoff stay provably safe together. Cover one-owner claim behavior, stale reclaim, job-type dispatch, preserved optimized state on failed async ATS and targeting runs, artifact lineage through `resume_generations`, and the fact that heavy `/api/agent` acknowledgements now trigger runtime kickoff instead of ending at durable job creation. Keep the suite mock-driven and fast enough to run after every task commit.</action>
  <verify>npm run typecheck && npx vitest run src/lib/jobs/runtime.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/resume-generation/generate-billable-resume.test.ts src/lib/jobs/source-of-truth.test.ts "src/app/api/session/[id]/generate/route.test.ts" src/app/api/agent/route.sse.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/jobs/runtime.test.ts` contains `stale`
    - `src/lib/jobs/runtime.test.ts` contains `artifact_generation`
    - `src/lib/agent/tools/pipeline.test.ts` contains `preserves the previous valid optimizedCvState`
    - `src/app/api/session/[id]/generate/route.test.ts` contains `202`
    - `src/app/api/agent/route.sse.test.ts` contains `startDurableJobProcessing`
    - `npm run typecheck && npx vitest run src/lib/jobs/runtime.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/resume-generation/generate-billable-resume.test.ts src/lib/jobs/source-of-truth.test.ts "src/app/api/session/[id]/generate/route.test.ts" src/app/api/agent/route.sse.test.ts` exits 0
  </acceptance_criteria>
  <done>Phase 39 closes with focused proof that durable workers run safely, preserve state, and keep artifact lineage correct without regressing request-boundary behavior.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/lib/jobs/runtime.test.ts src/lib/agent/tools/pipeline.test.ts src/lib/resume-generation/generate-billable-resume.test.ts src/lib/jobs/source-of-truth.test.ts "src/app/api/session/[id]/generate/route.test.ts" src/app/api/agent/route.sse.test.ts`
- [ ] confirm no inline `runAtsEnhancementPipeline(...)`, `runJobTargetingPipeline(...)`, or `dispatchTool('generate_file', ...)` remains in request-bound route paths for the Phase 39 surfaces
- [ ] confirm failed ATS and targeting runs preserve the previous valid optimized snapshot in focused tests
- [ ] confirm artifact jobs complete with durable `resume_generation` refs instead of signed URLs
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 39 leaves the repo with one same-app durable processor runtime, three typed heavy-work processors, non-destructive async failure semantics for optimized state, a durably dispatched artifact route, and focused proof that the new async execution path is safe
</success_criteria>

<output>
After completion, create `.planning/phases/39-move-ats-targeting-and-artifact-work-into-async-processors/39-01-SUMMARY.md`
</output>
