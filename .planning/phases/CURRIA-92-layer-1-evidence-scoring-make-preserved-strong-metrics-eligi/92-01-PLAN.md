---
phase: 92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resume/optimized-preview-highlights.ts
  - src/lib/resume/optimized-preview-highlights.test.ts
  - .planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-VALIDATION.md
autonomous: true
requirements:
  - EXP-HILITE-EVIDENCE-01
  - EXP-HILITE-EVIDENCE-ELIGIBILITY-01
  - EXP-HILITE-EVIDENCE-TEST-01
user_setup: []
must_haves:
  truths:
    - "Preserved quantified bullets remain eligible for highlight even when original-vs-optimized rewrite delta is small."
    - "Layer 1 returns numeric `evidenceScore` and `improvementScore` separately, and eligibility uses independent evidence/improvement thresholds instead of any combined ranking score."
    - "Layer 3 same-entry surfacing stays semantically unchanged: once eligibility is restored, Tier 1 metric evidence still beats same-entry scope/scale and Tier 2 under the existing cap."
  artifacts:
    - path: "src/lib/resume/optimized-preview-highlights.ts"
      provides: "Explicit Layer 1 evidence scoring, numeric thresholds, and minimal Layer 2 plumbing of `evidenceScore`"
      contains: "evidenceScore"
    - path: "src/lib/resume/optimized-preview-highlights.test.ts"
      provides: "Direct numeric Layer 1 tests plus real same-entry surfacing regression proof"
      contains: "preserved strong metrics"
    - path: ".planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-VALIDATION.md"
      provides: "Phase note documenting thresholds, preserved-metric recovery, stack-only guardrail, and unchanged non-goals"
      contains: "no combined rankingScore"
  key_links:
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "Layer 1 helper output flows into bullet result assembly"
      pattern: "(evaluateExperienceBulletImprovement|evidenceScore|buildBulletHighlight)"
    - from: "src/lib/resume/optimized-preview-highlights.ts"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "Bullet results remain consumed by existing Layer 3 surfacing"
      pattern: "(ExperienceBulletHighlightResult|selectVisibleExperienceHighlightsForEntry)"
    - from: "src/lib/resume/optimized-preview-highlights.test.ts"
      to: "src/lib/resume/optimized-preview-highlights.ts"
      via: "Direct score assertions plus real preview pipeline regression"
      pattern: "(evaluateExperienceBulletImprovement|buildOptimizedPreviewHighlights)"
---

<objective>
Implement Phase 92 as a narrow Layer 1 scoring change that restores highlight eligibility for preserved strong evidence without reopening Layer 2 or Layer 3 ownership.

Purpose: stop strong preserved metric bullets from losing visibility just because the rewrite diff is small.
Output: explicit numeric `evidenceScore` thresholds, separate Layer 1 signals, minimal Layer 2 plumbing, focused tests, and a short validation note.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-CONTEXT.md
@.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-RESEARCH.md
@.planning/phases/CURRIA-87-formalize-experience-entry-highlight-surfacing-policy-as-an-/87-01-SUMMARY.md
@.planning/phases/CURRIA-88-harden-experience-entry-highlight-surfacing-with-explicit-po/88-01-SUMMARY.md
@.planning/phases/CURRIA-89-validate-ssr-safety-of-debug-flag-and-constant-coupling-in-e/89-01-SUMMARY.md
@src/lib/resume/optimized-preview-highlights.ts
@src/lib/resume/optimized-preview-highlights.test.ts

<scope_rules>
- Keep the phase inside `optimized-preview-highlights.ts` plus focused tests and validation notes.
- Layer 1 may add `evidenceScore`, explicit numeric thresholds, and independent eligibility paths.
- Layer 2 may only receive the new signal through minimal plumbing; do not create a combined `rankingScore`.
- Layer 3 ordering, cap handling, and debug semantics must not change semantically.
- Technology mentions alone must not cross the evidence threshold without measurable or scope/scale anchors.
</scope_rules>

<interfaces>
From `src/lib/resume/optimized-preview-highlights.ts`:
```ts
type ExperienceBulletImprovement = {
  eligible: boolean
  score: number
}

export type ExperienceBulletHighlightResult = {
  bullet: string
  bulletIndex: number
  line: HighlightedLine
  eligible: boolean
  hasVisibleHighlightCandidate: boolean
  renderable: boolean
  improvementScore: number
  winnerScore: number
  highlightTier?: "strong" | "secondary"
  highlightCategory?: ExperienceHighlightCategory
}

export function evaluateExperienceBulletImprovement(
  original: string,
  optimized: string,
): ExperienceBulletImprovement
```

Current Layer 3 ordering that must stay semantically unchanged:
```ts
.sort((left, right) =>
  getExperienceHighlightTierPriority(left) - getExperienceHighlightTierPriority(right)
  || getExperienceHighlightCategoryPriority(left) - getExperienceHighlightCategoryPriority(right)
  || right.winnerScore - left.winnerScore
  || right.improvementScore - left.improvementScore
  || left.bulletIndex - right.bulletIndex,
)
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit Layer 1 evidence score and independent eligibility thresholds</name>
  <files>src/lib/resume/optimized-preview-highlights.ts, src/lib/resume/optimized-preview-highlights.test.ts</files>
  <behavior>
    - Test 1: `evaluateExperienceBulletImprovement(...)` returns a numeric `evidenceScore` high enough to keep a preserved quantified bullet eligible even when `improvementScore` stays below the improvement threshold.
    - Test 2: a stack-only bullet with minimal diff and no measurable or scope/scale anchor stays below the evidence threshold and does not regain eligibility through the evidence path.
    - Test 3: a genuinely improved quantified bullet still exposes both numeric signals and remains eligible through the independent threshold logic.
  </behavior>
  <action>Replace the opaque Layer 1 return shape with explicit `improvementScore` and `evidenceScore`, plus named numeric threshold constants for both paths. Implement a small optimized-text-only evidence helper adjacent to `evaluateExperienceBulletImprovement(...)` that scores preserved measurable evidence from the optimized bullet itself rather than from original-vs-optimized diff. Keep technology-only evidence below threshold unless it is anchored by measurable or scope/scale signals, preserve the existing empty/minor-formatting guards, and do not introduce any combined `rankingScore` or same-entry surfacing decision inside Layer 1.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"</automated>
  </verify>
  <done>`evaluateExperienceBulletImprovement(...)` returns explicit numeric `evidenceScore` and `improvementScore`, preserved strong metrics can pass eligibility on the evidence path, and weak stack-only bullets do not become falsely eligible through technology mentions alone.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Plumb evidenceScore forward without changing Layer 3 policy and prove the same-entry recovery</name>
  <files>src/lib/resume/optimized-preview-highlights.ts, src/lib/resume/optimized-preview-highlights.test.ts</files>
  <behavior>
    - Test 1: `ExperienceBulletHighlightResult` carries `evidenceScore` and `improvementScore` as separate numeric Layer 1 signals.
    - Test 2: in a real same-entry pipeline case, preserved metric bullets with low diff still reach Layer 3 as Tier 1 and beat a same-entry `scope_scale` bullet under cap pressure.
    - Test 3: existing happy-path metric and scope/scale selections do not regress, and Layer 3 does not gain `evidenceScore` as a new semantic sort key.
  </behavior>
  <action>Add `evidenceScore` to `ExperienceBulletHighlightResult` and any minimal local trace/plumbing needed so downstream layers can inspect it without re-owning Layer 1 logic. Update `buildBulletHighlight(...)` and the real preview pipeline so eligibility is based on `evidenceScore >= EVIDENCE_THRESHOLD || improvementScore >= IMPROVEMENT_THRESHOLD` before existing renderability checks. Keep `collectRankedExperienceHighlightCandidates(...)` and `selectVisibleExperienceHighlightsForEntry(...)` semantically unchanged: Layer 2 still owns candidate comparison, Layer 3 still owns visible same-entry surfacing, and neither layer should receive a new combined ranking signal.</action>
  <verify>
    <automated>npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The new Layer 1 signal reaches downstream result objects, preserved metric evidence becomes visible again in the real preview pipeline, and same-entry editorial behavior stays anchored to the existing Layer 3 policy.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Record the numeric evidence contract, recovered case, and explicit non-goals</name>
  <files>.planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-VALIDATION.md</files>
  <behavior>
    - Validation note records the numeric evidence and improvement thresholds used by Layer 1.
    - Validation note includes one preserved-metric recovery example and one stack-only non-goal example.
    - Validation note states that Layer 3 policy is unchanged and no combined `rankingScore` was introduced.
  </behavior>
  <action>Write `92-VALIDATION.md` as a short seam note that explains the new Layer 1 evidence score contract, the independent threshold rule, one concrete preserved-metric recovery case, one stack-only guardrail case, and the unchanged non-goals for Layer 2 and Layer 3. Keep the note narrowly scoped to evidence eligibility so it does not imply broader ranking or UI changes.</action>
  <verify>
    <automated>rg -n "(evidenceScore|EVIDENCE_THRESHOLD|IMPROVEMENT_THRESHOLD|no combined rankingScore|Layer 3 policy unchanged)" ".planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-VALIDATION.md"</automated>
  </verify>
  <done>The phase directory contains a concise validation artifact documenting the numeric evidence contract, preserved-metric recovery, stack-only guardrail, and unchanged Layer 2/Layer 3 ownership.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Layer 1 evidence helper -> bullet result assembly | The new signal must stay a Layer 1 eligibility input, not a hidden cross-layer ranking rewrite |
| bullet result assembly -> Layer 3 surfacing selector | Restored eligibility must not silently change same-entry editorial ordering or cap behavior |
| tests and validation note -> future highlight tuning | Numeric thresholds and non-goals must stay explicit so later phases do not collapse back into an implicit combined score |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-92-01 | T | `evaluateExperienceBulletImprovement(...)` | mitigate | Keep `evidenceScore` optimized-text-only, return it separately from `improvementScore`, and forbid any combined `rankingScore` in Layer 1. |
| T-92-02 | D | evidence eligibility | mitigate | Require measurable or scope/scale anchors for the evidence path so technology-only bullets do not become false positives. |
| T-92-03 | T | `selectVisibleExperienceHighlightsForEntry(...)` | mitigate | Preserve Layer 3 sort semantics and prove with focused same-entry regressions that restored eligibility does not rewrite visible-slot policy. |
</threat_model>

<verification>
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts"`
- `npm run typecheck`
- `rg -n "(evidenceScore|EVIDENCE_THRESHOLD|IMPROVEMENT_THRESHOLD|no combined rankingScore|Layer 3 policy unchanged)" ".planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-VALIDATION.md"`
</verification>

<success_criteria>
- Preserved strong metric bullets remain eligible even when rewrite diff is small because `evidenceScore` independently crosses the explicit threshold.
- `evaluateExperienceBulletImprovement(...)` exposes numeric `evidenceScore` and `improvementScore`, and downstream bullet results carry them separately.
- Layer 3 same-entry surfacing remains semantically unchanged and no combined `rankingScore` is introduced.
- The focused highlight suite and typecheck pass, and `92-VALIDATION.md` documents the numeric contract plus non-goals.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-92-layer-1-evidence-scoring-make-preserved-strong-metrics-eligi/92-01-SUMMARY.md`
</output>
