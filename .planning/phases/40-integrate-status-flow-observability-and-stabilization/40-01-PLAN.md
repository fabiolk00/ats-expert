---
phase: 40-integrate-status-flow-observability-and-stabilization
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/jobs/repository.ts
  - src/app/api/jobs/[jobId]/route.ts
  - src/app/api/jobs/[jobId]/route.test.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/session/[id]/route.test.ts
  - src/app/api/file/[sessionId]/route.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/types/dashboard.ts
  - src/lib/dashboard/workspace-client.ts
  - src/hooks/use-session-documents.ts
  - src/hooks/use-session-documents.test.tsx
  - src/components/dashboard/session-documents-panel.tsx
  - src/components/dashboard/session-documents-panel.test.tsx
  - src/components/dashboard/resume-workspace.tsx
  - src/components/dashboard/resume-workspace.test.tsx
  - src/lib/agent/async-dispatch.ts
  - src/lib/jobs/runtime.ts
  - src/lib/jobs/processors/artifact-generation.ts
  - src/lib/agent/request-orchestrator.test.ts
  - src/app/api/agent/route.sse.test.ts
  - .planning/phases/40-integrate-status-flow-observability-and-stabilization/**
autonomous: true
requirements:
  - OBS-01
  - TEST-01
user_setup: []
must_haves:
  truths:
    - "Phase 40 consumes the durable job contracts frozen in Phase 37 and the runtime handoff shipped in Phases 38-39; it must not redefine `JobStatusSnapshot`, `JobStatus`, `JobType`, or the durable result-ref model."
    - "`/api/agent` remains the public chat entry point and lightweight chat streaming behavior must stay intact; Phase 40 only adds status consumption and observability around the async work."
    - "The brownfield dashboard and document sidebar stay recognizable. No websocket infrastructure, user-facing cancel/retry controls, or broad UX redesign belongs in this phase."
    - "`cvState` remains canonical resume truth, `agentState` remains operational context, and `generatedOutput` remains artifact metadata. Durable jobs are the canonical async lifecycle truth."
    - "All job, session, target, and artifact status reads must stay scoped by internal app user ID."
    - "Structured logs for edited backend surfaces must use `logInfo`, `logWarn`, and `logError` with correlatable fields instead of ad hoc logging."
  artifacts:
    - path: "src/app/api/jobs/[jobId]/route.ts"
      provides: "Canonical user-scoped durable job status read surface for ATS, targeting, and artifact jobs"
      contains: "export async function GET"
    - path: "src/app/api/session/[id]/route.ts"
      provides: "Workspace payload enriched with session-scoped durable job snapshots"
      contains: "jobs:"
    - path: "src/app/api/file/[sessionId]/route.ts"
      provides: "Artifact availability response with latest artifact-job lifecycle summary"
      contains: "generationStatus"
    - path: "src/types/dashboard.ts"
      provides: "Aligned dashboard contracts for async artifact acknowledgement and workspace job snapshots"
      contains: "jobId: string"
    - path: "src/hooks/use-session-documents.ts"
      provides: "Artifact polling hook that can distinguish generating, failed, and ready states"
      contains: "artifactStatus"
    - path: "src/app/api/jobs/[jobId]/route.test.ts"
      provides: "Focused proof that generic job status reads stay user-scoped and read-only"
      contains: "describe('GET /api/jobs/[jobId]'"
  key_links:
    - from: "src/app/api/jobs/[jobId]/route.ts"
      to: "src/lib/jobs/repository.ts"
      via: "the route must read durable job snapshots through the shared repository helper and app-user scoping only"
      pattern: "getJob("
    - from: "src/app/api/session/[id]/route.ts"
      to: "src/types/dashboard.ts"
      via: "the workspace payload shape and the browser-side type contract must stay identical"
      pattern: "jobs"
    - from: "src/app/api/file/[sessionId]/route.ts"
      to: "src/hooks/use-session-documents.ts"
      via: "the file route's artifact status summary drives the sidebar polling hook and visible states"
      pattern: "generationStatus"
    - from: "src/components/dashboard/resume-workspace.tsx"
      to: "src/lib/dashboard/workspace-client.ts"
      via: "workspace polling and generate acknowledgement handling must use the shared browser fetch helpers"
      pattern: "generateResume("
---

<objective>
Integrate durable async status flow into the brownfield dashboard and API surfaces, add diagnosable observability across dispatch and runtime boundaries, and lock the new execution model with focused regression proof.

Purpose: Phase 40 is where the async runtime becomes legible to both users and operators. The contracts and processors already exist; this phase makes their status, lifecycle, and failures visible without reopening the architecture.
Output: One generic durable-job status route, one session-scoped job projection, artifact polling that can distinguish generating/failed/ready, structured correlation logs across the async path, and focused regression coverage that proves sync chat parity plus async stability.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/40-integrate-status-flow-observability-and-stabilization/40-RESEARCH.md
@.planning/phases/40-integrate-status-flow-observability-and-stabilization/40-VALIDATION.md
@AGENTS.md
@CLAUDE.md
@src/types/jobs.ts
@src/types/dashboard.ts
@src/lib/jobs/repository.ts
@src/app/api/session/[id]/generate/route.ts
@src/app/api/session/[id]/route.ts
@src/app/api/file/[sessionId]/route.ts
@src/lib/dashboard/workspace-client.ts
@src/hooks/use-session-documents.ts
@src/components/dashboard/session-documents-panel.tsx
@src/components/dashboard/resume-workspace.tsx
@src/components/resume/resume-builder.tsx
@src/lib/observability/structured-log.ts
</context>

<threat_model>
<trust_boundaries>
- Authenticated browser polling -> user-scoped session, file, and generic job status routes
- Public chat SSE transport -> async dispatch/runtime observability surfaces
- Durable job repository -> read-only status projection for UI and operators
- Artifact availability route -> signed URL creation plus safe projection of artifact-job lifecycle state
</trust_boundaries>
<stride_register>
<threat>
  <threat_id>T-40-01</threat_id>
  <category>Elevation of Privilege</category>
  <component>Generic job and session status routes</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Scope every session, target, and job read by internal app user ID and return `404` for missing or foreign resources.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-40-02</threat_id>
  <category>Tampering</category>
  <component>Status contract projection</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Reuse `JobStatusSnapshot` and aligned dashboard DTOs exactly so clients do not invent a second lifecycle model or mis-handle terminal states.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-40-03</threat_id>
  <category>Information Disclosure</category>
  <component>Failure projection and artifact status responses</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Project safe terminal error messages from durable refs and keep raw diagnostics in structured logs instead of leaking internal details to the browser.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-40-04</threat_id>
  <category>Repudiation</category>
  <component>Dispatch/runtime observability</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Log every edited async surface with consistent correlation fields including `jobId`, `sessionId`, `resumeTargetId`, `type`, `status`, `stage`, and `latencyMs` where applicable.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-40-05</threat_id>
  <category>Denial of Service</category>
  <component>Status polling routes</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep generic status routes read-only so repeated polls cannot start or duplicate heavy work, and keep polling cadence bounded in the client hooks.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Add canonical durable-job read surfaces and align dashboard contracts</name>
  <files>src/lib/jobs/repository.ts, src/app/api/jobs/[jobId]/route.ts, src/app/api/jobs/[jobId]/route.test.ts, src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts, src/types/dashboard.ts, src/lib/dashboard/workspace-client.ts</files>
  <read_first>src/types/jobs.ts, src/lib/jobs/repository.ts, src/app/api/session/[id]/route.ts, src/app/api/session/[id]/generate/route.ts, src/types/dashboard.ts, .planning/phases/40-integrate-status-flow-observability-and-stabilization/40-RESEARCH.md</read_first>
  <action>Add the smallest repository read helper needed to project session-scoped durable jobs, such as `listJobsForSession({ userId, sessionId, limit })`, instead of querying the database directly from routes. Create `src/app/api/jobs/[jobId]/route.ts` as the canonical status read surface: authenticate with `getCurrentAppUser()`, load the durable job through `getJob(jobId, appUser.id)`, return the `JobStatusSnapshot` JSON unchanged on success, and return `401` or `404` on auth or ownership failure. The route must be read-only and must not call `startDurableJobProcessing(...)`, `createJob(...)`, or any runtime kickoff helper. Update `src/app/api/session/[id]/route.ts` to add a top-level `jobs: JobStatusSnapshot[]` array, newest-first, limited to the current session's ATS, targeting, and artifact jobs. Update `src/types/dashboard.ts` so `SessionWorkspace` includes that top-level `jobs` array and `GenerateResumeResponse` exactly matches the live durable generate route contract: `{ success: true; scope: 'base' | 'target'; targetId?: string; creditsUsed: number; generationType: ResumeGenerationType; jobId: string; inProgress?: boolean; resumeGenerationId?: string }`. Keep the route handlers thin and user-scoped.</action>
  <verify>npm run typecheck && npx vitest run src/app/api/jobs/[jobId]/route.test.ts src/app/api/session/[id]/route.test.ts</verify>
  <acceptance_criteria>
    - `src/app/api/jobs/[jobId]/route.ts` contains `export async function GET`
    - `src/app/api/jobs/[jobId]/route.ts` contains `getJob(`
    - `src/app/api/jobs/[jobId]/route.ts` does not contain `startDurableJobProcessing(`
    - `src/app/api/session/[id]/route.ts` contains `jobs:`
    - `src/types/dashboard.ts` contains `jobs: JobStatusSnapshot[]`
    - `src/types/dashboard.ts` contains `jobId: string`
    - `src/app/api/jobs/[jobId]/route.test.ts` contains `describe('GET /api/jobs/[jobId]'`
  </acceptance_criteria>
  <done>The app has one canonical durable-job status route and one aligned workspace payload contract that brownfield consumers can trust.</done>
</task>

<task type="auto">
  <name>Task 2: Wire workspace and document polling to durable status without widening the UX</name>
  <files>src/app/api/file/[sessionId]/route.ts, src/app/api/file/[sessionId]/route.test.ts, src/lib/dashboard/workspace-client.ts, src/hooks/use-session-documents.ts, src/hooks/use-session-documents.test.tsx, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/session-documents-panel.test.tsx, src/components/dashboard/resume-workspace.tsx, src/components/dashboard/resume-workspace.test.tsx</files>
  <read_first>src/app/api/file/[sessionId]/route.ts, src/lib/dashboard/workspace-client.ts, src/hooks/use-session-documents.ts, src/components/dashboard/session-documents-panel.tsx, src/components/dashboard/resume-workspace.tsx, src/components/resume/resume-builder.tsx, src/components/resume/resume-builder.test.tsx, .planning/phases/40-integrate-status-flow-observability-and-stabilization/40-RESEARCH.md</read_first>
  <action>Extend `/api/file/[sessionId]` so it still returns signed download URLs, but now also returns artifact lifecycle summary fields: `{ docxUrl, pdfUrl, available, generationStatus, jobId?, stage?, progress?, errorMessage? }`. `generationStatus` must reflect the current `generatedOutput` status plus the latest matching artifact durable job for the same base or target scope, so queued/running and failed states are distinguishable even before a file URL exists. Update `useSessionDocuments()` to return an `artifactStatus` object carrying those lifecycle fields alongside `files`, `isLoading`, `error`, and `refresh`. Update `SessionDocumentsPanel` to render explicit generating, failed, and ready states when a session exists instead of disappearing whenever no PDF URL is present. Update `ResumeWorkspace` so a `202` or `inProgress: true` generate response stores the returned `jobId`, sets status copy to `Geracao em andamento. Atualizaremos os arquivos quando estiver pronto.`, and polls `getSessionWorkspace()` until that exact job reaches `completed`, `failed`, or `cancelled` before showing terminal success or failure copy. Keep the current layout and interaction model; do not add cancel buttons, retry controls, or websocket logic.</action>
  <verify>npm run typecheck && npx vitest run src/app/api/file/[sessionId]/route.test.ts src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/session-documents-panel.test.tsx src/hooks/use-session-documents.test.tsx</verify>
  <acceptance_criteria>
    - `src/app/api/file/[sessionId]/route.ts` contains `generationStatus`
    - `src/app/api/file/[sessionId]/route.ts` contains `jobId`
    - `src/hooks/use-session-documents.ts` contains `artifactStatus`
    - `src/components/dashboard/session-documents-panel.tsx` contains `generationStatus`
    - `src/components/dashboard/resume-workspace.tsx` contains `Geracao em andamento. Atualizaremos os arquivos quando estiver pronto.`
    - `src/hooks/use-session-documents.test.tsx` contains `generating`
    - `src/components/dashboard/resume-workspace.test.tsx` contains `inProgress`
  </acceptance_criteria>
  <done>The brownfield dashboard can observe async artifact progress and failure states without pretending a durable acknowledgement means synchronous completion.</done>
</task>

<task type="auto">
  <name>Task 3: Add structured observability and keep status reads retry-safe</name>
  <files>src/lib/agent/async-dispatch.ts, src/lib/jobs/runtime.ts, src/lib/jobs/processors/artifact-generation.ts, src/app/api/jobs/[jobId]/route.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/jobs/[jobId]/route.test.ts, src/app/api/file/[sessionId]/route.test.ts, src/app/api/agent/route.sse.test.ts, src/lib/agent/request-orchestrator.test.ts</files>
  <read_first>src/lib/observability/structured-log.ts, src/lib/agent/async-dispatch.ts, src/lib/jobs/runtime.ts, src/lib/jobs/processors/artifact-generation.ts, src/app/api/jobs/[jobId]/route.ts, src/app/api/file/[sessionId]/route.ts, src/app/api/agent/route.sse.test.ts, .planning/phases/40-integrate-status-flow-observability-and-stabilization/40-RESEARCH.md</read_first>
  <action>Normalize the edited async path around structured, correlatable events. For every backend surface touched in this phase, ensure the logs include `jobId`, `sessionId`, `resumeTargetId`, `type`, `status`, `stage`, and `latencyMs` when known. Add or retain log events for created or reused dispatches, runtime completion or failure, generic status reads, and artifact availability responses. Keep both `GET /api/jobs/[jobId]` and `GET /api/file/[sessionId]` read-only: they must never call runtime kickoff helpers or create new jobs. When a durable artifact job is already failed, the file and status routes must surface a safe failure state and message rather than collapsing it into the same "not available yet" branch used for queued or running work. Update the route and orchestrator tests so they still prove heavy work starts only in dispatch paths and that status reads only observe the resulting durable state.</action>
  <verify>npm run typecheck && npx vitest run src/app/api/jobs/[jobId]/route.test.ts src/app/api/file/[sessionId]/route.test.ts src/app/api/agent/route.sse.test.ts src/lib/agent/request-orchestrator.test.ts</verify>
  <acceptance_criteria>
    - `src/app/api/jobs/[jobId]/route.ts` contains `logInfo(`
    - `src/app/api/jobs/[jobId]/route.ts` contains `jobId`
    - `src/app/api/jobs/[jobId]/route.ts` does not contain `startDurableJobProcessing(`
    - `src/app/api/file/[sessionId]/route.ts` contains `logInfo(`
    - `src/app/api/file/[sessionId]/route.ts` contains `generationStatus`
    - `src/app/api/agent/route.sse.test.ts` contains `startDurableJobProcessing`
    - `src/lib/agent/request-orchestrator.test.ts` contains `jobId`
  </acceptance_criteria>
  <done>Operators can correlate async dispatch, runtime, and status behavior from structured logs, and polling routes remain safe to call repeatedly.</done>
</task>

<task type="auto">
  <name>Task 4: Lock Phase 40 with focused regressions across sync chat parity and async status flow</name>
  <files>src/app/api/agent/route.sse.test.ts, src/lib/agent/request-orchestrator.test.ts, src/app/api/jobs/[jobId]/route.test.ts, src/app/api/session/[id]/route.test.ts, src/app/api/session/[id]/generate/route.test.ts, src/app/api/file/[sessionId]/route.test.ts, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/session-documents-panel.test.tsx, src/hooks/use-session-documents.test.tsx</files>
  <read_first>src/app/api/agent/route.sse.test.ts, src/lib/agent/request-orchestrator.test.ts, src/app/api/session/[id]/generate/route.test.ts, src/app/api/file/[sessionId]/route.test.ts, src/components/dashboard/resume-workspace.test.tsx, src/components/dashboard/session-documents-panel.test.tsx, .planning/phases/40-integrate-status-flow-observability-and-stabilization/40-VALIDATION.md</read_first>
  <action>Finish the focused regression suite so Phase 40 proves the integrated model end to end: sync chat still streams normally, heavy async actions still acknowledge with `jobId`, the session workspace payload carries durable job snapshots, generic job status reads are authenticated and user-scoped, the file route and document hook distinguish queued/running/failed/ready, and preview or artifact consistency still tracks the effective source snapshot selected by earlier phases. Keep the suite mock-driven and fast enough to run on every task commit via the quick-run command in `40-VALIDATION.md`.</action>
  <verify>npm run typecheck && npx vitest run src/app/api/agent/route.sse.test.ts src/lib/agent/request-orchestrator.test.ts src/app/api/jobs/[jobId]/route.test.ts src/app/api/session/[id]/route.test.ts src/app/api/session/[id]/generate/route.test.ts src/app/api/file/[sessionId]/route.test.ts src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/session-documents-panel.test.tsx src/hooks/use-session-documents.test.tsx</verify>
  <acceptance_criteria>
    - `src/app/api/jobs/[jobId]/route.test.ts` contains `404`
    - `src/app/api/session/[id]/route.test.ts` contains `jobs`
    - `src/app/api/file/[sessionId]/route.test.ts` contains `generationStatus`
    - `src/components/dashboard/session-documents-panel.test.tsx` contains `failed`
    - `src/hooks/use-session-documents.test.tsx` contains `ready`
    - `src/app/api/agent/route.sse.test.ts` contains `startDurableJobProcessing`
    - `npm run typecheck && npx vitest run src/app/api/agent/route.sse.test.ts src/lib/agent/request-orchestrator.test.ts src/app/api/jobs/[jobId]/route.test.ts src/app/api/session/[id]/route.test.ts src/app/api/session/[id]/generate/route.test.ts src/app/api/file/[sessionId]/route.test.ts src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/session-documents-panel.test.tsx src/hooks/use-session-documents.test.tsx` exits 0
  </acceptance_criteria>
  <done>Phase 40 closes with focused proof that the async runtime is observable, user-visible, and still compatible with the existing chat flow.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/app/api/agent/route.sse.test.ts src/lib/agent/request-orchestrator.test.ts src/app/api/jobs/[jobId]/route.test.ts src/app/api/session/[id]/route.test.ts src/app/api/session/[id]/generate/route.test.ts src/app/api/file/[sessionId]/route.test.ts src/components/dashboard/resume-workspace.test.tsx src/components/dashboard/session-documents-panel.test.tsx src/hooks/use-session-documents.test.tsx`
- [ ] confirm `GET /api/jobs/[jobId]` is read-only and never starts or dispatches work
- [ ] confirm `GET /api/session/[id]` returns a top-level `jobs` array that matches `SessionWorkspace`
- [ ] confirm `/api/file/[sessionId]` can distinguish generating, failed, and ready artifact states without hiding terminal failure behind `available: false`
- [ ] confirm sync chat parity tests stay green while async status tests are added
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 40 leaves the repo with one canonical durable-job read route, one aligned workspace job projection, artifact polling that exposes lifecycle state, structured async observability, and focused regression proof that sync chat parity and async status flow both hold
</success_criteria>

<output>
After completion, create `.planning/phases/40-integrate-status-flow-observability-and-stabilization/40-01-SUMMARY.md`
</output>
