---
phase: 03-billing-settlement-validation
plan: "02"
type: execute
wave: 2
depends_on:
  - "03-01"
files_modified:
  - .planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md
  - .planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md
  - .planning/phases/03-billing-settlement-validation/03-SCENARIO-RESPONSES.json
autonomous: true
requirements:
  - BILL-01
  - BILL-02
  - BILL-03
user_setup:
  - Populate `.env.staging` with current staging secrets and database connection values
  - Ensure `bash`, `curl`, and `psql` are available on the operator workstation
  - Provide staging API access, staging database access, and Asaas sandbox webhook credentials
  - Provision or reset a staging billing user and known identifiers before running the matrix
must_haves:
  truths:
    - "Every required settlement scenario is executed in staging and recorded with request identifiers, responses, and resulting billing state."
    - "Duplicate or replay scenarios prove credits are granted exactly once for the same settled event."
    - "Display totals from `user_quotas` are compared directly against runtime balances from `credit_accounts` during validation."
  artifacts:
    - path: ".planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md"
      provides: "Auditable narrative evidence for every staging billing scenario"
      contains: "duplicate_delivery"
    - path: ".planning/phases/03-billing-settlement-validation/03-SCENARIO-RESPONSES.json"
      provides: "Structured request and response capture for staged scenarios"
      contains: "one_time_settlement"
    - path: ".planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md"
      provides: "Explicit gap register with requirement and owner mapping"
      contains: "owner_files"
  key_links:
    - from: ".planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md"
      to: "scripts/replay-staging-asaas.ts"
      via: "documented replay command per scenario"
      pattern: "--scenario"
    - from: ".planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md"
      to: "scripts/check-staging-billing-state.ts"
      via: "paired billing snapshots"
      pattern: "credit_accounts"
    - from: ".planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md"
      to: "src/lib/asaas/event-handlers.ts"
      via: "owner file mapping for any detected inconsistency"
      pattern: "owner_files"
---

<objective>
Run the live staging billing validation matrix and capture evidence that can withstand audit and replay.

Purpose: Local tests already cover the billing contract, but Phase 3 requirements are only satisfied when staging behavior is exercised with real operator credentials, recorded, and compared against the expected settlement semantics.
Output: Staging evidence log, raw scenario response capture, and a structured gaps register.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-billing-settlement-validation/03-RESEARCH.md
@.planning/phases/03-billing-settlement-validation/03-VALIDATION.md
@.planning/phases/03-billing-settlement-validation/03-01-PLAN.md
@AGENTS.md
@.env.staging.example
@scripts/verify-staging.sh
@scripts/replay-staging-asaas.ts
@scripts/check-staging-billing-state.ts
@docs/staging/SETUP_GUIDE.md
@docs/staging/VALIDATION_PLAN.md
@docs/billing/OPS_RUNBOOK.md
@docs/billing/IMPLEMENTATION.md
</context>

<threat_model>
<trust_boundaries>
- Operator-run replay commands -> staging billing webhook endpoint
- Evidence files -> future remediation and launch decisions
- Snapshot helper -> staging database reads used to prove idempotency and display correctness
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-03-04</threat_id>
  <category>Repudiation</category>
  <component>Staging evidence capture</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Record every scenario name, command, timestamp, identifiers, response, and resulting state in committed evidence artifacts.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-05</threat_id>
  <category>Tampering</category>
  <component>Replay execution</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Run only committed named scenarios and preserve raw request and response payloads in a structured artifact for later review.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-03-06</threat_id>
  <category>Integrity</category>
  <component>Display-total validation</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Compare runtime balances and display totals explicitly per scenario rather than inferring display correctness from successful webhook processing.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="manual">
  <name>Task 1: Run staging preflight and capture baseline billing state</name>
  <files>.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md, .planning/phases/03-billing-settlement-validation/03-SCENARIO-RESPONSES.json</files>
  <read_first>scripts/verify-staging.sh, scripts/check-staging-billing-state.ts, docs/staging/SETUP_GUIDE.md, docs/staging/VALIDATION_PLAN.md, docs/billing/OPS_RUNBOOK.md</read_first>
  <action>Run `bash scripts/verify-staging.sh` and record the actual operator result in `03-STAGING-EVIDENCE.md`, including shell, environment, and migration readiness. Choose the Phase 3 staging user or fixture identifiers, then run `tsx scripts/check-staging-billing-state.ts --user <user>` and save the baseline snapshot to both the evidence markdown and `03-SCENARIO-RESPONSES.json`.</action>
  <verify>bash scripts/verify-staging.sh</verify>
  <acceptance_criteria>
    - `03-STAGING-EVIDENCE.md` records the preflight result, operator date, and chosen user or identifiers
    - baseline state includes `billing_checkouts`, `credit_accounts`, `user_quotas`, and `processed_events`
    - `03-SCENARIO-RESPONSES.json` stores baseline identifiers before scenario replay begins
  </acceptance_criteria>
  <done>The live staging environment is proven ready and Phase 3 has a baseline state to compare against.</done>
</task>

<task type="manual">
  <name>Task 2: Execute every named settlement scenario and record the resulting state</name>
  <files>.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md, .planning/phases/03-billing-settlement-validation/03-SCENARIO-RESPONSES.json</files>
  <read_first>scripts/replay-staging-asaas.ts, scripts/check-staging-billing-state.ts, docs/staging/VALIDATION_PLAN.md, docs/billing/IMPLEMENTATION.md</read_first>
  <action>Run the staging replay helper for `one_time_settlement`, `inactive_subscription_snapshot`, `initial_recurring_activation`, `renewal_replace_balance`, `cancellation_metadata_only`, `duplicate_delivery`, and `partial_success_reconcile`. After each replay, capture the command, webhook response, database snapshot, relevant identifiers, and explicit `BILL-01`, `BILL-02`, and `BILL-03` pass or fail judgments in `03-STAGING-EVIDENCE.md`. Store raw request and response payloads in `03-SCENARIO-RESPONSES.json` so the evidence is auditable without re-running staging immediately.</action>
  <verify>powershell -NoProfile -Command "$names = 'one_time_settlement','inactive_subscription_snapshot','initial_recurring_activation','renewal_replace_balance','cancellation_metadata_only','duplicate_delivery','partial_success_reconcile'; $content = Get-Content '.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md' -Raw; foreach ($name in $names) { if ($content -notmatch [regex]::Escape($name)) { exit 1 } }; exit 0"</verify>
  <acceptance_criteria>
    - all seven named scenarios appear in `03-STAGING-EVIDENCE.md`
    - every scenario entry records request identifiers, response outcome, and post-run state
    - duplicate and replay scenarios explicitly state whether credits were granted once or more than once
    - display totals versus runtime balances are recorded for scenarios that mutate credit state
  </acceptance_criteria>
  <done>Every required billing scenario has staged evidence instead of relying on assumptions from local tests.</done>
</task>

<task type="manual">
  <name>Task 3: Create the structured gaps register for any failed or warning scenarios</name>
  <files>.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md</files>
  <read_first>.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md, docs/billing/IMPLEMENTATION.md, src/lib/asaas/event-handlers.ts, src/lib/asaas/credit-grants.ts, src/lib/asaas/quota.ts, src/app/api/checkout/route.ts, src/lib/asaas/external-reference.ts</read_first>
  <action>Review the staging evidence and create `03-STAGING-GAPS.md`. For every failed or warning scenario, record the scenario name, requirement ID, observed behavior, expected behavior, likely owner files, and issue class (`code`, `docs`, `data`, or `operator`). If all scenarios pass, still create the file with a `No open gaps` section and a short explanation of why remediation is not required.</action>
  <verify>powershell -NoProfile -Command "$targets = 'owner_files','BILL-01','BILL-02','BILL-03','No open gaps'; $content = Get-Content '.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md' -Raw; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"</verify>
  <acceptance_criteria>
    - each open or warning scenario maps to at least one requirement ID
    - each gap includes `owner_files` and an issue class
    - the file still exists and is explicit even if there are zero gaps
  </acceptance_criteria>
  <done>Phase 3 has an explicit remediation target list instead of burying staging issues in narrative notes.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `bash scripts/verify-staging.sh`
- [ ] `powershell -NoProfile -Command "$names = 'one_time_settlement','inactive_subscription_snapshot','initial_recurring_activation','renewal_replace_balance','cancellation_metadata_only','duplicate_delivery','partial_success_reconcile'; $content = Get-Content '.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md' -Raw; foreach ($name in $names) { if ($content -notmatch [regex]::Escape($name)) { exit 1 } }; exit 0"`
- [ ] `powershell -NoProfile -Command "$targets = 'runtime_balance','display_total','credit_accounts','user_quotas'; $files = '.planning/phases/03-billing-settlement-validation/03-STAGING-EVIDENCE.md','.planning/phases/03-billing-settlement-validation/03-STAGING-GAPS.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 3 has auditable staging evidence for the full billing matrix and an explicit gaps register
  </success_criteria>

<output>
After completion, create `.planning/phases/03-billing-settlement-validation/03-02-SUMMARY.md`
</output>
