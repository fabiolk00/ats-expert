# 78-01 Plan

1. Add an `AsyncLocalStorage` request-query context with bounded sampling and reusable flush helpers.
2. Instrument the live Supabase admin client with a custom `global.fetch` that counts only PostgREST `/rest/v1/` DB requests.
3. Add a reusable request wrapper for tracked API handlers, with explicit stream-end flushing for `/api/agent`.
4. Wrap `/api/agent`, `/api/session/[id]`, `/api/file/[sessionId]`, and `/api/session/[id]/generate` without changing their HTTP contracts.
5. Add unit and route-level regression coverage for context accounting, threshold warnings, Supabase seam filtering, and tracked-route summaries.
