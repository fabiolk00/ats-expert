# Phase 85 Plan

1. Replace `improvedSummaryClarity` length bias with structural checks:
   - meaningful change
   - not too close to original
   - not structurally noisy
   - minimum viable summary substance without requiring growth
2. Emit explicit `keywordCoverageImprovement` in `ats_enhancement` by comparing keyword visibility across summary, skills, and experience.
3. Keep the no-JD fallback available only when no explicit keyword signal exists.
4. Add readiness contract tests and rewrite pipeline tests for the new behavior.
5. Verify with focused Vitest runs plus `npm run typecheck`.
