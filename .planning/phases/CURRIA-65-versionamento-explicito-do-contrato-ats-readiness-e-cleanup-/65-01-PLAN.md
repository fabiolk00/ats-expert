# 65-01 Plan

- Promote the ATS Readiness canonical contract constant and exported types to an explicit v2 shape.
- Add formal v1-to-v2 normalization in the canonical readiness resolver, including legacy `withheld_pending_quality` migration and contract-version-aware telemetry.
- Keep new writes and canonical reads on the v2 shape only.
- Reduce ambiguous internal `atsScore` references by relabeling internal-only raw score usage and aligning agent-facing strings with exact versus estimated readiness semantics.
- Extend regression coverage for contract versioning, legacy normalization, mixed-shape compatibility, and agent/context copy alignment.
