# 75-01 Plan

## Goal

Review and harden the manual-edit versus active-export flow so the Phase 74 policy is explicit, test-backed, and product-honest instead of merely avoiding the worst deadlock.

## Steps

1. Review the Phase 74 save/export/download policy as a product state machine, not just a route patch.
2. Make stale preserved artifacts explicit in the file-access response contract.
3. Surface stale-artifact state in preview/download UI so saved edits and old PDFs are not silently conflated.
4. Strengthen manual-edit, file-access, preview, and editor tests around save-persisted plus deferred-refresh scenarios.
5. Record the adjusted policy and residual risks in GSD artifacts.

## Verification

- `npm run typecheck`
- `npx vitest run "src/app/api/file/[sessionId]/route.test.ts" "src/app/api/session/[id]/manual-edit/route.test.ts" "src/components/dashboard/resume-editor-modal.test.tsx" "src/components/dashboard/preview-panel.test.tsx" "src/lib/routes/session-generate/policy.test.ts"`
- `npm run audit:copy-regression`
