---
phase: 01-contract-alignment-and-fail-fast-guards
plan: "03"
type: execute
wave: 2
depends_on:
  - "01-01"
  - "01-02"
files_modified:
  - docs/PRODUCTION-READINESS-CHECKLIST.md
  - docs/staging/VALIDATION_PLAN.md
  - docs/staging/SETUP_GUIDE.md
  - scripts/verify-staging.sh
  - scripts/README.md
autonomous: true
requirements:
  - OPS-03
user_setup: []
must_haves:
  truths:
    - "Production-readiness documentation names the current billing migrations, internal event expectations, and proof commands without stale prerequisites."
    - "Staging setup guidance, the staging env template, and `scripts/verify-staging.sh` describe the same required staging inputs."
    - "Operators can tell exactly which commands prove the Phase 1 contract locally, in CI, and in real staging."
  artifacts:
    - path: "docs/PRODUCTION-READINESS-CHECKLIST.md"
      provides: "Launch checklist aligned to the current settlement billing contract"
      contains: "20260406_align_asaas_webhook_contract.sql"
    - path: "docs/staging/VALIDATION_PLAN.md"
      provides: "Updated staging billing scenarios and prerequisites"
      contains: "PAYMENT_SETTLED"
    - path: "docs/staging/SETUP_GUIDE.md"
      provides: "Template-driven staging setup instructions"
      contains: ".env.staging.example"
    - path: "scripts/verify-staging.sh"
      provides: "Runnable staging prerequisite check"
      contains: "STAGING_ASAAS_WEBHOOK_TOKEN"
    - path: "scripts/README.md"
      provides: "Operator-facing command index for verification scripts"
      contains: "verify-staging.sh"
  key_links:
    - from: "docs/staging/SETUP_GUIDE.md"
      to: "scripts/verify-staging.sh"
      via: "documented preflight script"
      pattern: "verify-staging\\.sh"
    - from: "docs/staging/SETUP_GUIDE.md"
      to: ".env.staging.example"
      via: "copy-from-template setup"
      pattern: "\\.env\\.staging\\.example"
    - from: "docs/PRODUCTION-READINESS-CHECKLIST.md"
      to: "docs/staging/VALIDATION_PLAN.md"
      via: "shared billing event and migration contract"
      pattern: "PAYMENT_SETTLED|SUBSCRIPTION_STARTED"
---

<objective>
Refresh the production and staging runbooks so they match the hardened Phase 1 contract and tell operators exactly how to prove it.

Purpose: Code hardening is not enough if deploy and validation docs still point at stale migrations, missing templates, or ambiguous operator steps.
Output: Updated production-readiness checklist, staging setup and validation docs, and an aligned staging verification script reference.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-CONTEXT.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-RESEARCH.md
@.planning/phases/01-contract-alignment-and-fail-fast-guards/01-VALIDATION.md
@docs/PRODUCTION-READINESS-CHECKLIST.md
@docs/staging/VALIDATION_PLAN.md
@docs/staging/SETUP_GUIDE.md
@scripts/verify-staging.sh
@scripts/README.md
@.env.staging.example
@.github/workflows/ci.yml
@package.json
</context>

<threat_model>
<trust_boundaries>
- Repo runbooks and scripts -> operators running staging validation and production rollout
- Staging prerequisite script -> live staging database, webhook token, and deployment endpoint
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-01-06</threat_id>
  <category>Repudiation</category>
  <component>Production and staging runbooks</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Rewrite the checklist and validation docs so they name the current migration set, internal event expectations, and exact proof commands used in Phase 1.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-01-07</threat_id>
  <category>Tampering</category>
  <component>Staging template, setup guide, and verification script</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep `.env.staging.example`, `docs/staging/SETUP_GUIDE.md`, `docs/staging/VALIDATION_PLAN.md`, and `scripts/verify-staging.sh` synchronized on the same required staging vars and preflight flow.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Refresh the production readiness checklist to the live settlement contract</name>
  <files>docs/PRODUCTION-READINESS-CHECKLIST.md</files>
  <read_first>docs/PRODUCTION-READINESS-CHECKLIST.md, docs/billing/IMPLEMENTATION.md, docs/billing/MIGRATION_GUIDE.md, docs/staging/VALIDATION_PLAN.md, prisma/migrations/billing_webhook_hardening.sql, prisma/migrations/20260406_align_asaas_webhook_contract.sql, prisma/migrations/20260406_fix_billing_checkout_timestamp_defaults.sql, prisma/migrations/20260407_persist_billing_display_totals.sql, prisma/migrations/20260407_harden_text_id_generation.sql, prisma/migrations/20260407_harden_standard_timestamps.sql</read_first>
  <action>Rewrite `docs/PRODUCTION-READINESS-CHECKLIST.md` so the required migration list, DB verification queries, and runtime behavior checks match the current settlement-based billing implementation. Keep the migration names exact, keep the processed-event expectations on the internal names (`PAYMENT_SETTLED`, `SUBSCRIPTION_STARTED`, and related values), and remove any stale prerequisite wording that no longer matches the code or docs. Add a short verification section that explicitly lists `npm run typecheck` and `npm test` as the repo-local proof commands for Phase 1.</action>
  <verify>rg -n "20260406_align_asaas_webhook_contract.sql|20260406_fix_billing_checkout_timestamp_defaults.sql|20260407_persist_billing_display_totals.sql|20260407_harden_text_id_generation.sql|20260407_harden_standard_timestamps.sql|PAYMENT_SETTLED|SUBSCRIPTION_STARTED|npm run typecheck|npm test" docs/PRODUCTION-READINESS-CHECKLIST.md</verify>
  <acceptance_criteria>
    - `docs/PRODUCTION-READINESS-CHECKLIST.md` contains the exact current billing migration filenames
    - `docs/PRODUCTION-READINESS-CHECKLIST.md` references internal processed-event names such as `PAYMENT_SETTLED` and `SUBSCRIPTION_STARTED`
    - `docs/PRODUCTION-READINESS-CHECKLIST.md` contains `npm run typecheck` and `npm test`
    - `docs/PRODUCTION-READINESS-CHECKLIST.md` does not introduce legacy env names such as `ASAAS_API_KEY` or `UPSTASH_REDIS_URL`
  </acceptance_criteria>
  <done>The production checklist matches the live billing contract and tells operators how to prove the repo-side Phase 1 changes.</done>
</task>

<task type="auto">
  <name>Task 2: Align staging setup docs and the verification script around one operator flow</name>
  <files>docs/staging/VALIDATION_PLAN.md, docs/staging/SETUP_GUIDE.md, scripts/verify-staging.sh, scripts/README.md</files>
  <read_first>docs/staging/VALIDATION_PLAN.md, docs/staging/SETUP_GUIDE.md, scripts/verify-staging.sh, scripts/README.md, .env.staging.example</read_first>
  <action>Update the staging docs so they explicitly start from `.env.staging.example`, keep the required staging vars synchronized with `scripts/verify-staging.sh`, and mention the preflight script before running billing scenarios. Update `scripts/verify-staging.sh` so its prerequisite messages explicitly name `.env.staging.example`, `STAGING_ASAAS_WEBHOOK_TOKEN`, `STAGING_ASAAS_ACCESS_TOKEN`, and the current migration guidance used by the docs. In `scripts/README.md`, describe the script as the operator proof step for staging readiness and keep the command copy-pastable as `bash scripts/verify-staging.sh`.</action>
  <verify>rg -n "\\.env\\.staging\\.example|bash scripts/verify-staging\\.sh|STAGING_ASAAS_WEBHOOK_TOKEN|STAGING_ASAAS_ACCESS_TOKEN" docs/staging/VALIDATION_PLAN.md docs/staging/SETUP_GUIDE.md scripts/verify-staging.sh scripts/README.md</verify>
  <acceptance_criteria>
    - `docs/staging/SETUP_GUIDE.md` contains `.env.staging.example`
    - `docs/staging/VALIDATION_PLAN.md` contains `bash scripts/verify-staging.sh`
    - `scripts/verify-staging.sh` contains `.env.staging.example`, `STAGING_ASAAS_WEBHOOK_TOKEN`, and `STAGING_ASAAS_ACCESS_TOKEN`
    - `scripts/README.md` contains `bash scripts/verify-staging.sh`
  </acceptance_criteria>
  <done>Operators have one consistent staging setup and preflight path instead of partially overlapping instructions.</done>
</task>

<task type="auto">
  <name>Task 3: Make the Phase 1 proof set explicit in operator-facing docs</name>
  <files>docs/PRODUCTION-READINESS-CHECKLIST.md, docs/staging/VALIDATION_PLAN.md, scripts/README.md</files>
  <read_first>.planning/phases/01-contract-alignment-and-fail-fast-guards/01-VALIDATION.md, .github/workflows/ci.yml, package.json, docs/PRODUCTION-READINESS-CHECKLIST.md, docs/staging/VALIDATION_PLAN.md, scripts/README.md</read_first>
  <action>Add a short "proof set" section where operators can see the exact commands that must be green after Phase 1 execution. Use the exact labels `Repo-local proof` and `Live staging proof`, and list `npm run typecheck`, `npm test`, and `bash scripts/verify-staging.sh` under the correct heading. Keep the wording copy-pastable and do not invent new commands that are not already present in `package.json` or `scripts/`.</action>
  <verify>rg -n "npm run typecheck|npm test|bash scripts/verify-staging\\.sh" docs/PRODUCTION-READINESS-CHECKLIST.md docs/staging/VALIDATION_PLAN.md scripts/README.md</verify>
  <acceptance_criteria>
    - `docs/PRODUCTION-READINESS-CHECKLIST.md` contains `npm run typecheck` and `npm test`
    - `docs/staging/VALIDATION_PLAN.md` contains `Repo-local proof`, `Live staging proof`, and `bash scripts/verify-staging.sh`
    - `scripts/README.md` contains `bash scripts/verify-staging.sh`
  </acceptance_criteria>
  <done>The Phase 1 validation bar is visible to operators without requiring them to reverse-engineer it from code or planning docs.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `rg -n "20260406_align_asaas_webhook_contract.sql|20260406_fix_billing_checkout_timestamp_defaults.sql|20260407_persist_billing_display_totals.sql|20260407_harden_text_id_generation.sql|20260407_harden_standard_timestamps.sql|PAYMENT_SETTLED|SUBSCRIPTION_STARTED" docs/PRODUCTION-READINESS-CHECKLIST.md docs/staging/VALIDATION_PLAN.md`
- [ ] `rg -n "\\.env\\.staging\\.example|bash scripts/verify-staging\\.sh|STAGING_ASAAS_WEBHOOK_TOKEN|STAGING_ASAAS_ACCESS_TOKEN|npm run typecheck|npm test" docs/staging/SETUP_GUIDE.md docs/staging/VALIDATION_PLAN.md scripts/verify-staging.sh scripts/README.md docs/PRODUCTION-READINESS-CHECKLIST.md`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Production and staging operators can execute the current billing rollout and preflight flow without relying on stale tribal knowledge
  </success_criteria>

<output>
After completion, create `.planning/phases/01-contract-alignment-and-fail-fast-guards/01-03-SUMMARY.md`
</output>
