---
phase: 04-observability-and-launch-readiness
plan: "03"
type: execute
wave: 3
depends_on:
  - "01"
  - "02"
files_modified:
  - docs/logging.md
  - docs/PRODUCTION-READINESS-CHECKLIST.md
  - docs/launch-readiness.md
  - docs/INDEX.md
autonomous: true
requirements:
  - OBS-01
  - OBS-02
user_setup: []
must_haves:
  truths:
    - "The final handoff documents match the current structured-log payload shape and the Phase 1-4 proof commands."
    - "Launch blockers, non-blocking caveats, and the current release decision are captured in one committed location."
    - "Operators can find both the observability events and the launch proof set without reading planning artifacts."
  artifacts:
    - path: "docs/logging.md"
      provides: "Current structured-log shape and query guidance"
      contains: "event"
    - path: "docs/PRODUCTION-READINESS-CHECKLIST.md"
      provides: "Final launch proof set across env, browser, billing, and observability"
      contains: "npm run test:e2e -- --project=chromium"
    - path: "docs/launch-readiness.md"
      provides: "Go or no-go summary with blockers, caveats, and handoff notes"
      contains: "launch decision"
  key_links:
    - from: "docs/launch-readiness.md"
      to: "docs/PRODUCTION-READINESS-CHECKLIST.md"
      via: "final handoff references the operational checklist"
      pattern: "Production Readiness Checklist"
    - from: "docs/logging.md"
      to: "src/lib/observability/structured-log.ts"
      via: "documentation matches the real logger shape"
      pattern: "event"
    - from: "docs/INDEX.md"
      to: "docs/launch-readiness.md"
      via: "operators can discover the final launch handoff from the docs index"
      pattern: "launch-readiness"
---

<objective>
Produce the final launch-readiness sweep and handoff notes for the milestone.

Purpose: The last phase plan should consolidate what the code now guarantees, what operators must verify before deploy, and which caveats still remain after the observability pass.
Output: Updated logging and production-readiness docs, a committed launch-readiness handoff, and a final proof set aligned with Phases 1 through 4.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-observability-and-launch-readiness/04-RESEARCH.md
@.planning/phases/04-observability-and-launch-readiness/04-VALIDATION.md
@AGENTS.md
@README.md
@docs/logging.md
@docs/PRODUCTION-READINESS-CHECKLIST.md
@docs/billing/MONITORING.md
@src/lib/observability/structured-log.ts
@.github/workflows/ci.yml
@playwright.config.ts
@scripts/verify-staging.sh
</context>

<threat_model>
<trust_boundaries>
- Operator handoff docs -> release execution and incident response
- Structured-log documentation -> querying live production diagnostics
- Launch-readiness summary -> go or no-go decisions by humans
</trust_boundaries>

<stride_register>
<threat>
  <threat_id>T-04-07</threat_id>
  <category>Tampering</category>
  <component>Release documentation</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Update docs only from the current code and committed proof commands rather than copying stale planning assumptions or historical examples.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-04-08</threat_id>
  <category>Repudiation</category>
  <component>Launch decision</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Record the actual proof set, blockers, caveats, and final recommendation in a committed handoff document so the release decision is auditable later.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Reconcile logging and readiness docs with the shipped system</name>
  <files>docs/logging.md, docs/PRODUCTION-READINESS-CHECKLIST.md, docs/INDEX.md</files>
  <read_first>docs/logging.md, docs/PRODUCTION-READINESS-CHECKLIST.md, docs/billing/MONITORING.md, src/lib/observability/structured-log.ts, .github/workflows/ci.yml, playwright.config.ts, scripts/verify-staging.sh</read_first>
  <action>Update `docs/logging.md` so it matches the current flat JSON envelope from `structured-log.ts`, including example fields and query guidance for the new event names. Refresh `docs/PRODUCTION-READINESS-CHECKLIST.md` so the final proof set includes the real Phase 1-4 commands: `npm run typecheck`, the focused Vitest coverage, the Chromium Playwright lane, and `bash scripts/verify-staging.sh`. Update `docs/INDEX.md` so the final launch-readiness handoff is discoverable from the docs index.</action>
  <verify>powershell -NoProfile -Command "$targets = 'event','timestamp','npm run test:e2e -- --project=chromium','bash scripts/verify-staging.sh'; $files = 'docs/logging.md','docs/PRODUCTION-READINESS-CHECKLIST.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"</verify>
  <acceptance_criteria>
    - logging docs describe the current event-based structured-log payload instead of the old metadata envelope
    - production-readiness docs name the actual repo-local and live proof commands from the milestone
    - the docs index links to the final launch-readiness handoff
  </acceptance_criteria>
  <done>Operators and developers can find the current observability and release proof docs without consulting planning artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Capture the final launch decision and handoff notes</name>
  <files>docs/launch-readiness.md</files>
  <read_first>.planning/PROJECT.md, .planning/ROADMAP.md, .planning/REQUIREMENTS.md, docs/PRODUCTION-READINESS-CHECKLIST.md, docs/billing/MONITORING.md, README.md</read_first>
  <action>Create `docs/launch-readiness.md` as the final milestone handoff. Summarize what Phases 1 through 4 now guarantee, list the automated and live proof commands, name the key structured events for agent, billing, and profile import diagnostics, record any remaining caveats, and end with an explicit launch decision such as ready for controlled launch or blocked with reasons. Keep the decision grounded in what actually shipped during this milestone.</action>
  <verify>powershell -NoProfile -Command "$targets = 'launch decision','Phase 1','Phase 2','Phase 3','Phase 4','billing.info.load_failed','npm run typecheck'; $content = Get-Content 'docs/launch-readiness.md' -Raw; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"</verify>
  <acceptance_criteria>
    - the final handoff names the current release decision explicitly
    - the handoff references the actual proof set from the milestone
    - the handoff records at least one remaining caveat or states clearly that none remain
    - the handoff points operators to the most important log events for incident response
  </acceptance_criteria>
  <done>The milestone ends with a real release handoff instead of leaving the launch decision implicit in planning files.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `powershell -NoProfile -Command "$targets = 'event','timestamp','npm run test:e2e -- --project=chromium','bash scripts/verify-staging.sh'; $files = 'docs/logging.md','docs/PRODUCTION-READINESS-CHECKLIST.md'; $content = ($files | ForEach-Object { Get-Content $_ -Raw }) -join \"`n\"; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"`
- [ ] `powershell -NoProfile -Command "$targets = 'launch decision','Phase 1','Phase 2','Phase 3','Phase 4'; $content = Get-Content 'docs/launch-readiness.md' -Raw; foreach ($target in $targets) { if ($content -notmatch [regex]::Escape($target)) { exit 1 } }; exit 0"`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Final launch-readiness docs accurately describe the shipped proof set, observability events, and release decision
  </success_criteria>

<output>
After completion, create `.planning/phases/04-observability-and-launch-readiness/04-03-SUMMARY.md`
</output>
