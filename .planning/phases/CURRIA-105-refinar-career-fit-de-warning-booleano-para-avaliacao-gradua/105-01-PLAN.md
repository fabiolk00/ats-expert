---
phase: 105-refinar-career-fit-de-warning-booleano-para-avaliacao-gradua
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/agent.ts
  - src/lib/agent/profile-review.ts
  - src/lib/agent/job-targeting-pipeline.ts
  - src/lib/agent/tools/index.ts
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/context/sources/build-source-context.ts
  - src/lib/agent/profile-review.test.ts
  - src/lib/agent/streaming-loop.test.ts
  - src/lib/agent/context-builder.test.ts
autonomous: true
requirements:
  - CAREER-FIT-RISK-01
  - CAREER-FIT-GATE-01
  - CAREER-FIT-TEST-01
user_setup: []
must_haves:
  truths:
    - "Career fit is persisted as a structured low/medium/high evaluation with risk points and signals, while existing target-fit compatibility fields remain available."
    - "Adjacent-role scenarios can surface a warning without forcing the same hard override used for structurally high-risk mismatches."
    - "High-risk generation still requires explicit confirmation before generation, while medium-risk generation warns and then proceeds without storing a high-risk override."
    - "Prompt/context guardrails and regression tests stay aligned with the persisted risk level."
  artifacts:
    - path: "src/types/agent.ts"
      provides: "Additive career-fit domain types and persisted agent-state field"
      contains: "CareerFitEvaluation"
    - path: "src/lib/agent/profile-review.ts"
      provides: "Central graduated risk evaluation, compatibility wrappers, warning copy, and guardrail snapshot"
      contains: "evaluateCareerFitRisk"
    - path: "src/lib/agent/agent-loop.ts"
      provides: "Risk-level-based generation gating and phaseMeta persistence"
      contains: "careerFitRiskLevelAtWarning"
    - path: "src/lib/agent/profile-review.test.ts"
      provides: "Low/medium/high domain proof including adjacent-role behavior"
      contains: "evaluateCareerFitRisk"
    - path: "src/lib/agent/streaming-loop.test.ts"
      provides: "Generation gating proof for medium warning versus high override"
      contains: "careerFitRiskLevelAtWarning"
  key_links:
    - from: "src/lib/agent/profile-review.ts"
      to: "src/lib/agent/job-targeting-pipeline.ts"
      via: "targeting pipeline must persist the same central evaluation"
      pattern: "evaluateCareerFitRisk"
    - from: "src/lib/agent/profile-review.ts"
      to: "src/lib/agent/tools/index.ts"
      via: "tool-driven analyze_gap must persist the same evaluation path"
      pattern: "evaluateCareerFitRisk"
    - from: "src/lib/agent/profile-review.ts"
      to: "src/lib/agent/agent-loop.ts"
      via: "loop gating must distinguish medium warning from high override"
      pattern: "(riskLevel|needsExplicitConfirmation)"
    - from: "src/lib/agent/profile-review.ts"
      to: "src/lib/agent/context/sources/build-source-context.ts"
      via: "prompt guardrail must render the current risk level consistently"
      pattern: "buildCareerFitPromptSnapshot"
---

<objective>
Refactor career fit evaluation from a boolean warning into a graduated low/medium/high risk system while keeping all current compatibility fields and consumers functioning during the migration.

Purpose: remove rigid hard-block behavior for adjacent-role targeting without weakening the honesty guardrail for structurally poor-fit applications.
Output: one persisted `careerFitEvaluation` seam, one risk-level-based generation gate, aligned prompt guardrails, and focused low/medium/high regression coverage.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-105-refinar-career-fit-de-warning-booleano-para-avaliacao-gradua/105-CONTEXT.md
@.planning/phases/CURRIA-105-refinar-career-fit-de-warning-booleano-para-avaliacao-gradua/105-RESEARCH.md
@src/types/agent.ts
@src/lib/agent/profile-review.ts
@src/lib/agent/job-targeting-pipeline.ts
@src/lib/agent/tools/index.ts
@src/lib/agent/agent-loop.ts
@src/lib/agent/context/sources/build-source-context.ts
@src/lib/agent/profile-review.test.ts
@src/lib/agent/streaming-loop.test.ts
@src/lib/agent/context-builder.test.ts

<scope_rules>
- Keep the migration additive; do not remove `targetFitAssessment`, `CareerFitCheckpoint`, or existing warning metadata fields.
- Preserve brownfield product shape; no dashboard UI redesign is required in this phase.
- Keep `cvState` as canonical truth and `agentState` as operational context only.
- Use structured logs through existing observability helpers; do not invent a parallel logger contract.
- Avoid broad rewrites of agent-loop flow beyond the career-fit gating seam.
</scope_rules>

<decision_trace>
- D-01: Centralize risk scoring in `profile-review.ts` through `evaluateCareerFitRisk(...)`.
- D-02: Model role-family distance explicitly and symmetrically, with `unknown` treated as non-penalizing.
- D-03: Persist `careerFitEvaluation` anywhere targeting signals are refreshed.
- D-04: Separate warning display (`medium` and `high`) from override requirement (`high` only).
- D-05: Keep compatibility wrappers so existing consumers remain valid during migration.
- D-06: Align prompt/context guardrails and committed tests with the new risk-level semantics.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Introduce the additive career-fit domain model and centralized evaluation logic</name>
  <files>src/types/agent.ts, src/lib/agent/profile-review.ts, src/lib/agent/profile-review.test.ts</files>
  <behavior>
    - Test 1: an adjacent BI/data-style scenario with partial fit lands as `medium` and does not require explicit confirmation.
    - Test 2: a distant scenario with weak signals and seniority mismatch lands as `high` and requires explicit confirmation.
    - Test 3: a clearly aligned scenario lands as `low`.
    - Test 4: compatibility wrappers keep returning warning behavior for medium/high while checkpoint/warning text remain available for downstream consumers.
  </behavior>
  <action>Add `CareerFitRiskLevel` and `CareerFitEvaluation` to `src/types/agent.ts` plus `careerFitEvaluation?: CareerFitEvaluation` under `AgentState`. In `src/lib/agent/profile-review.ts`, keep `inferRoleFamily(...)` intact, add an explicit symmetric `roleFamilyDistance(...)` model with `unknown` fallback, and centralize the full risk-point calculation in `evaluateCareerFitRisk(...)`. Update warning-copy and checkpoint helpers to consume the evaluation without deleting current wrappers. Extend `profile-review.test.ts` with focused low/medium/high coverage that matches the requested thresholds and adjacent-role safeguards.</action>
  <verify>
    <automated>npx vitest run "src/lib/agent/profile-review.test.ts"</automated>
  </verify>
  <done>The repo has one authoritative career-fit evaluation helper with additive types and focused domain proof for low/medium/high behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Persist the evaluation through targeting seams and update prompt guardrails</name>
  <files>src/lib/agent/job-targeting-pipeline.ts, src/lib/agent/tools/index.ts, src/lib/agent/context/sources/build-source-context.ts, src/lib/agent/context-builder.test.ts</files>
  <behavior>
    - Test 1: job-targeting state can carry both `targetFitAssessment` and `careerFitEvaluation`.
    - Test 2: `analyze_gap` persists `careerFitEvaluation` alongside current targeting fields.
    - Test 3: the prompt snapshot includes the current risk level and the medium/high behavior rule when an evaluation exists.
  </behavior>
  <action>After the current target-fit derivation points in `job-targeting-pipeline.ts` and `tools/index.ts`, compute `careerFitEvaluation` from the same session context, persist it in `agentState`, and emit a structured calibration log through the repo's existing observability helpers. Update `buildCareerFitPromptSnapshot(...)` / `build-source-context.ts` so the guardrail block reflects `low | medium | high`, requires explicit confirmation only for `high`, and warns-but-continues conservatively for `medium`. Extend `context-builder.test.ts` to assert the prompt contains the current risk level and the new behavioral guidance.</action>
  <verify>
    <automated>npx vitest run "src/lib/agent/context-builder.test.ts"</automated>
  </verify>
  <done>Targeting persistence and prompt context now share the same graduated career-fit state instead of the old boolean-only guardrail.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Replace boolean loop gating with risk-level warning and override behavior</name>
  <files>src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts</files>
  <behavior>
    - Test 1: medium-risk generation warns but does not require stored override confirmation to proceed on the next generation turn.
    - Test 2: high-risk generation still requires explicit confirmation before generation.
    - Test 3: phase metadata records `careerFitRiskLevelAtWarning` and only sets `careerFitOverrideConfirmedAt` for high-risk confirmation.
  </behavior>
  <action>Refactor `agent-loop.ts` so warning display is driven by `agentState.careerFitEvaluation.riskLevel` instead of the previous boolean-first logic. Medium and high risk should both surface warning copy, but only `high` may set or require explicit override metadata before generation continues. Extend `phaseMeta` additively with the risk-level-at-warning seam, preserve existing target-job-description scoping, and keep the rest of the confirmed-generation flow intact. Update `streaming-loop.test.ts` with focused medium-versus-high regression proof.</action>
  <verify>
    <automated>npx vitest run "src/lib/agent/streaming-loop.test.ts"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The generation loop now distinguishes warning-only medium risk from confirmation-required high risk without breaking brownfield targeting flow.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/lib/agent/profile-review.test.ts"`
- `npx vitest run "src/lib/agent/context-builder.test.ts"`
- `npx vitest run "src/lib/agent/streaming-loop.test.ts"`
- `npm run typecheck`
</verification>

<success_criteria>
- `careerFitEvaluation` is persisted additively and becomes the primary career-fit decision surface.
- Adjacent-role scenarios can land as medium risk instead of hard-blocking like distant mismatches.
- High-risk mismatches still require explicit confirmation before generation.
- Prompt guardrails, loop behavior, and focused tests all reflect the same low/medium/high model.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-105-refinar-career-fit-de-warning-booleano-para-avaliacao-gradua/105-01-SUMMARY.md`
</output>
