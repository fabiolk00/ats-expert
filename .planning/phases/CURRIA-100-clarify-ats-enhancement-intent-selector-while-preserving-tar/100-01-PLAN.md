---
phase: 100-clarify-ats-enhancement-intent-selector-while-preserving-tar
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/resume/user-data-page.tsx
  - src/components/resume/user-data-page.test.tsx
  - tests/e2e/profile-setup.spec.ts
  - .planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-VALIDATION.md
autonomous: true
requirements:
  - ATS-INTENT-UI-01
  - ATS-INTENT-GUARD-01
  - ATS-INTENT-TEST-01
user_setup: []
must_haves:
  truths:
    - "The enhancement panel makes ATS improvement the default visible intent without showing an empty vacancy textarea."
    - "The user can explicitly switch to target-job adaptation, see matching copy before typing, and only submit that path with a non-empty job description."
    - "Switching back to ATS clears any draft target-job text, while submit behavior still follows the existing `generationMode` and endpoint contract."
    - "Existing dialogs, loading states, toasts, credits messaging, and compare-page handoff remain intact."
  artifacts:
    - path: "src/components/resume/user-data-page.tsx"
      provides: "UI-only enhancement intent state, display-mode split, local empty-target guard, and refreshed enhancement-panel layout over the existing handler"
      contains: "EnhancementIntent"
    - path: "src/components/resume/user-data-page.test.tsx"
      provides: "Focused component coverage for default ATS, explicit target-job mode, local validation, and preserved ATS/job-targeting behavior"
      contains: "Melhorar para ATS"
    - path: "tests/e2e/profile-setup.spec.ts"
      provides: "Browser proof for the explicit selector, empty-target guard, and preserved submit semantics in the setup funnel"
      contains: "target job"
    - path: ".planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-VALIDATION.md"
      provides: "Phase note recording the preserved handler/endpoint contract and the commands run"
      contains: "handleSetupGeneration preserved"
  key_links:
    - from: "src/components/resume/user-data-page.tsx"
      to: "src/components/resume/user-data-page.tsx"
      via: "Visible intent must be separated from backend behavior so selector changes do not silently reroute generation"
      pattern: "(EnhancementIntent|displayMode|generationMode|handleSetupGeneration)"
    - from: "src/components/resume/user-data-page.test.tsx"
      to: "src/components/resume/user-data-page.tsx"
      via: "Component tests must prove the local empty-target guard fires before the shared handler and endpoints"
      pattern: "(ats-panel-cta|target-job-description-input|/api/profile/ats-enhancement|/api/profile/smart-generation)"
    - from: "tests/e2e/profile-setup.spec.ts"
      to: "src/components/resume/user-data-page.tsx"
      via: "Browser tests keep the provider-free setup harness but update only the enhancement-panel interaction steps"
      pattern: "(target-job-description-input|aria-pressed|Melhorar para ATS|Adaptar para esta vaga)"
---

<objective>
Clarify the enhancement panel by separating visible user intent from the existing generation behavior seam, so ATS improvement versus target-job adaptation is explicit before submit without changing backend contracts.

Purpose: Improve product clarity on the resume setup screen while preserving the current handlers, endpoints, dialogs, billing behavior, loading flow, and compare-page redirect.
Output: Updated enhancement-panel UI in `user-data-page.tsx`, focused component/browser regressions, and a concise Phase 100 validation note.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-CONTEXT.md
@.planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-RESEARCH.md
@src/components/resume/user-data-page.tsx
@src/components/resume/user-data-page.test.tsx
@tests/e2e/profile-setup.spec.ts

<scope_rules>
- Keep the phase inside the enhancement panel on the resume setup screen only.
- Preserve `handleSetupGeneration()` and the current endpoint split between `/api/profile/ats-enhancement` and `/api/profile/smart-generation`.
- Do not change billing logic, ATS readiness rules, rewrite validation rules, route-after-success semantics, or profile-editor behavior.
- Keep the work local to the current brownfield component shape: editor page and enhancement panel remain in `src/components/resume/user-data-page.tsx`.
- Update only the enhancement-focused unit and browser tests; do not churn import/save/editor test coverage.
</scope_rules>

<decision_trace>
- D-01: Reuse the existing enhancement logic in `user-data-page.tsx`; do not create a parallel generation flow.
- D-02: Add `type EnhancementIntent = "ats" | "target_job"` as UI-only state, defaulting to `"ats"`.
- D-03: Keep `generationMode` as the behavior source of truth for handler, endpoint, loading, and server-facing semantics.
- D-04: Use accessible `button` elements with `aria-pressed`; ATS hides the large textarea, target-job shows it prominently.
- D-05: Visible badge/title/helper/features/CTA/top-bar copy must follow the selected intent.
- D-06: Selecting ATS clears `targetJobDescription`; typing into the textarea keeps or switches the UI intent to target-job.
- D-07: Empty target-job submit must be blocked locally before `handleSetupGeneration()` runs.
- D-08: Preserve dialogs, toasts, loading/disabled states, credits behavior, compare redirect, and focused test scope.
</decision_trace>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add an explicit UI intent selector without changing generation behavior</name>
  <files>src/components/resume/user-data-page.tsx, src/components/resume/user-data-page.test.tsx</files>
  <behavior>
    - Default render shows ATS selected, hides the large vacancy textarea, and renders `Melhorar para ATS (1 crédito)`.
    - Selecting target-job reveals the textarea and matching helper/CTA before any text is entered.
    - Typing into the textarea keeps target-job selected; selecting ATS clears the textarea and any local validation message.
    - Clicking the CTA in target-job mode with an empty description shows a local validation message and does not invoke `handleSetupGeneration()`.
  </behavior>
  <action>Inside the existing enhancement-panel branch of `user-data-page.tsx`, add UI-local `EnhancementIntent` and empty-target validation state plus a separate `displayMode` for visible badge/title/helper/features/CTA/header copy. Keep `generationMode` derived only from `targetJobDescription.trim()` and leave `handleSetupGeneration()` unchanged for ATS blocking checks, persistence, endpoint choice, dialogs, toasts, loading overlay, and compare redirect. The visible selector UI must follow `displayMode`, while missing-items modal copy, failure fallback copy, rewrite-validation fallback behavior, and loading-overlay type remain behavior-driven by `generationMode`. Empty target-job validation must happen on CTA click before `handleSetupGeneration()` runs, while the CTA remains enabled whenever the existing busy/credit gate allows it. Rework only the enhancement-panel subtree in this file: accessible selector buttons with `aria-pressed`, ATS explanation card when ATS is selected, prominent textarea when target-job is selected, stronger but mostly-white value-panel styling with emerald accents in the value bullets/highlights, and a panel header/top bar that surfaces the selected mode plus available credits while preserving the current navigation behavior. Preserve existing `data-testid` hooks and dashboard-guide targeting on the CTA. Update the focused component tests in the same task to lock the new default ATS view, explicit target-job selection, local empty-target guard, ATS-clears-textarea behavior, behavior-driven modal/loading copy, and preserved endpoint/compare-route assertions.</action>
  <verify>
    <automated>npx vitest run "src/components/resume/user-data-page.test.tsx"</automated>
  </verify>
  <done>The enhancement panel has an explicit UI intent selector, ATS is the default visible mode, target-job is explicit before typing, local empty-target validation blocks the shared handler, and focused component tests prove the existing ATS/job-targeting handler semantics remain intact.</done>
</task>

<task type="auto">
  <name>Task 2: Update browser regression proof around the explicit selector only</name>
  <files>tests/e2e/profile-setup.spec.ts</files>
  <action>Touch only the enhancement-panel scenarios in the profile-setup Playwright spec. ATS happy-path should run through the default ATS selection with no textarea prerequisite and explicitly prove the ATS request hits `/api/profile/ats-enhancement`. Target-job happy-path must click the new target-job selector before filling `target-job-description-input` and explicitly prove the target-job request hits `/api/profile/smart-generation`. Success assertions must follow the preserved compare-page handoff from `user-data-page.tsx`, not a generic dashboard/session landing. Add one smoke test for empty target-job submit that asserts the local validation message appears, the page stays on the setup screen, and no generation transition happens. Preserve the existing mocked-provider harness, auth bootstrap, payload captures, credit gating, and focused scope.</action>
  <verify>
    <automated>npx playwright test "tests/e2e/profile-setup.spec.ts" --project=chromium</automated>
  </verify>
  <done>The browser suite proves default ATS behavior, explicit target-job selection, empty-target local validation, and preserved submit semantics without adding unrelated churn to the profile-setup spec.</done>
</task>

<task type="auto">
  <name>Task 3: Record the preserved UI-to-handler contract in Phase 100 validation notes</name>
  <files>.planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-VALIDATION.md</files>
  <action>Write a short validation note capturing the final contract: ATS intent is explicit by default, target-job intent is explicit before typing, empty target-job submit is blocked locally, `handleSetupGeneration()` remains the single execution path, `/api/profile/ats-enhancement` and `/api/profile/smart-generation` remain the only endpoints, and dialogs/toasts/loading/compare redirect are preserved. Include the exact automated commands run.</action>
  <verify>
    <automated>rg -n "(ATS intent|target-job|handleSetupGeneration preserved|/api/profile/ats-enhancement|/api/profile/smart-generation|compare)" ".planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-VALIDATION.md"</automated>
  </verify>
  <done>The phase directory contains a concise validation artifact that documents the UI-only selector contract and proves backend behavior was preserved.</done>
</task>

</tasks>

<verification>
- `npx vitest run "src/components/resume/user-data-page.test.tsx"`
- `npx playwright test "tests/e2e/profile-setup.spec.ts" --project=chromium`
- `npm run typecheck`
</verification>

<success_criteria>
- ATS intent is explicit and selected by default, with no large empty target-job textarea visible in ATS mode.
- Target-job intent is explicit before typing, shows matching copy and textarea, and blocks empty submit locally.
- `handleSetupGeneration()` remains the only execution path, endpoint selection still comes from `generationMode`, and compare-page handoff plus dialogs/toasts/loading remain unchanged.
- Focused unit and browser regressions prove the selector contract without expanding scope into the rest of the profile setup page.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-100-clarify-ats-enhancement-intent-selector-while-preserving-tar/100-01-SUMMARY.md`
</output>
