---
phase: 55-brownfield-route-consolidation-and-repo-topology-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/api/session/[id]/compare/route.ts
  - src/app/api/session/[id]/comparison/route.ts
  - src/app/api/session/[id]/comparison/route.test.ts
  - src/lib/routes/session-comparison/context.ts
  - src/lib/routes/session-comparison/decision.ts
  - src/lib/routes/session-comparison/decision.test.ts
  - src/lib/routes/session-comparison/response.ts
  - src/lib/routes/session-comparison/response.test.ts
  - src/lib/routes/session-comparison/types.ts
autonomous: true
requirements: [ROUTE-CONS-01, ROUTE-CONS-TEST-01]
must_haves:
  truths:
    - "Future compare semantics have one canonical home: POST /api/session/[id]/compare."
    - "GET /api/session/[id]/comparison keeps its current dashboard contract, statuses, and preview-aware behavior after extraction."
    - "Locked preview, not-found, unauthorized, no-optimized, and internal-error outcomes stay behaviorally unchanged."
  artifacts:
    - path: "src/lib/routes/session-comparison/types.ts"
      provides: "Typed context and normalized decision contract for dashboard comparison."
    - path: "src/lib/routes/session-comparison/context.ts"
      provides: "Auth, ownership, and session-load boundary for the compatibility comparison route."
    - path: "src/lib/routes/session-comparison/decision.ts"
      provides: "Optimized-state sanitization, generation-type selection, scoring, and normalized outcomes."
    - path: "src/lib/routes/session-comparison/response.ts"
      provides: "HTTP mapping for comparison decisions without reinterpreting route semantics."
    - path: "src/app/api/session/[id]/comparison/route.ts"
      provides: "Thin GET adapter over session-comparison modules."
    - path: "src/app/api/session/[id]/comparison/route.test.ts"
      provides: "Route seam proof for unchanged public behavior."
  key_links:
    - from: "src/app/api/session/[id]/comparison/route.ts"
      to: "src/lib/routes/session-comparison/context.ts"
      via: "resolveSessionComparisonContext"
      pattern: "resolveSessionComparisonContext"
    - from: "src/app/api/session/[id]/comparison/route.ts"
      to: "src/lib/routes/session-comparison/decision.ts"
      via: "decideSessionComparison"
      pattern: "decideSessionComparison"
    - from: "src/app/api/session/[id]/comparison/route.ts"
      to: "src/lib/routes/session-comparison/response.ts"
      via: "toSessionComparisonResponse"
      pattern: "toSessionComparisonResponse"
---

<objective>
Canonicalize compare ownership and extract the brownfield dashboard comparison route into the established route-layer pattern without changing product behavior.

Purpose: Remove the last compare/comparison ambiguity in runtime code while preserving the current GET and POST surfaces.
Output: Thin `comparison` route modules, canonical-versus-compatibility route comments, and regression proof for unchanged compare behavior.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-55-brownfield-route-consolidation-and-repo-topology-alignment/55-CONTEXT.md
@.planning/phases/CURRIA-55-brownfield-route-consolidation-and-repo-topology-alignment/55-RESEARCH.md
@docs/architecture/route-policy-boundaries.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-03-SUMMARY.md
@.planning/phases/CURRIA-49-hardening-the-route-decision-architecture/49-01-SUMMARY.md

<interfaces>
From src/app/api/session/[id]/compare/route.ts:
```typescript
export async function POST(
  req: NextRequest,
  { params }: { params: { id: string } },
): Promise<NextResponse>
```

From src/lib/dashboard/workspace-client.ts:
```typescript
export async function getResumeComparison(sessionId: string): Promise<ResumeComparisonResponse> {
  return requestJson<ResumeComparisonResponse>(`/api/session/${sessionId}/comparison`)
}
```

From src/types/dashboard.ts:
```typescript
export type ResumeComparisonResponse = {
  sessionId: string
  workflowMode?: WorkflowMode
  generationType: ResumeGenerationType
  targetJobDescription?: string
  originalCvState: CVState
  optimizedCvState: CVState
  previewLock?: PreviewLockSummary
  optimizationSummary?: {
    changedSections: Array<'summary' | 'experience' | 'skills' | 'education' | 'certifications'>
    notes: string[]
    keywordCoverageImprovement?: string[]
  }
  originalScore: {
    total: number
    label: string
  }
  optimizedScore: {
    total: number
    label: string
  }
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Freeze compare ownership and define comparison route contracts</name>
  <files>src/app/api/session/[id]/compare/route.ts, src/app/api/session/[id]/comparison/route.ts, src/lib/routes/session-comparison/types.ts, src/lib/routes/session-comparison/context.ts</files>
  <action>Add concise top-of-file comments to both route handlers stating that `POST /compare` is the canonical compare seam and `GET /comparison` is the dashboard compatibility surface, per the Phase 55 ownership decision. Create `src/lib/routes/session-comparison/types.ts` and `context.ts` so the compatibility route has an explicit typed boundary matching the winning route pattern. Keep `context.ts` limited to auth resolution, ownership enforcement via `getSession(params.id, appUser.id)`, and session loading; do not move optimized-resume availability, preview-lock handling, ATS scoring, or HTTP mapping into this layer. Keep the public GET contract unchanged and do not repoint any caller to `/compare`.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/comparison/route.test.ts"</automated>
  </verify>
  <done>`compare` and `comparison` declare canonical versus compatibility status in code comments, and the new `session-comparison` boundary types/context exist without changing the GET route contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extract comparison semantics into decision and response seams with regression proof</name>
  <files>src/app/api/session/[id]/comparison/route.ts, src/app/api/session/[id]/comparison/route.test.ts, src/lib/routes/session-comparison/decision.ts, src/lib/routes/session-comparison/decision.test.ts, src/lib/routes/session-comparison/response.ts, src/lib/routes/session-comparison/response.test.ts</files>
  <behavior>
    - Test 1: unauthenticated comparison requests still return `401 { error: 'Unauthorized' }`.
    - Test 2: missing or foreign sessions still return `404 { error: 'Not found' }`.
    - Test 3: sessions without an optimized resume still return `409 { error: 'No optimized resume found for this session.' }`.
    - Test 4: locked preview still returns sanitized optimized content plus the existing preview-lock summary.
    - Test 5: ATS and job-targeting scoring paths keep the same labels, payload shape, and fallback behavior.
    - Test 6: unexpected scoring/build failures still return `500 { error: 'Internal server error' }`.
  </behavior>
  <action>Move the brownfield `GET /comparison` route logic into `src/lib/routes/session-comparison/decision.ts` and `response.ts`, then rewrite the route handler as a thin adapter that calls `resolveSessionComparisonContext`, `decideSessionComparison`, and `toSessionComparisonResponse`. Reuse the existing helpers for locked-preview sanitization and score fallback; preserve helper call order so real optimized content never leaks when preview access is locked. Extend the route test for `401`, `404`, `409`, locked-preview, and `500` branches, and add focused decision/response tests so the extracted seam is proven independently. Do not add a `policy.ts` unless a real blocking gate appears during extraction, and do not merge dashboard comparison semantics into `session-compare` because `comparison` remains a compatibility-only GET contract.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/compare/route.test.ts" "src/app/api/session/[id]/comparison/route.test.ts" "src/lib/routes/session-comparison/decision.test.ts" "src/lib/routes/session-comparison/response.test.ts" "src/app/api/preview-lock-transverse.test.ts"</automated>
  </verify>
  <done>`comparison/route.ts` is a thin adapter over route-local modules, regression tests prove unchanged public behavior, and `compare` remains the canonical compare seam while `comparison` stays compatibility-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser/client -> `GET /api/session/[id]/comparison` | Untrusted session ID input crosses into authenticated dashboard data access |
| route adapter -> decision layer | Normalized request context crosses into preview-aware scoring and optimized-state selection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-01 | E | `src/lib/routes/session-comparison/context.ts` | mitigate | Require `getCurrentAppUser()` and session ownership via `getSession(params.id, appUser.id)` before any comparison data is resolved |
| T-55-02 | I | `src/lib/routes/session-comparison/decision.ts` | mitigate | Reuse `sanitizeGeneratedCvStateForClient()` and `getPreviewLockSummary()` before building optimized resume text or returning optimized CV data |
| T-55-03 | T | `src/lib/routes/session-comparison/response.ts` | mitigate | Keep response mapping limited to normalized decision unions so HTTP mapping cannot reinterpret preview or availability semantics ad hoc |
</threat_model>

<verification>
Run the focused comparison lane:

`npx vitest run "src/app/api/session/[id]/compare/route.test.ts" "src/app/api/session/[id]/comparison/route.test.ts" "src/lib/routes/session-comparison/decision.test.ts" "src/lib/routes/session-comparison/response.test.ts" "src/app/api/preview-lock-transverse.test.ts"`
</verification>

<success_criteria>
`POST /compare` remains the canonical compare seam, `GET /comparison` becomes a thin compatibility adapter over route-local modules, and regression coverage proves no public behavior drift across auth, not-found, no-optimized, locked-preview, and internal-error outcomes.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-55-brownfield-route-consolidation-and-repo-topology-alignment/55-01-SUMMARY.md`
</output>
