---
phase: 55-brownfield-route-consolidation-and-repo-topology-alignment
plan: 02
type: execute
wave: 2
depends_on: [55-01]
files_modified:
  - README.md
  - docs/architecture/route-policy-boundaries.md
  - docs/architecture/components-boundaries.md
  - docs/architecture/architecture-scorecard.md
  - docs/architecture/hotspot-watchlist.md
  - docs/architecture/approved-chokepoints.md
autonomous: true
requirements: [ROUTE-CONS-DOC-01]
must_haves:
  truths:
    - "A contributor can quickly tell that `compare` is canonical and `comparison` is compatibility-only."
    - "Top-level onboarding reflects the current repo topology and points readers to the architecture docs instead of duplicating them."
    - "Component promotion rules are explicit enough to guide future brownfield work without triggering a mass move."
    - "Governance artifacts acknowledge the compare/comparison consolidation and do not expand chokepoints unless the code truly requires it."
  artifacts:
    - path: "README.md"
      provides: "Current high-signal repo topology and architecture entry points."
    - path: "docs/architecture/components-boundaries.md"
      provides: "Feature-local versus shared component placement policy."
    - path: "docs/architecture/route-policy-boundaries.md"
      provides: "Updated compare/comparison ownership guidance inside the route-boundary contract."
    - path: "docs/architecture/architecture-scorecard.md"
      provides: "Updated route-thinness and hotspot status after consolidation."
    - path: "docs/architecture/hotspot-watchlist.md"
      provides: "Explicit comparison hotspot status if it remains monitored, otherwise the removal rationale."
    - path: "docs/architecture/approved-chokepoints.md"
      provides: "Canonical chokepoint list that only changes if the extracted comparison seam deserves approved-chokepoint status."
  key_links:
    - from: "README.md"
      to: "docs/architecture/route-policy-boundaries.md"
      via: "architecture section"
      pattern: "route-policy-boundaries"
    - from: "docs/architecture/route-policy-boundaries.md"
      to: "src/app/api/session/[id]/compare/route.ts"
      via: "canonical ownership note"
      pattern: "compare"
    - from: "docs/architecture/components-boundaries.md"
      to: "src/components/"
      via: "placement rules"
      pattern: "feature-local|shared"
---

<objective>
Align README, topology docs, and route governance artifacts with the consolidated compare/comparison architecture and add a pragmatic `components/` boundary policy.

Purpose: Make the repo narrative match the running code so future contributors do not recreate the ambiguity Phase 55 removes.
Output: Updated README, architecture boundary docs, component-placement guidance, and governance artifacts that reflect the compare/comparison decision.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-55-brownfield-route-consolidation-and-repo-topology-alignment/55-CONTEXT.md
@.planning/phases/CURRIA-55-brownfield-route-consolidation-and-repo-topology-alignment/55-RESEARCH.md
@README.md
@docs/architecture/route-policy-boundaries.md
@docs/architecture/architecture-scorecard.md
@docs/architecture/hotspot-watchlist.md
@docs/architecture/approved-chokepoints.md
@.planning/phases/CURRIA-52-architecture-governance-automation/52-01-SUMMARY.md
@.planning/phases/CURRIA-54-architecture-proof-pack/54-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refresh onboarding and component-boundary guidance for the current topology</name>
  <files>README.md, docs/architecture/components-boundaries.md, docs/architecture/route-policy-boundaries.md</files>
  <action>Update the top-level README so it reflects the current repo topology at a high signal level, points readers to the canonical architecture docs, and names the `src/lib/routes/*` route-layer pattern without turning the README into a deep architecture document. Add `docs/architecture/components-boundaries.md` to define when UI stays feature-local, when it can move to shared `src/components/ui` or other shared folders, and which anti-patterns should be avoided in this brownfield monolith. Update `docs/architecture/route-policy-boundaries.md` to state that `POST /compare` is the canonical compare seam and `GET /comparison` is a compatibility/dashboard surface with its own thin route-layer extraction. Preserve the existing guidance that the full pattern is for semantically dense routes only.</action>
  <verify>
    <automated>npm run audit:route-architecture</automated>
  </verify>
  <done>README and architecture docs make the current topology legible, and contributors can identify compare ownership and component-placement rules without reading planning artifacts.</done>
</task>

<task type="auto">
  <name>Task 2: Update governance artifacts to reflect the consolidation outcome</name>
  <files>docs/architecture/architecture-scorecard.md, docs/architecture/hotspot-watchlist.md, docs/architecture/approved-chokepoints.md</files>
  <action>Update the architecture scorecard to reflect the compare/comparison ambiguity reduction and the current hotspot count after extraction. Update `hotspot-watchlist.md` only if `comparison` remains a monitored hotspot or if an ambiguity note should be removed because the brownfield route is now aligned. Update `approved-chokepoints.md` only if the extracted `session-comparison/decision.ts` genuinely becomes an approved route decision chokepoint; otherwise record the canonical ownership in the docs without expanding the chokepoint list. Keep the governance change set minimal and evidence-based, consistent with the proof-pack and audit automation established in Phases 52 and 54.</action>
  <verify>
    <automated>npm run audit:route-architecture &amp;&amp; npx vitest run "src/app/api/session/[id]/compare/route.test.ts" "src/app/api/session/[id]/comparison/route.test.ts"</automated>
  </verify>
  <done>Governance docs acknowledge the consolidation, hotspot and chokepoint guidance match the real codebase, and no new route-governance ambiguity remains around compare versus comparison.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| architecture docs -> future code reviews | Governance text influences which route changes reviewers allow or reject |
| README onboarding -> contributor implementation choices | Repo topology guidance shapes where future code is added |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-55-04 | T | `docs/architecture/route-policy-boundaries.md` | mitigate | State canonical compare ownership explicitly so future route edits do not drift back into duplicate semantics |
| T-55-05 | R | `docs/architecture/approved-chokepoints.md` | mitigate | Only add `session-comparison/decision.ts` if the implemented extraction makes it a true monitored chokepoint; otherwise leave the approved set unchanged and document why |
| T-55-06 | I | `README.md` | accept | README only exposes repo topology and doc entry points, not secrets or privileged runtime details |
</threat_model>

<verification>
Run:

`npm run audit:route-architecture`

and then:

`npx vitest run "src/app/api/session/[id]/compare/route.test.ts" "src/app/api/session/[id]/comparison/route.test.ts"`
</verification>

<success_criteria>
README, route-boundary docs, component-placement policy, and governance artifacts all reflect the canonical `compare` versus compatibility `comparison` decision, and the route-architecture audit remains green.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-55-brownfield-route-consolidation-and-repo-topology-alignment/55-02-SUMMARY.md`
</output>
