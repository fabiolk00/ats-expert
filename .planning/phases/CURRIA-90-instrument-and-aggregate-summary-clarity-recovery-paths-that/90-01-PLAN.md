---
phase: 90-instrument-and-aggregate-summary-clarity-recovery-paths-that-fall-back-to-estimated-range
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/ats-enhancement-pipeline.ts
  - src/lib/ats/scoring/observability.ts
  - src/lib/ats/scoring/quality-gates.ts
  - src/lib/ats/scoring/types.ts
  - src/lib/ats/scoring/observability.test.ts
  - src/lib/agent/tools/pipeline.test.ts
  - .planning/phases/CURRIA-90-instrument-and-aggregate-summary-clarity-recovery-paths-that/90-VALIDATION.md
autonomous: true
requirements:
  - ATS-SUMMARY-CLARITY-OBS-01
  - ATS-SUMMARY-CLARITY-CONVERGENCE-01
  - ATS-SUMMARY-CLARITY-TEST-01
user_setup: []
must_haves:
  truths:
    - "A single self-contained structured event closes the loop between summary recovery semantics, summary clarity gating, and the final ATS readiness outcome."
    - "The event is emitted only after `scoreStatus`, confidence, and withhold reasons are known."
    - "No summary rewrite behavior, smart-repair behavior, summary clarity threshold, or ATS scoring policy changes in this phase."
  artifacts:
    - path: "src/lib/ats/scoring/observability.ts"
      provides: "A builder/recorder for the ATS summary clarity outcome event with explicit derived booleans"
      contains: "summaryRepairThenClarityFail"
    - path: "src/lib/agent/ats-enhancement-pipeline.ts"
      provides: "A single emit point at the latest safe convergence point in the ATS enhancement pipeline"
      contains: "agent.ats_enhancement.summary_clarity_outcome"
    - path: "src/lib/ats/scoring/observability.test.ts"
      provides: "Unit coverage for healthy, recovered, failed-clarity, and non-smart-repair payload assembly"
      contains: "summaryValidationRecovered"
    - path: "src/lib/agent/tools/pipeline.test.ts"
      provides: "Logging assertions that the new event is emitted once per ATS enhancement session"
      contains: "summary_clarity_outcome"
  key_links:
    - from: "src/lib/agent/ats-enhancement-pipeline.ts"
      to: "src/lib/ats/scoring/observability.ts"
      via: "latest safe convergence point after ATS readiness contract creation"
      pattern: "recordAtsSummaryClarityOutcome"
    - from: "src/lib/ats/scoring/quality-gates.ts"
      to: "src/lib/ats/scoring/observability.ts"
      via: "stable summary-clarity withhold reason constant"
      pattern: "ATS_SUMMARY_CLARITY_WITHHOLD_REASON"
---

<objective>
Instrument ATS enhancement sessions with a single summary clarity outcome event that makes recovery-path-to-estimated-range behavior directly queryable from logs, while keeping product behavior unchanged.

Output: structured observability, focused tests, and a validation note that documents the convergence point and confirms no behavior regression.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/CURRIA-90-instrument-and-aggregate-summary-clarity-recovery-paths-that/90-CONTEXT.md
@.planning/phases/CURRIA-90-instrument-and-aggregate-summary-clarity-recovery-paths-that/90-RESEARCH.md
@src/lib/agent/ats-enhancement-pipeline.ts
@src/lib/ats/scoring/index.ts
@src/lib/ats/scoring/observability.ts
@src/lib/ats/scoring/quality-gates.ts
@src/lib/ats/scoring/types.ts
@src/lib/agent/tools/pipeline.test.ts
@src/lib/ats/scoring/observability.test.ts
@src/lib/ats/scoring/index.test.ts

<scope_rules>
- Do not change summary rewrite prompts or recovery behavior.
- Do not change quality-gate thresholds or ATS scoring policy.
- Do not require joins with later comparison/render events to answer the core question.
- Keep the event shape queryable through explicit booleans rather than free-text reconstruction.
</scope_rules>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Add a self-contained ATS summary clarity outcome log builder</name>
  <files>src/lib/ats/scoring/observability.ts src/lib/ats/scoring/quality-gates.ts src/lib/ats/scoring/types.ts</files>
  <behavior>
    - The code exposes one builder/recorder for `agent.ats_enhancement.summary_clarity_outcome`.
    - The payload includes stable final-outcome fields plus explicit booleans for the summary recovery path.
    - `summaryRepairThenClarityFail` remains semantically strict to smart-repair-based summary recoveries only.
  </behavior>
  <action>Add a narrow typed payload builder in ATS scoring observability. Reuse canonical ATS readiness fields, normalize the summary-clarity withhold reason into a stable constant, and keep the payload intentionally queryable with booleans such as `summaryValidationRecovered`, `summaryRecoveryWasSmartRepair`, `summaryClarityGateFailed`, `summaryRepairThenClarityFail`, `estimatedRangeOutcome`, and `usedExactScore`.</action>
  <verify>
    <automated>npx vitest run "src/lib/ats/scoring/observability.test.ts"</automated>
  </verify>
  <done>The new event payload can distinguish healthy sessions, recovered sessions, smart-repair failures, and non-smart-repair recovery paths without changing ATS behavior.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Emit the event once at the latest safe convergence point in the ATS enhancement pipeline</name>
  <files>src/lib/agent/ats-enhancement-pipeline.ts</files>
  <behavior>
    - The event is emitted only after the final ATS readiness contract is known.
    - The pipeline contributes summary-specific recovery semantics that ATS scoring does not persist on its own.
    - The event is emitted once per ATS enhancement session and does not require later joins.
  </behavior>
  <action>Move ATS readiness contract construction into a local variable, derive summary-specific recovery semantics in the pipeline, and emit the new event immediately after the contract is built and before final persistence/return logic. Keep the emit local and low-coupling.</action>
  <verify>
    <automated>npx vitest run "src/lib/agent/tools/pipeline.test.ts"</automated>
  </verify>
  <done>The latest safe convergence point is instrumented, and the session-level event is emitted once with final score fields already resolved.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Prove healthy and problematic paths with focused tests and validation notes</name>
  <files>src/lib/ats/scoring/observability.test.ts src/lib/agent/tools/pipeline.test.ts .planning/phases/CURRIA-90-instrument-and-aggregate-summary-clarity-recovery-paths-that/90-VALIDATION.md</files>
  <behavior>
    - Tests cover healthy no-recovery, smart-repair recovery pass, smart-repair recovery fail to estimated_range, summary clarity fail without recovery, non-summary recovery, and non-smart-repair summary recovery.
    - Validation notes explain where the emit point lives and why that seam owns the complete payload.
  </behavior>
  <action>Add focused builder tests and narrow pipeline logging assertions, then document in `90-VALIDATION.md` why the chosen emit point is the latest safe convergence point and confirm that the phase only added observability.</action>
  <verify>
    <automated>npx vitest run "src/lib/ats/scoring/index.test.ts"</automated>
    <automated>npm run typecheck</automated>
  </verify>
  <done>The observability path is regression-tested and the phase artifacts explicitly document that this was a measurement-only change.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| ATS enhancement pipeline -> ATS readiness contract | Recovery semantics live in the pipeline while final score outcomes live in ATS scoring; the new event must assemble them only at the latest safe convergence point |
| Structured logs -> downstream aggregation | Queryability depends on explicit booleans and stable field names rather than free-text reconstruction |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-90-01 | T | event placement | mitigate | Emit the event only after the ATS readiness contract is built so `scoreStatus`, confidence, and withhold reasons are final. |
| T-90-02 | T | boolean semantics | mitigate | Keep `summaryRepairThenClarityFail` strict to smart-repair-based summary recoveries and test non-smart-repair branches directly. |
| T-90-03 | I | observability drift | mitigate | Reuse existing ATS field names and serialize withhold reasons deterministically so log aggregation stays stable. |
</threat_model>

<verification>
- `npx vitest run "src/lib/ats/scoring/observability.test.ts"`
- `npx vitest run "src/lib/agent/tools/pipeline.test.ts"`
- `npx vitest run "src/lib/ats/scoring/index.test.ts"`
- `npm run typecheck`
</verification>

<success_criteria>
- The new event is self-contained enough to answer the summary recovery -> clarity gate -> score outcome question without later joins.
- Healthy sessions and problematic sessions are both emitted for comparability.
- Non-smart-repair summary recoveries are represented accurately when they occur.
- No rewrite, gate, scoring, or rendering behavior changes land in the phase.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-90-instrument-and-aggregate-summary-clarity-recovery-paths-that/90-01-SUMMARY.md`
</output>
