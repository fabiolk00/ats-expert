---
phase: 37-freeze-async-execution-contracts-and-durable-job-foundations
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - prisma/schema.prisma
  - prisma/migrations/20260416_generic_jobs_foundation.sql
  - src/types/jobs.ts
  - src/types/agent.ts
  - src/lib/jobs/source-of-truth.ts
  - src/lib/jobs/repository.ts
  - src/lib/jobs/contracts.test.ts
  - src/lib/jobs/repository.test.ts
  - src/lib/jobs/source-of-truth.test.ts
  - .planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/**
autonomous: true
requirements:
  - JOB-01
user_setup: []
must_haves:
  truths:
    - "Phase 37 freezes one generic durable `jobs` model before `/api/agent` orchestration and worker execution split in later phases."
    - "Durable job rows are operational state only; `cvState` remains canonical truth, `agentState` remains operational context, and artifact lineage stays traceable to exact snapshot/version refs."
    - "The repo already uses serverless-friendly DB-backed claim/lease state machines via `linkedin_import_jobs` and `pdf_import_jobs`; the new foundation must reuse that pattern instead of introducing BullMQ or another external queue."
    - "The shared lifecycle vocabulary must cover `queued`, `running`, `completed`, `failed`, and reserved `cancelled` states with explicit `type`, `stage`, `progress`, timestamps, and terminal result/error refs."
  artifacts:
    - path: "src/types/jobs.ts"
      provides: "Shared AgentActionType, ExecutionMode, JobType, JobStatus, dispatch payload, and typed ref contracts"
      contains: "export type JobStatus"
    - path: "prisma/schema.prisma"
      provides: "Generic Job model with ownership refs, lifecycle fields, JSON ref columns, and status indexes"
      contains: "model Job"
    - path: "src/lib/jobs/repository.ts"
      provides: "Create/read/claim/reclaim/complete/fail/cancel durable job helpers using Supabase admin access"
      contains: "claimJob"
    - path: "src/lib/jobs/source-of-truth.ts"
      provides: "Explicit source-selection and snapshot-ref helpers for async ATS, targeting, and artifact jobs"
      contains: "resolveEffectiveResumeSource"
    - path: "src/lib/db/resume-generations.ts"
      provides: "Existing immutable artifact snapshot lineage contract that job result refs must point at instead of duplicating"
      contains: "createPendingResumeGeneration"
  key_links:
    - from: "src/types/jobs.ts"
      to: "src/lib/jobs/repository.ts"
      via: "repository reads and persists the frozen lifecycle vocabulary instead of local string literals"
      pattern: "JobStatus"
    - from: "src/lib/jobs/source-of-truth.ts"
      to: "src/lib/agent/tools/index.ts"
      via: "effective resume source and artifact snapshot refs stay aligned with existing optimized-state selection"
      pattern: "getEffectiveBaseCvState"
    - from: "prisma/schema.prisma"
      to: "src/lib/jobs/repository.ts"
      via: "repository fields and indexes match the durable jobs table contract"
      pattern: "model Job"
---

<objective>
Freeze the shared async execution and durable-job foundation contracts that later phases will depend on without moving heavy ATS, targeting, or artifact execution into the background yet.

Purpose: Phase 37 must make the action/job/source-of-truth contract explicit before `/api/agent`, worker execution, polling, and SSE work split across later phases.
Output: One shared async contract module, one generic durable `jobs` persistence model plus repository helpers, a blocking schema-push step, and committed regression proof for contract shape, claim fencing, and source-of-truth selection.
</objective>

<execution_context>
@C:/Users/fabio/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/fabio/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-CONTEXT.md
@.planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-RESEARCH.md
@.planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-VALIDATION.md
@AGENTS.md
@prisma/schema.prisma
@src/types/agent.ts
@src/lib/agent/tools/index.ts
@src/lib/db/session-lifecycle.ts
@src/lib/db/resume-generations.ts
@src/lib/linkedin/import-jobs.ts
@src/lib/profile/pdf-import-jobs.ts
</context>

<threat_model>
<trust_boundaries>
- Future `/api/agent` dispatch decisions -> durable job rows and execution-mode contracts
- Durable job rows -> canonical session, target, and artifact state
- Claimed job ownership -> terminal status writes and stale-job recovery
- Artifact snapshot refs -> preview and generated-file consistency
</trust_boundaries>
<stride_register>
<threat>
  <threat_id>T-37-01</threat_id>
  <category>Elevation of Privilege</category>
  <component>Durable job ownership</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Scope generic jobs by internal app user IDs and keep the contract/ref helpers aligned with the repo's existing ownership-checked job access patterns.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-37-02</threat_id>
  <category>Tampering</category>
  <component>Claim and terminal-write lifecycle</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Reuse optimistic claim transitions plus `claimed_at` fencing so stale or concurrent workers cannot both finalize the same durable job.</mitigation_plan>
</threat>
<threat>
  <threat_id>T-37-03</threat_id>
  <category>Repudiation</category>
  <component>Source snapshot and artifact lineage</component>
  <disposition>mitigate</disposition>
  <mitigation_plan>Keep job rows operational only and point artifact results at immutable snapshot/version references so preview and generated outputs remain traceable to the same source.</mitigation_plan>
</threat>
</stride_register>
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Freeze the shared async action, lifecycle, and source-of-truth contracts</name>
  <files>src/types/jobs.ts, src/types/agent.ts, src/lib/jobs/source-of-truth.ts</files>
  <read_first>src/types/agent.ts, src/lib/agent/tools/index.ts, src/lib/db/resume-generations.ts, .planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-CONTEXT.md, .planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-RESEARCH.md</read_first>
  <action>Create `src/types/jobs.ts` and export these exact unions and contract types: `AgentActionType = 'ats_enhancement' | 'job_targeting' | 'artifact_generation'`, `ExecutionMode = 'sync' | 'async'`, `JobType = 'ats_enhancement' | 'job_targeting' | 'artifact_generation'`, `JobStatus = 'queued' | 'running' | 'completed' | 'failed' | 'cancelled'`, `JobProgress = { percent?: number; label?: string }`, typed `JobInputRef`, `JobResultRef`, and `JobErrorRef` objects, plus a durable dispatch payload that carries `jobId`, `userId`, `sessionId`, optional `resumeTargetId`, `actionType`, `executionMode`, `requestedAt`, and source/result/error refs. Add `src/lib/jobs/source-of-truth.ts` with explicit helpers that encode `cvState` as canonical base truth, `agentState.optimizedCvState ?? cvState` as the effective source snapshot for async ATS/targeting/artifact jobs, and artifact result refs that point to `resumeGenerationId` or exact snapshot metadata instead of signed URLs. Update `src/types/agent.ts` only where needed so existing brownfield agent types can import or re-export the new shared contracts without changing current synchronous business behavior.</action>
  <verify>npm run typecheck && npx vitest run src/lib/jobs/contracts.test.ts src/lib/jobs/source-of-truth.test.ts</verify>
  <acceptance_criteria>
    - `src/types/jobs.ts` contains `export type JobStatus =` and the literals `'queued'`, `'running'`, `'completed'`, `'failed'`, and `'cancelled'`
    - `src/types/jobs.ts` contains `export type AgentActionType = 'ats_enhancement' | 'job_targeting' | 'artifact_generation'`
    - `src/lib/jobs/source-of-truth.ts` contains `export function resolveEffectiveResumeSource(`
    - `src/lib/jobs/source-of-truth.ts` contains a branch that prefers `optimizedCvState` before `cvState`
  </acceptance_criteria>
  <done>The shared async action/job/source-of-truth contracts are explicit, stable, and importable before later runtime work splits.</done>
</task>

<task type="auto">
  <name>Task 2: Add the generic durable jobs table and repository helpers</name>
  <files>prisma/schema.prisma, prisma/migrations/20260416_generic_jobs_foundation.sql, src/lib/jobs/repository.ts</files>
  <read_first>prisma/schema.prisma, src/lib/linkedin/import-jobs.ts, src/lib/profile/pdf-import-jobs.ts, src/lib/db/session-lifecycle.ts, src/lib/db/resume-generations.ts, .planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-RESEARCH.md</read_first>
  <action>Add `model Job` to `prisma/schema.prisma` mapped to `jobs` with these fields: `id`, `userId`, optional `sessionId`, optional `resumeTargetId`, `type`, `status`, `stage`, `progress`, `dispatchInputRef`, `terminalResultRef`, `terminalErrorRef`, `metadata`, `claimedAt`, `startedAt`, `completedAt`, `cancelledAt`, `createdAt`, and `updatedAt`. Create `prisma/migrations/20260416_generic_jobs_foundation.sql` with matching SQL that creates `jobs` using `jsonb` for `progress`, `dispatch_input_ref`, `terminal_result_ref`, `terminal_error_ref`, and `metadata`, with status/type checks covering `ats_enhancement`, `job_targeting`, `artifact_generation` and `queued`, `running`, `completed`, `failed`, `cancelled`, plus indexes on `(user_id, created_at desc)`, `(status, created_at desc)`, and `(type, status)`. Implement `src/lib/jobs/repository.ts` using `getSupabaseAdminClient()` with `createJob`, `getJob`, `listJobsForUser`, `claimJob`, `completeJob`, `failJob`, `cancelJob`, and `resetStaleJobs`, reusing optimistic `UPDATE ... WHERE status = <expected>` claims plus `claimed_at` fencing from the existing import-job helpers.</action>
  <verify>npm run typecheck && npx vitest run src/lib/jobs/repository.test.ts</verify>
  <acceptance_criteria>
    - `prisma/schema.prisma` contains `model Job` and `@@map("jobs")`
    - `prisma/migrations/20260416_generic_jobs_foundation.sql` contains `create table if not exists jobs`
    - `src/lib/jobs/repository.ts` contains `export async function claimJob(`
    - `src/lib/jobs/repository.ts` contains `.eq('claimed_at', ownerClaimedAt)`
    - `src/lib/jobs/repository.ts` contains terminal helpers for `completed`, `failed`, and `cancelled`
  </acceptance_criteria>
  <done>The repo has one generic durable jobs table and one claimable repository foundation aligned with existing serverless-safe job patterns.</done>
</task>

<task type="auto">
  <name>Task 3: [BLOCKING] Push the schema change before verification</name>
  <files>prisma/schema.prisma, prisma/migrations/20260416_generic_jobs_foundation.sql</files>
  <read_first>prisma/schema.prisma, prisma/migrations/20260416_generic_jobs_foundation.sql, .planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-VALIDATION.md</read_first>
  <action>After Task 2 is complete, run `npx prisma db push` against the configured database so the new `Job` model is present in the live schema used by the app. If Prisma reports destructive changes, stop and get explicit approval before rerunning with `npx prisma db push --accept-data-loss`. If `DATABASE_URL` or equivalent database connectivity is missing, stop and surface the missing environment or connection instead of claiming verification passed.</action>
  <verify>npx prisma db push</verify>
  <acceptance_criteria>
    - `npx prisma db push` exits 0 or the task is explicitly escalated for missing DB connectivity or destructive-change approval
    - the executor records whether the `jobs` table was pushed instead of silently skipping the live schema step
    - phase verification is not marked complete while this blocking task is unresolved
  </acceptance_criteria>
  <done>The live database schema matches the committed generic jobs contract or the phase stops visibly at the blocking schema gate.</done>
</task>

<task type="auto">
  <name>Task 4: Add regression proof for contract shape, claim fencing, and source selection</name>
  <files>src/lib/jobs/contracts.test.ts, src/lib/jobs/repository.test.ts, src/lib/jobs/source-of-truth.test.ts</files>
  <read_first>src/lib/jobs/contracts.test.ts, src/lib/jobs/repository.test.ts, src/lib/jobs/source-of-truth.test.ts, src/lib/linkedin/import-jobs.test.ts, .planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-VALIDATION.md</read_first>
  <action>Add `src/lib/jobs/contracts.test.ts` to assert `JobStatus` includes `queued`, `running`, `completed`, `failed`, `cancelled` and `JobType` includes `ats_enhancement`, `job_targeting`, `artifact_generation`. Add `src/lib/jobs/repository.test.ts` with Supabase-admin mocks that prove `claimJob` fences on `status`, stale `claimed_at` rows can be reclaimed, and `completeJob` / `failJob` / `cancelJob` cannot overwrite a newer owner. Add `src/lib/jobs/source-of-truth.test.ts` to prove `resolveEffectiveResumeSource(...)` prefers `agentState.optimizedCvState ?? cvState` and that artifact result refs point to `resumeGenerationId` or exact snapshot refs rather than signed URLs.</action>
  <verify>npm run typecheck && npx vitest run src/lib/jobs/contracts.test.ts src/lib/jobs/repository.test.ts src/lib/jobs/source-of-truth.test.ts</verify>
  <acceptance_criteria>
    - `src/lib/jobs/contracts.test.ts` contains `describe('JobStatus'`
    - `src/lib/jobs/repository.test.ts` contains `claimed_at`
    - `src/lib/jobs/source-of-truth.test.ts` contains `resolveEffectiveResumeSource`
    - `npm run typecheck && npx vitest run src/lib/jobs/contracts.test.ts src/lib/jobs/repository.test.ts src/lib/jobs/source-of-truth.test.ts` exits 0
  </acceptance_criteria>
  <done>Phase 37 closes with committed proof that the new contract shape, claim fencing, and source-selection rules are stable.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run typecheck`
- [ ] `npx vitest run src/lib/jobs/contracts.test.ts src/lib/jobs/repository.test.ts src/lib/jobs/source-of-truth.test.ts`
- [ ] `npx prisma db push`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- Phase 37 leaves the repo with one shared async contract module, one generic durable jobs persistence foundation, one explicit source-of-truth helper layer, and blocking proof that the schema was actually pushed
</success_criteria>

<output>
After completion, create `.planning/phases/37-freeze-async-execution-contracts-and-durable-job-foundations/37-01-SUMMARY.md`
</output>
