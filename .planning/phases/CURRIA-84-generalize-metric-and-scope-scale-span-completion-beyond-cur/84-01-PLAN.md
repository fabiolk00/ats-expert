# 84-01 Plan

## Goal

Generalize metric and scope/scale completion beyond the previous corpus by using reusable phrase-structure rules rather than narrow phrase-shape assumptions.

## Steps

1. Replace narrow completion assumptions with bounded local phrase-pattern matching around the winner span.
2. Anchor metric completion on the metric token so shorter, more natural left-anchored phrases can win.
3. Preserve compact scope/scale completion while widening support for cross-domain structural shapes.
4. Add cross-domain regressions for sales, healthcare, customer success, operations, and education.
5. Record a short validation note confirming the generalized behavior.

## Verification

- `npm run typecheck`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
