# 83-01 Plan

## Goal

Make `metric` and `scope_scale` highlight winners read as complete compact evidence phrases without reopening selector ranking or diff-aware gating.

## Steps

1. Add bounded completion helpers for metric and scope/scale winners only.
2. Recover short left or right phrase context when it clearly improves readability without adding narrative.
3. Preserve connector trimming, compact caps, contextual stack health, and zero-highlight behavior.
4. Add regressions for complete metric phrases, complete scope/scale phrases, bounded expansion, and non-regression in stack and zero-highlight paths.

## Verification

- `npm run typecheck`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
