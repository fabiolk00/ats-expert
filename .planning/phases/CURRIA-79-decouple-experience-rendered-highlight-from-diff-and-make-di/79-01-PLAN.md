# 79-01 Plan

## Goal

Split experience highlight behavior into diff-aware improvement evaluation and optimized-text-first span rendering so the optimized preview stops choosing its rendered highlight directly from lexical comparison.

## Steps

1. Extract a bullet improvement evaluator that compares the closest original bullet against the optimized bullet to produce eligibility and improvement score without selecting the rendered span.
2. Replace the current experience candidate collector with an optimized-text-first span selector that ranks structural evidence from the optimized bullet only and allows zero highlight when no strong span exists.
3. Rewire `buildOptimizedPreviewHighlights(...)` so bullet ranking still uses improvement metadata while rendered highlight lines come only from the optimized-text selector.
4. Add regression tests for score-only diff gating, optimized-text structural span selection, weak narrative-only no-highlight behavior, and the existing per-entry highlight caps.

## Plan Review Notes

- Smallest viable refactor: preserve the public `HighlightedLine` contract and the current render path in `resume-comparison-view.tsx`.
- Keep `findClosestOriginalBullet(...)` because it is a useful comparison baseline for gating, even though it must stop driving the final rendered span.
- Avoid widening scope into summary highlighting, rewrite prompts, export templates, or ATS readiness logic.

## Verification

- `npm run typecheck`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
