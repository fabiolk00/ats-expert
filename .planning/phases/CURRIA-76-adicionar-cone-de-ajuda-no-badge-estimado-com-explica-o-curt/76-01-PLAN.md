# 76-01 Plan

## Goal

Make the `Estimado` ATS Readiness badge self-explanatory with a compact accessible help icon and tooltip.

## Steps

1. Create a shared ATS readiness status badge component that supports `Final` and `Estimado`.
2. Render a compact `?` help icon only for `Estimado`.
3. Add a short PT-BR explanation in a tooltip that works on hover, focus, and click.
4. Wire the shared component into the comparison and session-list ATS readiness surfaces.
5. Add regression tests for conditional rendering and tooltip behavior.

## Verification

- `npm run typecheck`
- `npx vitest run "src/components/ats-readiness-status-badge.test.tsx" "src/components/resume/resume-comparison-view.test.tsx" "src/components/dashboard/session-list.test.tsx"`
- `npm run audit:copy-regression`
