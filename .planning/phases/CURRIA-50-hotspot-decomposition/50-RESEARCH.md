# Phase 50 Research

The remaining hotspot risk lived in `smart-generation/decision.ts` and secondarily in `session-generate/decision.ts`. The safest path was helper extraction by concern: workflow mode, readiness, dispatch, preview normalization, and artifact fallback. That preserves behavior while removing semantic density from the entry orchestrators.
