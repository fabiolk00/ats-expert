# Phase 50 Plan 01

1. Extract `smart-generation` workflow mode, dispatch, preview-access, and normalization helpers.
2. Keep `smart-generation/decision.ts` orchestration-only.
3. Isolate completed artifact fallback in `session-generate/artifact-fallback.ts`.
4. Add explicit hotspot thresholds to `docs/architecture/hotspot-watchlist.md`.
