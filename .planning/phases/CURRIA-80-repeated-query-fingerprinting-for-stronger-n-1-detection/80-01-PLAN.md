# 80-01 Plan

1. Add a dedicated query fingerprint utility that normalizes repeated Supabase/PostgREST request descriptor shapes without over-collapsing semantically different filters.
2. Extend the request query context to aggregate fingerprint stats alongside raw sampled descriptors.
3. Enrich request flush logging with pattern summary fields, top repeated patterns, and a conservative `suspectedNPlusOne` heuristic.
4. Preserve the existing SSE-safe flush behavior for `/api/agent` while reusing the current request tracking architecture.
5. Add unit and route-level regressions covering normalization, aggregation, warning payloads, and repeated-versus-diverse high-count traffic.
