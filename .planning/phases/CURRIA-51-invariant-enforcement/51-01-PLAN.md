# Phase 51 Plan 01

1. Add route invariants for locked compare and locked versions.
2. Assert preview-access consistency in smart generation normalization.
3. Convert response mappers to exhaustive `switch(decision.kind)` patterns with `assertNever`.
