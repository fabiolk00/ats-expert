# Phase 51 Research

The repo already had canonical preview-lock helpers in `locked-preview.ts` and `preview-sanitization.ts`. The highest-leverage move was to add route seam invariants around locked compare, locked versions, preview-access consistency, and exhaustive response mapping.
