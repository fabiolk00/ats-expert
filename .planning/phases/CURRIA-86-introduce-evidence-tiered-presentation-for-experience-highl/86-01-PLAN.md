# Phase 86 Plan

1. Extend the preview render contract to preserve winner category and evidence tier for highlighted experience spans.
2. Keep Tier 1 (`metric`, `scope_scale`) on the existing strong inline emphasis.
3. Render Tier 2 (`contextual_stack`, `anchored_leadership`, `anchored_outcome`) with a lighter secondary treatment.
4. Add focused contract and component tests proving tier propagation and differentiated render behavior.
5. Verify with preview highlight tests plus typecheck.
