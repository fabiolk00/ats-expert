# 82-01 Plan

## Goal

Tighten the calibrated experience selector so contextual stack spans recover more reliably and compact winners render with cleaner, more complete evidence phrases.

## Steps

1. Add bounded cleanup helpers for weak trailing connectors and incomplete compact evidence spans.
2. Improve contextual stack extraction so strong multi-token stack clusters survive candidate building cleanly.
3. Slightly complete compact metric and scope winners when the extra context improves readability without making the span noisy.
4. Add regressions for contextual stack recovery, connector trimming, compact completeness, and zero-highlight safety.

## Verification

- `npm run typecheck`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
