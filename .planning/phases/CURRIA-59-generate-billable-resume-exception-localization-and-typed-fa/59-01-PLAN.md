# 59-01 Plan

1. Add a `BillableResumeStage` model plus helper utilities in `generate-billable-resume.ts`.
2. Wrap critical downstream calls with stage-local logging and error narrowing.
3. Convert stable billable state failures into typed failures:
   - latest version missing
   - pending generation missing
   - reservation failure
   - render failure
   - persistence failure where safe
4. Propagate `billableStage`, `generationIntentKey`, and `resumeGenerationId` into top-level `agent.tool.failed`.
5. Add stage-failure metrics and regression tests.
6. Re-run typecheck, focused vitest suites, route audit, and architecture proof pack.

## Done When

- Post-preflight failures are no longer opaque.
- Logs reveal which billable stage failed.
- Typed failures are preserved where safe.
- Tests prove stage localization and top-level propagation.
