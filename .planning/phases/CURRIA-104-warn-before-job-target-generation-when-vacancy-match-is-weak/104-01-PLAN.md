---
phase: 104-warn-before-job-target-generation-when-vacancy-match-is-weak
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/agent/profile-review.ts
  - src/app/api/session/[id]/route.ts
  - src/app/api/session/[id]/route.test.ts
  - src/types/dashboard.ts
  - src/components/dashboard/resume-workspace.tsx
  - src/components/dashboard/resume-workspace.test.tsx
  - src/lib/agent/agent-intents.ts
  - src/lib/agent/agent-loop.ts
  - src/lib/agent/streaming-loop.test.ts
  - src/components/dashboard/chat-interface.tsx
  - src/components/dashboard/chat-interface.test.tsx
  - src/components/dashboard/chat-interface.route-stream.test.tsx
autonomous: true
requirements:
  - P104-SNAPSHOT
  - P104-WORKSPACE
  - P104-UI
  - P104-CONTINUE
  - P104-FALLBACK
user_setup: []
must_haves:
  truths:
    - "When a saved target vacancy is still marked as weak-fit and pending override, clicking the generation CTA opens a confirmation modal instead of sending generation immediately."
    - "Canceling the weak-fit modal leaves the transcript and session state unchanged."
    - "Choosing `Continuar mesmo assim` performs one explicit action that confirms the override and continues generation without requiring a second manual `Aceito` turn."
    - "If the client bypasses the modal and posts a generation request directly, the backend still issues or preserves the existing realism warning and blocks generation until explicit override confirmation exists."
    - "The dashboard session snapshot exposes only a derived weak-fit confirmation checkpoint for the current stored target and clears it when the target changes or override is already confirmed."
  artifacts:
    - path: "src/lib/agent/profile-review.ts"
      provides: "Server-derived weak-fit checkpoint helper over the existing realism-warning logic"
      contains: "buildCareerFitCheckpoint"
    - path: "src/app/api/session/[id]/route.ts"
      provides: "Session snapshot serialization of the derived weak-fit checkpoint"
      contains: "careerFitCheckpoint"
    - path: "src/types/dashboard.ts"
      provides: "Dashboard workspace typing for the snapshot-exposed weak-fit checkpoint"
      contains: "careerFitCheckpoint?:"
    - path: "src/lib/agent/agent-loop.ts"
      provides: "Single-action weak-fit continue flow while preserving the existing backend fallback guard"
      contains: "handleConfirmedGeneration"
    - path: "src/components/dashboard/chat-interface.tsx"
      provides: "AlertDialog-based weak-fit confirmation modal gated off the session snapshot"
      contains: "Continuar mesmo assim"
    - path: "src/components/dashboard/chat-interface.route-stream.test.tsx"
      provides: "Real route-stream proof that modal continue reaches generation in one explicit action"
      contains: "Continuar mesmo assim"
  key_links:
    - from: "src/lib/agent/profile-review.ts"
      to: "src/app/api/session/[id]/route.ts"
      via: "session snapshot derives a narrow client contract from the server realism-warning logic instead of leaking raw `phaseMeta`"
      pattern: "buildCareerFitCheckpoint\\("
    - from: "src/app/api/session/[id]/route.ts"
      to: "src/components/dashboard/resume-workspace.tsx"
      via: "workspace refresh remains the canonical source of weak-fit confirmation state"
      pattern: "careerFitCheckpoint"
    - from: "src/components/dashboard/resume-workspace.tsx"
      to: "src/components/dashboard/chat-interface.tsx"
      via: "snapshot-derived checkpoint is passed into chat as a prop instead of recomputed in the client"
      pattern: "weakFitCheckpoint"
    - from: "src/components/dashboard/chat-interface.tsx"
      to: "/api/agent"
      via: "modal continue sends one explicit message recognized by the backend"
      pattern: "Continuar mesmo assim"
    - from: "src/lib/agent/agent-intents.ts"
      to: "src/lib/agent/agent-loop.ts"
      via: "new continue intent is recognized before the existing generation approval path"
      pattern: "isWeakFitContinueRequest"
    - from: "src/lib/agent/agent-loop.ts"
      to: "src/lib/agent/profile-review.ts"
      via: "generation still depends on the existing realism-warning and override confirmation helpers"
      pattern: "(requiresCareerFitWarning|hasActiveCareerFitWarning|buildCareerFitWarningText)"
---

<objective>
Warn the user with a real confirmation modal before job-target generation when the stored vacancy fit is weak, while keeping the backend realism-warning guard authoritative.

Purpose: remove the confusing transcript-only override round-trip without weakening the current server-side safety model.
Output: one derived snapshot checkpoint, one single-action backend continue path, and one AlertDialog-based chat confirmation flow with focused regression coverage.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@CLAUDE.md
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-104-warn-before-job-target-generation-when-vacancy-match-is-weak/104-CONTEXT.md
@.planning/phases/CURRIA-104-warn-before-job-target-generation-when-vacancy-match-is-weak/104-RESEARCH.md
@src/lib/agent/profile-review.ts
@src/lib/agent/agent-intents.ts
@src/lib/agent/agent-loop.ts
@src/app/api/session/[id]/route.ts
@src/types/agent.ts
@src/types/dashboard.ts
@src/components/dashboard/resume-workspace.tsx
@src/components/dashboard/chat-interface.tsx
@src/components/ui/alert-dialog.tsx

<scope_rules>
- Keep the current chat-driven job-targeting flow and reuse the existing weak-fit logic; do not invent a new scoring model or client-side heuristic. 
- Keep the change small: one snapshot-derived contract, one explicit continue intent, and one chat modal over the existing `Aceito` CTA.
- Preserve the backend realism-warning semantics as the final safety gate even if the browser skips or races the modal.
- Do not redesign the broader dashboard/chat workspace, persistence model, or transcript architecture.
</scope_rules>

<decision_trace>
- D-01: Reuse the existing server weak-fit logic and chat-driven flow; do not create a parallel scoring or confirmation system.
- D-02: Prefer a small UI enhancement plus minimal route/type exposure over broad agent-loop or workspace rewrites.
- D-03: Keep backend realism-warning semantics as the non-bypassable fallback safety net.
- D-04: Show the modal only when generation is about to happen for a stored target whose current server-derived fit state still requires override.
- D-05: `Continuar mesmo assim` must proceed through one explicit action, not two hidden client messages or a second manual `Aceito`.
</decision_trace>

<interfaces>
From `src/types/agent.ts`:

```ts
export type CareerFitCheckpoint = {
  status: 'pending_confirmation'
  targetJobDescription: string
  summary: string
  reasons: string[]
  nextSteps: string[]
  assessedAt: string
}
```

From `src/types/agent.ts`:

```ts
phaseMeta?: {
  careerFitWarningIssuedAt?: string
  careerFitWarningTargetJobDescription?: string
  careerFitOverrideConfirmedAt?: string
  careerFitOverrideTargetJobDescription?: string
}
```

From `src/components/dashboard/chat-interface.tsx`:

```tsx
const showGenerationApproval =
  phase === "confirm" || (phase === "dialog" && displayedReadinessScore !== undefined)

interface ChatInterfaceProps {
  sessionId?: string
  onAgentTurnCompleted?: (payload: AgentDoneChunk) => void
}
```

From `src/app/api/session/[id]/route.ts`:

```ts
return NextResponse.json({
  session: {
    agentState: {
      targetJobDescription: session.agentState.targetJobDescription,
      targetFitAssessment: session.agentState.targetFitAssessment,
      gapAnalysis: session.agentState.gapAnalysis,
    },
  },
})
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Expose one snapshot-canonical weak-fit checkpoint to the dashboard chat flow</name>
  <files>src/lib/agent/profile-review.ts, src/app/api/session/[id]/route.ts, src/app/api/session/[id]/route.test.ts, src/types/dashboard.ts, src/components/dashboard/resume-workspace.tsx, src/components/dashboard/resume-workspace.test.tsx</files>
  <behavior>
    - Test 1: the session snapshot returns `careerFitCheckpoint` only when the current saved target still has an active warning and no matching override confirmation.
    - Test 2: the checkpoint is omitted when there is no target, the target changed, or override was already confirmed for the current target.
    - Test 3: `ResumeWorkspace` passes the checkpoint into `ChatInterface` through the existing snapshot refresh path without creating a second client-side source of truth.
  </behavior>
  <action>Per D-01, D-02, and D-04, add a narrow helper such as `buildCareerFitCheckpoint(session)` beside the existing profile-review helpers and derive the client contract from current server truth only. Reuse the existing `CareerFitCheckpoint` type in `src/types/agent.ts`; do not expose raw `phaseMeta` to the dashboard client as the UI API. Serialize `careerFitCheckpoint` from `src/app/api/session/[id]/route.ts`, thread it through `SessionWorkspace` in `src/types/dashboard.ts`, and pass it from `ResumeWorkspace` into `ChatInterface` during the existing snapshot refresh flow. Keep `/api/session/[id]` thin, keep ownership/auth behavior unchanged, and do not add a new endpoint or patch-only UI state channel.</action>
  <verify>
    <automated>npx vitest run "src/app/api/session/[id]/route.test.ts" "src/components/dashboard/resume-workspace.test.tsx"</automated>
  </verify>
  <done>The dashboard has one canonical, server-derived weak-fit checkpoint for the current stored target, and chat can consume it without recomputing fit state in React.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a single-action backend continue intent while preserving the fallback realism guard</name>
  <files>src/lib/agent/agent-intents.ts, src/lib/agent/agent-loop.ts, src/lib/agent/streaming-loop.test.ts</files>
  <behavior>
    - Test 1: the explicit continue phrase used by the modal is recognized as its own intent.
    - Test 2: when override is pending for the current weak-fit target, that explicit continue action confirms the override and reaches the existing confirmed-generation path in the same turn.
    - Test 3: direct `Aceito` or generation requests without explicit override confirmation still surface the realism warning and do not reach `generate_file`.
  </behavior>
  <action>Per D-01, D-03, and D-05, add one narrow intent matcher in `agent-intents.ts` for the modal action text and handle it in `agent-loop.ts` before the existing generation approval/request branches. The explicit continue path must call the current override-confirmation helper, reuse the existing generation prerequisites and `handleConfirmedGeneration(...)`, and keep the textual warning path intact for bypassed or stale clients. Do not remove the current `Aceito` protection, do not auto-chain two hidden browser messages, and do not reopen unrelated dialog/rewrite behavior.</action>
  <verify>
    <automated>npx vitest run "src/lib/agent/streaming-loop.test.ts"</automated>
  </verify>
  <done>The backend accepts one explicit continue action for pending weak-fit targets, but still blocks direct generation attempts until override confirmation exists.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Gate the existing `Aceito` CTA with a weak-fit confirmation modal in chat</name>
  <files>src/components/dashboard/chat-interface.tsx, src/components/dashboard/chat-interface.test.tsx, src/components/dashboard/chat-interface.route-stream.test.tsx</files>
  <behavior>
    - Test 1: clicking `Aceito` with a pending weak-fit checkpoint opens the modal and does not POST to `/api/agent`.
    - Test 2: clicking `Cancelar` closes the modal and leaves the transcript/session untouched.
    - Test 3: clicking `Continuar mesmo assim` closes the modal and sends exactly one explicit continue message.
    - Test 4: the real route-stream integration proves the modal continue action reaches generation without a second manual `Aceito`.
  </behavior>
  <action>Per D-02, D-04, and D-05, extend `ChatInterface` to accept the snapshot-derived checkpoint prop and wrap the existing generation CTA with the repo’s `AlertDialog` primitives. Only intercept the CTA when `showGenerationApproval` is true and the checkpoint is pending for the current stored target. Render the summary/reasons/next-step warning as plain React text, not HTML. `Cancelar` must only dismiss local modal state. `Continuar mesmo assim` must call `handleSend({ message: "Continuar mesmo assim" })` once, then rely on the backend path from Task 2. Preserve the normal `Aceito` behavior when no checkpoint is present, keep the existing transcript/session refresh mechanics, and keep the UI change local to `chat-interface.tsx` instead of redesigning the broader workspace.</action>
  <verify>
    <automated>npx vitest run "src/components/dashboard/chat-interface.test.tsx" "src/components/dashboard/chat-interface.route-stream.test.tsx"</automated>
  </verify>
  <done>The user sees a clear confirmation modal only for pending weak-fit target generation, can cancel safely, and can continue through one explicit action with focused UI and route-stream proof.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| browser -> `ChatInterface` modal | Client UI can be skipped or stale and must not become the only gate. |
| browser -> `/api/agent` | Direct POSTs can bypass the modal and still need backend enforcement. |
| session snapshot route -> dashboard client | Only a narrow derived weak-fit checkpoint should cross this boundary, not raw persistence details. |
| session snapshot route -> owned session data | Ownership/auth checks must remain intact before serializing any checkpoint state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-104-01 | T | `src/components/dashboard/chat-interface.tsx` | mitigate | Treat the modal as UX only; keep the current realism-warning/override checks in `agent-loop.ts` as the final generation gate. |
| T-104-02 | I | `src/app/api/session/[id]/route.ts` | mitigate | Preserve current auth plus owned-session lookup and expose a derived `careerFitCheckpoint` instead of raw `phaseMeta`. |
| T-104-03 | T | `src/lib/agent/profile-review.ts` | mitigate | Bind the checkpoint to the current `targetJobDescription` so stale warning/override metadata does not unlock a different vacancy. |
| T-104-04 | X | `src/components/dashboard/chat-interface.tsx` | mitigate | Render warning content as plain React text and keep `dangerouslySetInnerHTML` out of the modal. |
</threat_model>

<verification>
- `npx vitest run "src/app/api/session/[id]/route.test.ts" "src/components/dashboard/resume-workspace.test.tsx" "src/lib/agent/streaming-loop.test.ts" "src/components/dashboard/chat-interface.test.tsx" "src/components/dashboard/chat-interface.route-stream.test.tsx"`
- `npm run typecheck`
</verification>

<success_criteria>
- The session snapshot exposes a derived weak-fit checkpoint only when the current saved target still requires explicit override confirmation.
- Clicking `Aceito` in that state opens a confirmation modal instead of posting generation immediately.
- `Cancelar` leaves the transcript and session unchanged, while `Continuar mesmo assim` performs one explicit action that reaches generation without a second manual round-trip.
- Direct generation POSTs that bypass the modal still hit the backend realism-warning guard until explicit override confirmation exists.
- Focused route, workspace, agent-loop, and chat tests pass along with `npm run typecheck`.
</success_criteria>

<output>
After completion, create:

- `.planning/phases/CURRIA-104-warn-before-job-target-generation-when-vacancy-match-is-weak/104-01-SUMMARY.md`
- `.planning/phases/CURRIA-104-warn-before-job-target-generation-when-vacancy-match-is-weak/104-REVIEW.md`
- `.planning/phases/CURRIA-104-warn-before-job-target-generation-when-vacancy-match-is-weak/104-REVIEW-FIX.md`
</output>
