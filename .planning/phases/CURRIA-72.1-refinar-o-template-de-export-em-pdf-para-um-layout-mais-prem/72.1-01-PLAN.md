# 72.1-01 Plan

1. Embed the same Inter font family used by the preview into the PDF export path so the final artifact matches the product typography more closely.
2. Refine the PDF template hierarchy in `generatePDF(...)` with calmer separators, stronger heading rhythm, and more premium spacing for header, experience, education, and skills blocks.
3. Keep the export ATS-safe by preserving a linear single-column structure, real text rendering, and explicit section headings.
4. Update PDF export tests so they continue to verify content presence, ordering, and artifact behavior after the visual refinement.
