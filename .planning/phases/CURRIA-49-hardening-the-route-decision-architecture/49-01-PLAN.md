---
phase: 49-hardening-the-route-decision-architecture
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docs/architecture/route-policy-boundaries.md
  - docs/architecture/route-review-checklist.md
  - docs/architecture/hotspot-watchlist.md
  - src/lib/routes/file-access/response.test.ts
  - src/lib/routes/file-access/decision.test.ts
  - src/lib/routes/session-generate/response.test.ts
  - src/app/api/file/[sessionId]/route.test.ts
  - src/app/api/profile/smart-generation/route.test.ts
  - src/app/api/session/[id]/generate/route.test.ts
autonomous: true
requirements:
  - ROUTE-ARCH-TEST-01
  - ROUTE-ARCH-GUARD-01
must_haves:
  truths:
    - "Critical route boundaries are written down as enforceable may-do and must-not-do rules for context, policy, decision, and response layers."
    - "Reviewers can tell when the full route pattern applies to a dense policy route and when it should not be pushed onto simple CRUD handlers."
    - "Focused seam tests prove response mappers only honor explicit decision kinds and preserve preview-lock and precedence-sensitive behavior."
  artifacts:
    - path: "docs/architecture/route-policy-boundaries.md"
      provides: "Concrete boundary rules and examples for the Phase 48 route architecture"
    - path: "docs/architecture/route-review-checklist.md"
      provides: "Critical-route review checklist with when-to-use and when-not-to-use guidance"
    - path: "docs/architecture/hotspot-watchlist.md"
      provides: "Monitored hotspots and drift indicators for session-generate and smart-generation"
    - path: "src/lib/routes/file-access/response.test.ts"
      provides: "Mapper integrity proof for explicit decision-to-HTTP behavior"
    - path: "src/lib/routes/session-generate/response.test.ts"
      provides: "Generate-response proof for policy, reconciliation, completion, and failure mapping"
  key_links:
    - from: "src/lib/routes/file-access/response.test.ts"
      to: "src/lib/routes/file-access/response.ts"
      via: "decision-kind assertions"
      pattern: "toFileAccessResponse"
    - from: "src/lib/routes/session-generate/response.test.ts"
      to: "src/lib/routes/session-generate/response.ts"
      via: "execution-kind assertions"
      pattern: "toSessionGenerateExecutionResponse"
    - from: "src/app/api/session/[id]/generate/route.test.ts"
      to: "src/app/api/session/[id]/generate/route.ts"
      via: "route-order regression proof"
      pattern: "CAREER_FIT_CONFIRMATION_REQUIRED|BILLING_RECONCILIATION_PENDING|EXPORT_ALREADY_PROCESSING"
---

<objective>
Freeze the Phase 48 route architecture into an enforceable contract before refactoring the remaining hotspots.

Purpose: Satisfy guardrail and seam-proof requirements first so later boundary cleanup cannot drift into silent behavior changes.
Output: Hardened route architecture docs, review checklist and hotspot watchlist, plus focused mapper and precedence seam tests for the critical routes.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-49-hardening-the-route-decision-architecture/49-CONTEXT.md
@.planning/phases/CURRIA-49-hardening-the-route-decision-architecture/49-RESEARCH.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-01-SUMMARY.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-02-SUMMARY.md
@.planning/phases/CURRIA-48-route-policy-extraction-and-decision-normalization/48-03-SUMMARY.md
@docs/architecture/route-policy-boundaries.md
@src/lib/routes/file-access/response.ts
@src/lib/routes/session-generate/response.ts
@src/app/api/session/[id]/generate/route.ts

<interfaces>
From src/lib/routes/file-access/types.ts:
```ts
export type FileAccessDecision =
  | { kind: 'artifact_unavailable'; body: Record<string, unknown> }
  | { kind: 'locked_preview'; body: Record<string, unknown> }
  | { kind: 'artifact_available'; pdfPath: string; body: Record<string, unknown> }
```

From src/lib/routes/session-generate/types.ts:
```ts
export type SessionGeneratePolicyDecision =
  | { kind: 'allow' }
  | { kind: 'blocked_active_export'; conflictingJob: JobStatusSnapshot }

export type SessionGenerateExecutionDecision =
  | { kind: 'blocked_reconciliation'; job: JobStatusSnapshot }
  | { kind: 'failed'; job: JobStatusSnapshot }
  | { kind: 'completed'; job: JobStatusSnapshot; targetId?: string }
  | { kind: 'in_progress'; job: JobStatusSnapshot; targetId?: string }
```

From src/app/api/session/[id]/generate/route.ts:
```ts
const policyDecision = await evaluateSessionGeneratePolicy(context)
if (policyDecision.kind !== 'allow') {
  return toSessionGeneratePolicyResponse(context, policyDecision)
}

const executionDecision = await executeSessionGenerateFlow(context)
return toSessionGenerateExecutionResponse(context, executionDecision)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Harden the route-boundary docs and review guardrails</name>
  <files>docs/architecture/route-policy-boundaries.md, docs/architecture/route-review-checklist.md, docs/architecture/hotspot-watchlist.md</files>
  <action>Update `route-policy-boundaries.md` to encode the explicit "may do / must not do" boundaries from Phase 49 context per D-01 through D-05, including the rule that `response.ts` maps explicit decision kinds only, `context.ts` does typed request resolution only, and the full pattern applies only to dense policy routes. Create `route-review-checklist.md` as the reviewer gate for new critical routes, including when to require the pattern and when not to expand it to CRUD routes per D-03 and D-04. Create `hotspot-watchlist.md` that names `src/lib/routes/session-generate/decision.ts` and `src/lib/routes/smart-generation/decision.ts`, defines the drift signals to watch, and states the allowed route-specific decompositions without proposing generic framework work.</action>
  <verify>
    <automated>rg -n "may do|must not|context.ts|policy.ts|decision.ts|response.ts|CRUD|hotspot|session-generate|smart-generation" docs/architecture/route-policy-boundaries.md docs/architecture/route-review-checklist.md docs/architecture/hotspot-watchlist.md</automated>
  </verify>
  <done>The docs explicitly encode the Phase 49 invariants, reviewers can tell when the pattern applies, and the hotspot watchlist names the monitored modules and drift indicators without introducing deferred framework ideas.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add mapper-integrity seam tests for file access and session generate</name>
  <files>src/lib/routes/file-access/response.test.ts, src/lib/routes/file-access/decision.test.ts, src/lib/routes/session-generate/response.test.ts</files>
  <behavior>
    - Test 1: `toFileAccessResponse` returns the locked preview payload as-is for `locked_preview` and signs URLs only for `artifact_available`.
    - Test 2: `toFileAccessResponse` never needs raw artifact metadata to choose its HTTP branch; the branch is driven only by the explicit `FileAccessDecision.kind`.
    - Test 3: `toSessionGenerateExecutionResponse` maps `blocked_reconciliation`, `failed`, `completed`, and `in_progress` strictly from `SessionGenerateExecutionDecision.kind`, preserving the existing 409, error, 200, and 202 contracts.
    - Test 4: `toSessionGeneratePolicyResponse` keeps the active-export conflict payload unchanged when policy blocks first.
  </behavior>
  <action>Create focused Vitest suites that exercise the real response mappers with synthetic contexts and explicit decision objects. Keep the assertions route-specific per D-03 and D-05: do not add shared test harnesses or generic framework helpers. Extend the existing `file-access/decision.test.ts` only where needed to cover precedence-sensitive locked-preview behavior before signed URL paths are considered.</action>
  <verify>
    <automated>npx vitest run src/lib/routes/file-access/decision.test.ts src/lib/routes/file-access/response.test.ts src/lib/routes/session-generate/response.test.ts</automated>
  </verify>
  <done>Route seam tests fail if a response mapper starts inferring semantics from raw state instead of explicit decisions, and the current public response behavior remains asserted at the mapper seam.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Add route-level precedence proof for generate, file access, and smart generation</name>
  <files>src/app/api/file/[sessionId]/route.test.ts, src/app/api/profile/smart-generation/route.test.ts, src/app/api/session/[id]/generate/route.test.ts</files>
  <behavior>
    - Test 1: `POST /api/session/[id]/generate` still enforces career-fit confirmation and policy blocking before durable execution, preserving the current 409 outcomes.
    - Test 2: `GET /api/file/[sessionId]` still refuses real artifact access when preview lock wins, even if artifact metadata looks ready.
    - Test 3: `POST /api/profile/smart-generation` still returns the locked synthetic preview contract without leaking the real optimized output.
  </behavior>
  <action>Strengthen the route regression suites around the precedence rules called out in Phase 49 context and the Phase 48 review watchlist. Keep the coverage route-specific: assert the exact boundary between handler, decision, and response seams, but avoid generic route-framework assertions. These tests should protect D-01 and D-05 by proving blocking policy wins before execution and locked outcomes never leak real artifacts or diffs.</action>
  <verify>
    <automated>npx vitest run src/app/api/file/[sessionId]/route.test.ts src/app/api/profile/smart-generation/route.test.ts src/app/api/session/[id]/generate/route.test.ts</automated>
  </verify>
  <done>The route suites explicitly pin the precedence-sensitive behavior for generate, file access, and smart generation so later hotspot cleanup cannot reorder blocking logic or leak locked content.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> critical route handlers | Untrusted requests attempt to cross auth, preview-lock, and billing-sensitive boundaries |
| decision layer -> response layer | Normalized internal outcomes cross into public HTTP payloads and must not be reinterpreted |
| contributor change -> critical route modules | Future feature work can accidentally collapse boundaries or reorder precedence-sensitive guards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-49-01 | T | `src/lib/routes/file-access/response.ts` | mitigate | Add mapper-integrity tests proving signed URLs are emitted only for `artifact_available`, never inferred from raw artifact metadata. |
| T-49-02 | I | `src/app/api/file/[sessionId]/route.test.ts` | mitigate | Add precedence route tests that pin locked-preview behavior before artifact-ready paths are considered. |
| T-49-03 | T | `docs/architecture/route-review-checklist.md` | mitigate | Encode reviewer guardrails that force critical-route boundary checks and explicitly exclude CRUD routes from ceremony creep. |
| T-49-04 | R | `docs/architecture/hotspot-watchlist.md` | accept | Documentation alone cannot stop regressions, so pair the watchlist with executable seam tests and route proofs. |
</threat_model>

<verification>
Run the mapper and route seam suites listed in each task. Confirm the docs contain the explicit boundary and applicability rules before moving to the hotspot refactor plan.
</verification>

<success_criteria>
Phase 49 has enforceable guardrails before code refactors: docs define the boundaries, the review checklist says when the pattern does and does not apply, hotspot modules are explicitly monitored, and seam tests pin mapper integrity plus precedence-sensitive behavior for the critical routes.
</success_criteria>

<output>
After completion, create `.planning/phases/49-hardening-the-route-decision-architecture/49-01-SUMMARY.md`
</output>
