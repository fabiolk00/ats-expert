---
phase: 99-adaptar-a-ui-de-profile-do-curr-culo-para-layout-crm-preserv
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/resume/visual-resume-editor.tsx
  - src/components/resume/visual-resume-editor.test.tsx
  - src/components/resume/user-data-page.tsx
  - src/components/resume/user-data-page.test.tsx
autonomous: false
requirements:
  - RESUME-PROFILE-CRM-01
  - RESUME-PROFILE-EDIT-01
  - RESUME-PROFILE-TEST-01
must_haves:
  truths:
    - "The profile page renders in a clean CRM-style shell while still loading, saving, importing, and starting ATS or target-job generation from the same live handlers."
    - "Each section card edit button opens the real `VisualResumeEditor` and focuses the matching section instead of launching a second form or a decorative action."
    - "Desktop long-content sections stay internally scrollable without page-level overflow, while mobile keeps stacked scrolling."
    - "No fake download control is introduced: a download CTA appears only if Phase 99 can reuse a real page-local download seam that already exists."
  artifacts:
    - path: "src/components/resume/visual-resume-editor.tsx"
      provides: "Externally triggerable section-open/focus seam over the existing editor."
      contains: "typed section request contract for summary/experience/skills/education/certifications"
    - path: "src/components/resume/visual-resume-editor.test.tsx"
      provides: "Focused regression proof for repeated section requests and preserved editor behavior."
      contains: "section request opens and focuses the intended editor area"
    - path: "src/components/resume/user-data-page.tsx"
      provides: "CRM layout shell that still owns profile fetch/save, import, generation, dialogs, and routing."
      contains: "card edit actions wired into the real editor request seam"
    - path: "src/components/resume/user-data-page.test.tsx"
      provides: "Focused regression coverage for CRM card actions, preserved flows, and no-fake-download behavior."
      contains: "section edit buttons, ATS dialogs, overflow-safe cards, and download gate assertion"
  key_links:
    - from: "src/components/resume/user-data-page.tsx"
      to: "src/components/resume/visual-resume-editor.tsx"
      via: "page-owned typed section request state"
      pattern: "requested section/open/focus seam"
    - from: "src/components/resume/user-data-page.tsx"
      to: "/api/profile"
      via: "existing `persistProfile()` and load-on-mount flow"
      pattern: "fetch\\(\"/api/profile\""
    - from: "src/components/resume/user-data-page.tsx"
      to: "/api/profile/ats-enhancement|/api/profile/smart-generation"
      via: "existing `handleSetupGeneration()` endpoint routing"
      pattern: "generationEndpoint = generationMode === \"job_targeting\""
    - from: "src/components/resume/user-data-page.tsx"
      to: "src/components/resume/resume-builder.tsx"
      via: "existing `ImportResumeModal` open/start/finish/success callbacks"
      pattern: "ImportResumeModal"
---

<objective>
Adapt the resume profile page into the approved CRM-style layout without changing the page's current business logic, network handlers, or editing flow.

Purpose: satisfy `RESUME-PROFILE-CRM-01`, `RESUME-PROFILE-EDIT-01`, and `RESUME-PROFILE-TEST-01` by replacing the visual shell only, keeping `user-data-page.tsx` as the orchestration owner, and making every new section edit affordance reuse the real `VisualResumeEditor`.
Output: a CRM-style `user-data-page.tsx`, a thin section-focus seam in `VisualResumeEditor`, focused RTL coverage, and a human verification checkpoint against the approved layout direction.
</objective>

<execution_context>
@C:/CurrIA/.codex/get-shit-done/workflows/execute-plan.md
@C:/CurrIA/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/CURRIA-99-adaptar-a-ui-de-profile-do-curr-culo-para-layout-crm-preserv/99-CONTEXT.md
@.planning/phases/CURRIA-99-adaptar-a-ui-de-profile-do-curr-culo-para-layout-crm-preserv/99-RESEARCH.md
@.planning/phases/CURRIA-99-adaptar-a-ui-de-profile-do-curr-culo-para-layout-crm-preserv/99-PRD.md
@.planning/phases/CURRIA-72-padronizar-nome-inteligente-dos-arquivos-exportados-por-tipo/72-01-SUMMARY.md
@.planning/phases/CURRIA-74-corrigir-bloqueio-indevido-de-edi-o-manual-por-exporta-o-e-r/74-01-SUMMARY.md
@.planning/phases/CURRIA-75-code-review-e-hardening-por-testes-do-fluxo-de-edi-o-manual-/75-01-SUMMARY.md
@src/app/(auth)/dashboard/resumes/new/page.tsx
@src/components/resume/user-data-page.tsx
@src/components/resume/user-data-page.test.tsx
@src/components/resume/visual-resume-editor.tsx
@src/components/resume/resume-builder.tsx
@src/components/resume/generation-loading.tsx
@src/components/dashboard/preview-panel.tsx
@src/components/resume/resume-comparison-view.tsx
@b_Wj12d9cRyft.zip

<interfaces>
From `src/components/resume/user-data-page.tsx`:
```tsx
const [resumeData, setResumeData] = useState<CVState>(() => normalizeResumeData())
const [targetJobDescription, setTargetJobDescription] = useState("")
const [activeImportSource, setActiveImportSource] = useState<ImportSource | null>(null)

const persistProfile = async (): Promise<void> => { ... }
const handleSave = async (): Promise<void> => { ... }
const handleSetupGeneration = async (): Promise<void> => { ... }

<VisualResumeEditor
  value={resumeData}
  onChange={setResumeData}
  disabled={isSaving || isRunningAtsEnhancement}
  onAllSectionsClosedChange={setAllSectionsClosed}
  compactMode={allSectionsClosed}
  importProgressSource={activeImportSource}
/>
```

From `src/components/resume/visual-resume-editor.tsx`:
```ts
type VisualResumeEditorProps = {
  value: CVState
  onChange: (nextValue: CVState) => void
  disabled?: boolean
  onAllSectionsClosedChange?: (allClosed: boolean) => void
  compactMode?: boolean
  importProgressSource?: ImportSource | null
}

type SectionId =
  | "personal"
  | "summary"
  | "experience"
  | "skills"
  | "education"
  | "certifications"
```

From `src/components/resume/resume-builder.tsx`:
```ts
export type ImportSource = "linkedin" | "pdf"
```
</interfaces>

<existing_seams>
- `UserDataPage` currently owns profile load/save, import, generation setup, dialogs, toasts, and routing, but it does not currently hold `sessionId`, `previewUrl`, `pdfUrl`, `pdfFileName`, or an `/api/file` consumer.
- Existing download surfaces already in the repo require resolved artifact URLs first: `preview-panel.tsx` downloads from `previewUrl`, and `resume-comparison-view.tsx` downloads from `downloadState.pdfUrl` / `pdfFileName`.
- Phase 99 must therefore treat the header `Download PDF` request as a validation gate, not a guaranteed task: only wire it if the page gains a real existing seam during implementation without new APIs or fake artifact state. Otherwise keep the profile page honest and leave the CTA absent.
</existing_seams>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a typed section-focus seam to the real editor</name>
  <files>src/components/resume/visual-resume-editor.tsx, src/components/resume/visual-resume-editor.test.tsx</files>
  <behavior>
    - Test 1: an external request for `summary`, `experience`, `skills`, `education`, or `certifications` expands the matching section even if it was previously collapsed.
    - Test 2: repeated requests for the same section still reopen or refocus the same real editor area instead of being ignored as duplicate state.
    - Test 3: existing toggle, compact mode, and import-progress behavior still work after the new request seam is added.
  </behavior>
  <action>Extend `VisualResumeEditor` to expose a small parent-driven control seam for Phase 99 (supports `RESUME-PROFILE-EDIT-01`; per 99-CONTEXT "Behavior preservation" and "Testing expectations"). Export a typed editable-section contract derived from the current section IDs, and accept a request payload that lets `user-data-page.tsx` tell the editor which real section to reveal. When a request arrives, open that section, scroll it into view, and focus the first meaningful field in that section after render. Preserve the current `openSections` ownership, `onAllSectionsClosedChange`, import animation, and field mutation logic; do not clone the editor, do not create local-only form state, and do not weaken the current collapse semantics.</action>
  <verify>
    <automated>npx vitest run "src/components/resume/visual-resume-editor.test.tsx"</automated>
  </verify>
  <done>The editor has one explicit, test-backed seam that lets the CRM cards trigger the existing editing experience for the right section without introducing a second editor.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace the profile shell with CRM cards while preserving live flows</name>
  <files>src/components/resume/user-data-page.tsx, src/components/resume/user-data-page.test.tsx</files>
  <behavior>
    - Test 1: the CRM page still loads profile data from `/api/profile`, keeps import/save/generation handlers intact, and renders live content from `resumeData` / sanitized/template state instead of mock examples.
    - Test 2: each section card renders a black edit affordance that routes into the Task 1 editor seam, and empty certifications still show a header plus a working edit action.
    - Test 3: the ATS and target-job flows still use the existing textarea, dialogs, toasts, loading overlay, route push, and endpoint selection rules.
    - Test 4: desktop long-content sections expose overflow-safe containers, and the page does not introduce a decorative `Download PDF` button when no real page-local seam exists.
  </behavior>
  <action>Refactor `user-data-page.tsx` as a presentation-shell replacement only (supports `RESUME-PROFILE-CRM-01`, `RESUME-PROFILE-EDIT-01`, and `RESUME-PROFILE-TEST-01`; per 99-CONTEXT "Layout direction", "Behavior preservation", and "Data fidelity"). Keep `loadProfile`, `persistProfile`, `handleSave`, `handleImportSuccess`, `handleSetupGeneration`, `targetJobDescription`, dialogs, `ImportResumeModal`, `GenerationLoading`, and route pushes intact. Replace the KPI/sidebar shell with a compact CRM header, two-column read-only cards, black primary actions, and a still-mounted `VisualResumeEditor` workspace that the new card edit buttons can reveal/focus through Task 1's seam. Render all card content from `sanitizeResumeData(resumeData)` and `cvStateToTemplateData(...)`; keep honest empty states and scroll-safe card bodies on desktop while stacking naturally on smaller screens. Preserve existing CTA behavior for import and `Melhorar currículo com IA`. Before adding any `Download PDF` button, validate that this page has a real authenticated artifact/download seam equivalent to the existing preview/comparison surfaces; if that seam is still absent, omit the CTA and lock the absence in tests rather than inventing a fake action or a new API.</action>
  <verify>
    <automated>npx vitest run "src/components/resume/user-data-page.test.tsx" && npm run typecheck</automated>
  </verify>
  <done>`UserDataPage` matches the CRM layout direction, still drives the original profile/import/generation flow, card edit buttons open the real editor behavior, and no fake download contract is introduced.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human-verify the CRM shell and preserved flows</name>
  <action>Compare the implemented page against the approved CRM direction and confirm that every preserved user flow still works through the real editor, import modal, and generation handlers before closing Phase 99.</action>
  <what-built>CRM-style profile page shell over the existing profile/import/edit/generation flow, plus section edit buttons wired into the real `VisualResumeEditor`.</what-built>
  <how-to-verify>
    1. Start the app and open `/dashboard/resume/new` with a profile that has real summary, experience, skills, education, and empty or filled certifications.
    2. On desktop, confirm the page uses the approved CRM direction from `b_Wj12d9cRyft.zip`: compact white header, rounded section cards, black primary actions, two-column layout, and no old KPI/sidebar shell.
    3. Click the edit button on Summary, Experience, Skills, Education, and Certifications. Each action must reveal the existing editor and focus the matching section instead of opening a new disconnected form.
    4. Confirm Import still opens the existing modal, Save still persists the profile, and `Melhorar currículo com IA` still enters the existing enhancement flow with the same ATS or target-job behavior.
    5. Populate long experience content and confirm the section scrolls internally on desktop instead of making the full page overflow. On mobile/tablet widths, confirm the cards stack and the page scrolls normally.
    6. If a `Download PDF` button is present, verify it performs a real authenticated download from an existing seam. If no real seam was available, verify the button is absent.
  </how-to-verify>
  <verify>
    <automated>Manual checkpoint — complete the listed `/dashboard/resume/new` verification steps after Tasks 1 and 2 pass.</automated>
  </verify>
  <done>The user confirms that the CRM shell matches the approved direction and that import, save, edit, generation, and download expectations remain accurate on the real page.</done>
  <resume-signal>Type "approved" if the CRM shell and preserved flows match expectations, or describe the mismatch to continue.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CRM card actions -> `VisualResumeEditor` | New UI affordances now control the canonical editor state; wrong mapping can send users to the wrong editable section. |
| Profile page client -> existing profile and generation routes | The refactor keeps `/api/profile`, `/api/profile/ats-enhancement`, and `/api/profile/smart-generation` as the only live network seams. |
| Profile page header -> artifact/download surface | A new CTA could imply download capability even though this page does not currently own artifact URL state. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-99-01 | T | `src/components/resume/user-data-page.tsx` -> `src/components/resume/visual-resume-editor.tsx` | mitigate | Use one exported typed section union and explicit card-to-editor mapping, then cover each edit affordance in RTL so a card cannot open the wrong canonical section or a fake local editor. |
| T-99-02 | T | `src/components/resume/user-data-page.tsx` download/header actions | mitigate | Treat `Download PDF` as a validation gate: reuse only an already-authorized real seam with existing artifact URLs, otherwise omit the CTA entirely and assert its absence in tests. |
| T-99-03 | D | `src/components/resume/user-data-page.tsx` long-content cards | mitigate | Keep desktop cards `min-h-0` with internal `overflow-y-auto`, preserve mobile stacked scroll, and add UI regression assertions so long content cannot hide controls through overflow. |
| T-99-04 | T | `src/components/resume/user-data-page.tsx` data rendering | mitigate | Render CRM cards from `sanitizeResumeData(resumeData)` and `cvStateToTemplateData(...)` only, preserving the current canonical state path and avoiding mock/demo content in production rendering. |
</threat_model>

<verification>
- `npx vitest run "src/components/resume/visual-resume-editor.test.tsx" "src/components/resume/user-data-page.test.tsx"`
- `npm run typecheck`
- Human verification checkpoint against `/dashboard/resume/new` and `b_Wj12d9cRyft.zip`
</verification>

<success_criteria>
- The resume profile page uses the approved CRM-style shell while preserving current load/save/import/generation/dialog/toast behavior and route contracts.
- Every CRM section edit button triggers the real `VisualResumeEditor` section behavior, including repeated requests for the same section.
- Long real profile content remains accessible inside scroll-safe cards, and mobile/tablet layouts stack cleanly.
- The implementation either wires a real existing download seam or intentionally renders no download button on this page; no fake download behavior ships.
</success_criteria>

<output>
After completion, create `.planning/phases/CURRIA-99-adaptar-a-ui-de-profile-do-curr-culo-para-layout-crm-preserv/99-01-SUMMARY.md`
</output>
