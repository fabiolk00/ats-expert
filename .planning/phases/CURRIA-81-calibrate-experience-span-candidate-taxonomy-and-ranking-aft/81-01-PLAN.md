# 81-01 Plan

## Goal

Calibrate the experience highlight selector so structural evidence consistently outranks narrative phrasing after the diff/render split.

## Steps

1. Verify whether the bottleneck is gating or span selection before changing ranking logic.
2. Introduce a candidate taxonomy for the optimized-text selector with strong priority for metric, scope/scale, and contextual stack signals.
3. Filter weak standalone candidates and keep only compact, non-overlapping spans.
4. Add regressions proving structural metrics, explicit scope/scale, and contextual stacks beat generic narrative phrasing.

## Verification

- `npm run typecheck`
- `npx vitest run "src/lib/resume/optimized-preview-highlights.test.ts" "src/lib/resume/optimized-preview-contracts.test.ts" "src/components/resume/resume-comparison-view.test.tsx"`
