---
phase: 34-non-e2e-runtime-residual-budgeting
plan: "02"
type: execute
wave: 2
depends_on:
  - 34-01
files_modified:
  - package.json
  - scripts/**
  - src/components/resume/resume-builder.tsx
  - src/components/resume/resume-builder.test.tsx
  - .github/workflows/ci.yml
  - .planning/phases/34-non-e2e-runtime-residual-budgeting/**
autonomous: true
requirements:
  - PERF-05
user_setup: []
must_haves:
  truths:
    - "The dominant residual runtime bottleneck must either improve materially or be carried as explicit accepted debt with proof."
    - "If the repo keeps the bottleneck, the budget and gate must become visible and intentional."
    - "Runtime wins must not come from silently weakening polling or async-behavior assertions."
  artifacts:
    - path: "src/components/resume/resume-builder.test.tsx"
      provides: "Primary likely runtime outlier"
      contains: "waitFor"
    - path: "src/components/resume/resume-builder.tsx"
      provides: "Polling seam that can be optimized or made more test-deterministic"
      contains: "linkedinPollMs"
    - path: "package.json"
      provides: "Runtime command surface for budget proof or gating"
      contains: "test:profile:non-e2e"
  key_links:
    - from: "src/components/resume/resume-builder.tsx"
      to: "src/components/resume/resume-builder.test.tsx"
      via: "polling implementation and timer strategy must stay behaviorally aligned"
      pattern: "setInterval"
---

<objective>
Reduce or explicitly gate the dominant remaining non-E2E runtime bottleneck.

Purpose: Finish the milestone by leaving the residual runtime in one of two acceptable states: materially improved, or explicitly budgeted and gated.
Output: Either a safer faster dominant suite or a documented accepted budget with repo-native enforcement and proof.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/34-non-e2e-runtime-residual-budgeting/34-RESEARCH.md
@.planning/phases/34-non-e2e-runtime-residual-budgeting/34-VALIDATION.md
@.planning/phases/34-non-e2e-runtime-residual-budgeting/34-01-PLAN.md
@package.json
@src/components/resume/resume-builder.tsx
@src/components/resume/resume-builder.test.tsx
@.github/workflows/ci.yml
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Apply the safest material reduction available to the dominant residual suite</name>
  <files>src/components/resume/resume-builder.tsx, src/components/resume/resume-builder.test.tsx</files>
  <read_first>src/components/resume/resume-builder.tsx, src/components/resume/resume-builder.test.tsx</read_first>
  <action>Attempt the smallest safe change that materially reduces the dominant residual suite without weakening the intended async or polling assertions. Prefer deterministic timer control, reduced wall-clock waiting, or a safer test seam over broad behavioral rewrites.</action>
  <verify>npm test -- src/components/resume/resume-builder.test.tsx</verify>
  <acceptance_criteria>
    - the dominant suite remains behaviorally meaningful
    - the dominant suite runtime improves materially, or the attempt establishes why further safe reduction is limited
    - no assertions are removed just to force a lower runtime number
  </acceptance_criteria>
  <done>The dominant residual suite has either improved materially or been reduced to a clearly explained safe boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Make the final runtime contract explicit through repo-native proof and gating</name>
  <files>package.json, scripts/**, .github/workflows/ci.yml, .planning/phases/34-non-e2e-runtime-residual-budgeting/**</files>
  <read_first>package.json, .github/workflows/ci.yml, .planning/phases/34-non-e2e-runtime-residual-budgeting/34-RESEARCH.md</read_first>
  <action>Publish the final runtime contract in a durable form. If the dominant outlier is improved enough, record the new budget and proof path. If not, add an explicit accepted budget or gate that makes future drift visible in local or CI usage instead of leaving the issue implicit.</action>
  <verify>npm run test:profile:non-e2e -- src/components/resume/resume-builder.test.tsx</verify>
  <acceptance_criteria>
    - the repo records the final runtime contract explicitly
    - future drift becomes visible through a named proof path or gate
    - the milestone ends with a clear budget stance instead of another informal runtime note
  </acceptance_criteria>
  <done>The remaining non-E2E runtime bottleneck is either reduced enough or explicitly budgeted and gated.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm test -- src/components/resume/resume-builder.test.tsx`
- [ ] `npm run test:profile:non-e2e -- src/components/resume/resume-builder.test.tsx`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The repo ends Phase 34 with an explicit and durable runtime contract for the dominant residual non-E2E bottleneck
</success_criteria>

<output>
After completion, create `.planning/phases/34-non-e2e-runtime-residual-budgeting/34-02-SUMMARY.md`
</output>
