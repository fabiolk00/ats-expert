---
phase: 34-non-e2e-runtime-residual-budgeting
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/34-non-e2e-runtime-residual-budgeting/**
  - package.json
  - scripts/**
  - src/components/resume/resume-builder.test.tsx
autonomous: true
requirements:
  - PERF-04
user_setup: []
must_haves:
  truths:
    - "Residual runtime work must start from measured evidence, not from assumptions left over from the previous milestone."
    - "The dominant non-E2E suites after Phase 31.1 must be identified in committed artifacts."
    - "The existing profiling path `test:profile:non-e2e` should remain the canonical runtime command unless a stronger repo-native wrapper is clearly needed."
  artifacts:
    - path: "package.json"
      provides: "Canonical runtime profiling command"
      contains: "test:profile:non-e2e"
    - path: "src/components/resume/resume-builder.test.tsx"
      provides: "Current dominant suspected residual suite"
      contains: "polls queued PDF imports"
    - path: ".planning/milestones/v1.4-phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-03-SUMMARY.md"
      provides: "Archived residual runtime baseline"
      contains: "129s"
  key_links:
    - from: "src/components/resume/resume-builder.tsx"
      to: "src/components/resume/resume-builder.test.tsx"
      via: "polling seams and test behavior define the current residual bottleneck"
      pattern: "pdfImportPollMs"
---

<objective>
Re-profile the residual non-E2E runtime outliers and publish the current budget evidence in committed form.

Purpose: Turn the remaining runtime question into a concrete, rerunnable budget snapshot before deciding whether the dominant outlier should be reduced or explicitly accepted.
Output: Committed profiling evidence that identifies the suites still dominating non-E2E runtime after the earlier structural fixes.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/milestones/v1.4-phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-03-SUMMARY.md
@package.json
@src/components/resume/resume-builder.tsx
@src/components/resume/resume-builder.test.tsx
@AGENTS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture a fresh residual-runtime budget snapshot from the canonical profiling path</name>
  <files>.planning/phases/34-non-e2e-runtime-residual-budgeting/**, package.json</files>
  <read_first>package.json, .planning/milestones/v1.4-phases/CURRIA-31.1-reduce-test-suite-runtime-and-add-ci-friendly-performance-pr/31.1-03-SUMMARY.md</read_first>
  <action>Use the existing profiling path to capture the current residual non-E2E budget and identify which suites still dominate after Phase 31.1. Record the evidence in a committed artifact that names the dominant suites, their approximate runtime, and the practical budget the repo is currently paying.</action>
  <verify>npm run test:profile:non-e2e -- src/components/resume/resume-builder.test.tsx</verify>
  <acceptance_criteria>
    - the phase records current residual runtime evidence in a committed artifact
    - the dominant suite or suites are named explicitly instead of implied
    - the evidence reuses the repo-native profiling path instead of an ad hoc command
  </acceptance_criteria>
  <done>The repo has a fresh committed budget snapshot for the post-31.1 residual runtime state.</done>
</task>

<task type="auto">
  <name>Task 2: Convert the residual-runtime snapshot into a focused execution target for Wave 2</name>
  <files>.planning/phases/34-non-e2e-runtime-residual-budgeting/**, src/components/resume/resume-builder.test.tsx, src/components/resume/resume-builder.tsx</files>
  <read_first>src/components/resume/resume-builder.test.tsx, src/components/resume/resume-builder.tsx</read_first>
  <action>Translate the profiling result into one explicit dominant target for Wave 2, including the likely optimization seam or the likely budget-acceptance boundary. If `resume-builder` remains the primary outlier, make that explicit in the phase evidence instead of keeping the target generic.</action>
  <verify>node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" state validate</verify>
  <acceptance_criteria>
    - Wave 2 has one explicit dominant target
    - the target is grounded in fresh profiling evidence
    - the repo state remains coherent while publishing the new budget snapshot
  </acceptance_criteria>
  <done>The next runtime decision is narrowed to one explicit dominant bottleneck instead of a vague broad-suite concern.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `npm run test:profile:non-e2e -- src/components/resume/resume-builder.test.tsx`
- [ ] `node "$HOME/.codex/get-shit-done/bin/gsd-tools.cjs" state validate`
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- No errors or warnings introduced
- The residual non-E2E runtime is captured in a current committed budget snapshot with a named dominant target
</success_criteria>

<output>
After completion, create `.planning/phases/34-non-e2e-runtime-residual-budgeting/34-01-SUMMARY.md`
</output>
